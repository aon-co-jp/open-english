# open-english

> 📌 **آخرین به‌روزرسانی (2026-08-26، ادامه ۲، وظیفه‌ای برای دفعه بعد
> ثبت شد):** درست است که وقتی تقویت جست‌وجو فعال باشد، پاسخ هوش
> مصنوعی نتایج جست‌وجوی Google را منعکس می‌کند — اما محدودیت موجود
> همچنان پابرجاست: مدل‌های کوچک خانواده GPT-2 تضمینی برای استفاده
> دقیق از این زمینه جست‌وجو ندارند. وظیفه‌ای ثبت شد تا در جلسه بعدی
> بررسی شود که آیا بهبودهای مهندسی در Rust و RPoem و open-directx/
> open-cuda و aruaru-llm می‌توانند این محدودیت را کاهش دهند (هنوز
> چیزی بررسی یا پیاده‌سازی نشده است). جزئیات در CLAUDE.md.

> 📌 **به‌روزرسانی (2026-08-26، ادامه): جست‌وجوی GitHub/YouTube +
> اعلان اتمام سهمیه رایگان + پیوندهای دریافت کلید API + پرسش ذخیره در
> پایگاه‌داده محلی.** پنل «🔀 AI Provider Priority» اکنون دارای
> چک‌باکس‌هایی برای جست‌وجوی Google، GitHub (توکن اختیاری) و YouTube
> (نیازمند کلید API) است — نتایج جست‌وجو اکنون واقعاً در هر پیام چت به
> درخواست افزوده می‌شوند (این یک شکاف را برطرف می‌کند که پیش‌تر این
> ویژگی هرگز به روند واقعی گفت‌وگو متصل نبود). هنگامی که سهمیه رایگان
> روزانه همه ارائه‌دهندگان پیکربندی‌شده تمام شود، برنامه به دو زبان
> «سهمیه رایگان امروز تمام شد» را نمایش می‌دهد و به‌طور خودکار به هوش
> مصنوعی محلی سوئیچ می‌کند؛ ارائه‌دهنده با طرح پولی به‌طور خودکار به
> کار خود ادامه می‌دهد. پیوندهای مستقیم به صفحات دریافت کلید API اضافه
> شد. در نسخه دانلودی PC، هنگام ذخیره کلید اکنون به دو زبان پرسیده
> می‌شود که آیا در پایگاه‌داده محلی نیز ذخیره شود. جزئیات در CLAUDE.md.

> 📌 **به‌روزرسانی (2026-08-26): اولویت ارائه‌دهندگان چندگانه LLM.**
> علاوه بر جست‌وجوی Google، اکنون می‌توان ChatGPT (OpenAI)، DeepSeek،
> Gemini و Claude (Anthropic) را به‌صورت جداگانه یا هم‌زمان فراخوانی کرد.
> پنل جدید «🔀 AI Provider Priority» امکان فعال‌سازی «استفاده از سهمیه‌های
> رایگان به ترتیب» را می‌دهد — ترتیب این ۵ سرویس را می‌توان با وارد کردن
> عدد یا کلیک روی دکمه رادیویی شماره‌دار تنظیم کرد. کلیدهای API فقط در
> حافظه محلی همین مرورگر ذخیره می‌شوند و به‌عنوان تنظیم زمان اجرا به
> نمونه aruaru-llm خودتان ارسال می‌شوند (آنجا هم روی دیسک نوشته
> نمی‌شوند). به‌صورت واقعی از طریق درخواست‌های HTTP واقعی به Anthropic
> تأیید شد. جزئیات در CLAUDE.md.

> 📌 **به‌روزرسانی (2026-08-24): «راهنمای شغلی» به دوره خصوصی بر اساس پایه
> تحصیلی اضافه شد.** صفحه تمرین اکنون برای هر درس نشان می‌دهد که تسلط بر
> آن ممکن است به کدام صنعت‌ها/شغل‌ها کمک کند و با ادامه یادگیری، چه
> مشاغل پیشرفته‌ای ممکن است در دسترس باشد — همیشه با لحنی محتاطانه («ممکن
> است کمک کند»، بدون وعده استخدام تضمینی). طراحی بر اساس پژوهش واقعی
> درباره نظام دوگانه آموزش حرفه‌ای آلمان (Berufsschule، مدارک IHK،
> Ausbildung) انجام شده است. جزئیات و منابع در CLAUDE.md.

> 📌 **تازه‌ترین (2026-08-23، ادامه ۴): برنامه اکنون معمای اصیل سازنده را
> پیش روی شما می‌گذارد**: اگر بگویید «یک معما بپرس»، «کوییز بده» یا
> "give me a quiz"، برنامه **معمای اصیل سازنده‌ی خود، آقای ماساهیرو
> ایشیزوکا (石塚正浩)** را مطرح می‌کند. صورت مسئله: با چهار بار نوشتن رقم ۹،
> جای ◯ در `9 ◯ 9 ◯ 9 ◯ 9 = 10` را با یکی از نشانه‌های `+` `-` `×` `÷`
> پر کنید (تکرار یک نشانه آزاد است و در صورت نیاز می‌توانید با پرانتز ( )
> ترتیب عمل‌ها را عوض کنید) تا حاصل دقیقاً ۱۰ شود. این **یک چیستانِ
> بامزه یا پرسشی حقه‌بازانه نیست**: حسابی محض با چهار عمل اصلی است و با
> ماشین‌حساب یا چرتکه هم می‌توان درستی‌اش را آزمود. کم‌سن‌ترین کسی که تا
> امروز آن را حل کرده، کودکی در کلاس اول دبستان بوده است.
> گفت‌وگو **دو مرحله‌ای** است: نخست تنها صورت مسئله نمایش داده می‌شود و
> هرگاه بنویسید «نمی‌دانم» یا «جواب را بگو»، پاسخ نمایان می‌شود.
> صورت مسئله **به‌طور پیش‌فرض به ژاپنی و انگلیسی با هم** می‌آید و اگر
> زبان آموزشی یا زبان مادری‌تان را اسپانیایی، فرانسوی، آلمانی، چینی یا
> کره‌ای انتخاب کرده باشید، ترجمه‌ی همان زبان نیز در آغاز افزوده می‌شود.
> **یادداشت صادقانه**: ترجمه تنها برای همین ۷ زبان (ja/en/es/fr/de/zh/ko)
> آماده شده است؛ ما ۱۳۰ زبانِ پشتیبانی‌شده را با ترجمه‌ی ماشینی پر نکرده‌ایم
> تا وانمود کنیم «همه‌ی زبان‌ها پشتیبانی می‌شوند» (برای زبان‌های نیامده،
> همان صورت پیش‌فرض ژاپنی+انگلیسی نمایش داده می‌شود). ضمناً پرسش و پاسخ
> هر دو **متن ثابت‌اند و از استنتاج هوش مصنوعی نمی‌گذرند** — چون GPT-2 خام
> در حساب، پاسخ‌های نادرست را با اطمینان کامل می‌سازد. این گفت‌وگو از
> سهمیه‌ی روزانه‌ی شما کم نمی‌کند.

> 📌 **تازه‌ترین (2026-08-23، فقط واژه‌گزینی)**: سه بخش از پاسخ «۶۶۶ / نشان
> جانور» با زبانی روان‌تر و دوستانه‌تر بازنویسی شد — بازی واژگانی **«۶۶۶ =
> WWW»**، افسانه‌ی شهری بارکد، و نکته‌ی مکاشفه ۱۳:۱۶-۱۷ در کنار خرید
> امروزی. سلب‌مسئولیت‌های خشک («نه به‌عنوان ادعای تحقق پیشگویی») با لحنی
> گرم‌تر جایگزین شد: «این را یک نکته‌ی سرگرم‌کننده بدانید، نه دلیلی محکم»
> و «چه می‌دانم چه باید از آن برداشت کرد — اینجا کسی چیزی را ثابت نمی‌کند،
> اما از آن هم‌زمانی‌هایی است که متنی کهن را کمی نزدیک‌تر حس می‌کنی».
> **محتوای محدودیت‌ها تغییری نکرده است**: هیچ‌چیز به‌عنوان پیشگویی
> تحقق‌یافته ادعا نمی‌شود، ۶۶۶ بارکد همچنان صراحتاً افسانه‌ای شهری و بی‌پایه
> از نظر فنی خوانده می‌شود (Snopes: نادرست؛ میله‌های محافظ ۳ ماژول در برابر
> ۷ ماژولِ یک رقم) و شباهت Python به «جانور» همچنان تصادفی محض اعلام شده
> است. تنها **سبک نوشتار** عوض شده — برای خوانایی و صمیمیت، نه تضعیف
> صداقت.

> 📌 **آخرین به‌روزرسانی (2026-08-24، ادامه ۶)**: نقل‌قول‌ها/ضرب‌المثل‌ها +
> پیام انگیزشی + مبحث جدید «مهارت‌های ارتباط و پرسشگری» اضافه شد. هر
> جعبه راهنمایی شغلی اکنون یک نقل‌قول/ضرب‌المثل دوزبانه (فارسی/انگلیسی،
> مثل "Strike while the iron is hot") و پیامی انگیزشی محتاطانه (نه یک
> تضمین) نشان می‌دهد که امیدوار است یادگیرنده بتواند شغل پیدا کند، مسیر
> شغلی را تغییر دهد و در هر جا امرار معاش کند. مبحث جدید (دوره متوسطه/
> دبیرستان) عبارت‌های واقعی انگلیسی برای بحث فرضی سازنده و بازخورد
> سازنده آموزش می‌دهد. جزئیات در CLAUDE.md (نسخه ژاپنی).

> 📌 **آخرین به‌روزرسانی (2026-08-23)**: دو **پاسخ متن‌ثابت مبتنی بر
> قاعده** افزوده شد که با دست نوشته شده‌اند (دوزبانه ژاپنی/انگلیسی، بدون
> استنتاج هوش مصنوعی).
> **(۱) اسلام، ایران/پارس و جهان عرب**: به پرسش‌های مربوط به تاریخ و
> ریشه‌ها خلاصه‌ای بی‌طرفانه و مبتنی بر واقعیت داده می‌شود — جامعه‌های
> مسیحی عربستانِ پیش از اسلام (نجران، غسانیان)، شکل‌گیری قرآن که پژوهش
> آن را **سنتی مستقل و جداگانه** توصیف می‌کند، تفاوت تمدن ایرانی و عربی،
> و تأثیر زردشتی که صراحتاً فقط به عنوان **«فرضیه‌ای که برخی پژوهشگران
> مطرح می‌کنند»** آورده شده و نه واقعیتی قطعی. دو ادعا که در آغاز
> خواستِ گنجاندنشان بود (پیدایش قرآن از ترجمه‌ی کتاب مقدس؛ برادر محمد به
> عنوان مترجم) **به سبب نبود پشتوانه در منابع، آگاهانه کنار گذاشته شد**.
> در پایان این پیام می‌آید که موانع زبانی سوءتفاهم می‌آفرینند و **ترجمه‌ی
> خودکار و گفت‌وگوی چندزبانه می‌توانند به تفاهم و صلح یاری رسانند**.
> **(۲) «آیا ۶۶۶ نشان جانور است؟»**: پاسخی سبک در قالب دانستنی. بند
> مکاشفه بی‌طرفانه نقل می‌شود؛ بازی واژگانی امروزی **«۶۶۶ = WWW»**
> (جِماتریای عبری: حرف واو برابر ۶) **صراحتاً به عنوان خوانش برخی افراد و
> نه آموزه‌ای دینی** معرفی می‌گردد؛ داستان «۶۶۶ پنهان در بارکد»
> **صراحتاً افسانه‌ی شهری خوانده می‌شود** و فن‌آوری واقعی‌اش توضیح داده
> می‌شود: خط‌های بلندتر در دو کناره و میانه، **میله‌های محافظ (guard
> bars)** هستند که آغاز، پایان و میانه‌ی کد را به اسکنر نشان می‌دهند؛
> تنها *شبیه* رقم ۶ به نظر می‌رسند اما رمزگذاری‌شان متفاوت است (سه
> ماژول در برابر هفت) — راستی‌آزمایانی مانند Snopes این ادعا را نادرست
> ارزیابی می‌کنند: **نه معنای رمزی دارد و نه پایه‌ی فنی**. سپس
> جمع‌بندی مثبت: وب و اسکنرهای بارکد خرید را آسان کرده‌اند **بی‌آنکه
> کسی نیازمند نشانی بر بدن باشد**؛ و سرانجام این نکته که نشان زبان
> Python یک مار است اما نامش از مجموعه‌ی طنز «سیرک پرنده‌ی مونتی
> پایتون» گرفته شده — شباهت به «جانور» **صراحتاً تنها هم‌زمانی تصادفی و
> بازی با واژه‌ها اعلام شده** و هیچ پیوند معناداری ندارد.
> **افزوده‌ی 2026-08-23**: نکته‌ی جانبی دیگری افزوده شد مبنی بر اینکه در
> مکاشفه ۱۳:۱۶-۱۷ به‌راستی بندی هست که می‌گوید هیچ‌کس بدون آن نشان نمی‌تواند
> بخرد یا بفروشد، و اینکه **برخی افراد میان این و وابستگی روزافزون خرید
> امروزی به بارکد و پرداخت‌های آنلاین مانند Amazon هم‌سانی جالبی می‌بینند**
> — **این تنها به عنوان هم‌زمانی‌ای که به نظر برخی جالب می‌آید مطرح شده و
> هرگز ادعای تحقق یافتن پیشگویی نیست**.
> جزئیات در یادداشت‌های HANDOFF مورخ 2026-08-23 در [CLAUDE.md](CLAUDE.md).

> 📌 **آخرین به‌روزرسانی (2026-08-22)**: **آزمون‌های تمرینی برای
> زبان‌های جهان، رابط انتخاب زبان، و نمایش و خواندن پیاپی چندزبانه**
> افزوده شد. انگلیسی و ژاپنی همچنان پیش‌فرض هستند، اما با یک بنر
> دوزبانه و پنل «🌐 Languages» می‌توان مجموعه‌های تمرینی اصلی را برای
> **۳۸ زبان** (اروپا، خاورمیانه، آسیا، هند و آفریقا) فعال کرد. پس از
> نمره‌دهی، پرسش‌های نادرست دقیقاً مانند مسیر موجود
> Eiken/TOEIC/TOEFL/JLPT به گفت‌وگو با مربی همان زبان می‌رسند. همچنین
> می‌توان **۲ تا ۵ زبان** (شامل انگلیسی و ژاپنی) را برگزید تا یک جمله
> به‌ترتیب نمایش داده و خوانده شود؛ هر چند بار که بخواهید، همراه با
> کپی/چسباندن، دانلود فایل .txt و ذخیره در پایگاه دادهٔ محلی SQLite.
> افشای صادقانه: این‌ها پرسش‌های اصلی نوشته‌شده برای همین برنامه‌اند،
> نه پرسش‌های آزمون‌های واقعی، و هیچ ارتباطی با گواهی‌نامه‌های رسمی
> (DELE، DELF، Goethe-Zertifikat، HSK، TOPIK و…) ندارند. سطح‌های
> سبک CEFR (A1–C2) تنها تخمینی کلی‌اند، شمار پرسش‌ها یکسان نیست (۳ تا ۶
> برای هر زبان)، و خواندن از Web Speech API مرورگر استفاده می‌کند: بدون
> صدای نصب‌شده، متن فقط نمایش داده می‌شود. جزئیات در مدخل HANDOFF
> مورخ 2026-08-22 در [CLAUDE.md](CLAUDE.md).

> 📌 **آخرین به‌روزرسانی (2026-08-18)**: یک پایگاه‌داده محلی واقعی برای
> تاریخچه گفت‌وگو/تنظیمات اضافه شد (SQLite + آینه اختیاری خودترمیم‌گر
> `aruaru-db`/PostgreSQL)، به‌همراه APIهای انتخاب محل ذخیره‌سازی،
> پشتیبان‌گیری با rsync و درون‌ریزی داده‌های قدیمی. اگر `rsync` نصب
> نباشد، برنامه پیام دوزبانه **«Let's install RSync!»** را نمایش می‌دهد
> و می‌تواند آن را به‌صورت خودکار از طریق مدیر بسته سیستم‌عامل نصب کرده
> و بلافاصله پشتیبان‌گیری را اجرا کند. جزئیات در [CLAUDE.md](CLAUDE.md)
> (ورودی‌های HANDOFF مورخ 2026-08-18، به ژاپنی).

> 📌 **به‌روزرسانی قبلی (2026-08-11–12، نسخه v0.6.0)**: اکنون
> اندروید/تبلت به‌طور کاملاً مستقل اجرا می‌شود — بدون نیاز به رایانه
> شخصی یا سرور لینوکس. موتور پاسخ‌دهی هوش مصنوعی (`aruaru-llm`) اکنون
> مستقیماً درون فایل APK بسته‌بندی شده است؛ بررسی روی دستگاه واقعی
> تأیید کرد که هر دو فرآیند فعال باقی می‌مانند و به `/healthz`/
> `/v1/chat` پاسخ می‌دهند. همچنین اضافه شد: بخش آماده‌سازی برای
> آزمون‌های گواهی‌نامه (Eiken 1-5، TOEIC، TOEFL، JLPT N1-N5،
> Nihongo Kentei 1-3، هرکدام ۱۰ سؤال اصلی) که پس از نمره‌دهی، سؤالات
> اشتباه را به مربی هوش مصنوعی می‌سپارد (با تغییر خودکار به حالت
> «کلاس ژاپنی» برای JLPT/Nihongo Kentei)، انتخاب «کدام زبان را یاد
> بگیرید»، و نصب‌کننده‌های لینوکس/macOS (`installer/unix/install.sh`).
> افشای صادقانه: وزن‌های مدل (خانواده GPT-2، مدل embedding) در APK
> گنجانده نشده‌اند — استفاده از چت هوش مصنوعی روی اندروید همچنان نیاز
> به قرار دادن دستی فایل‌های مدل در حافظه داخلی دارد (هنوز دانلود
> خودکار وجود ندارد). به ورودی‌های HANDOFF تاریخ 2026-08-11 (ادامه
> 7-10) در [CLAUDE.md](CLAUDE.md) مراجعه کنید.

> 📌 **به‌روزرسانی قدیمی‌تر (2026-08-11، ادامه 3)**: ویژگی به‌روزرسانی
> خودکار (فقط ویندوز) اضافه شد که هنگام راه‌اندازی، آخرین نسخه را در
> گیت‌هاب بررسی می‌کند و در صورت وجود نسخه جدیدتر، نسخه قدیمی را
> به‌طور خودکار حذف و نسخه جدید را نصب می‌کند. افشای صادقانه: هنوز
> هیچ GitHub Release ای وجود ندارد، بنابراین جریان کامل حذف→نصب
> هنوز به‌طور کامل تأیید نشده است (منطق مقایسه نسخه و مسیر «نسخه‌ای
> یافت نشد، با امنیت ادامه بده» تأیید شده‌اند).

> 📌 **به‌روزرسانی قدیمی‌تر (2026-08-11، ادامه 2)**: تشخیص موضوعات
> جست‌وجوی شغل/تغییر شغل/گردشگری اضافه شد که aruaru.tokyo،
> audiocafe.tokyo/aruaru، audiocafe.tokyo/aruaru-lady و nasa.tokyo را
> به انگلیسی و ژاپنی معرفی می‌کند — هم در چت عادی و هم در حالت آموزش
> کار می‌کند، به‌صورت زنده تأیید شده است.

> 📌 **به‌روزرسانی قدیمی‌تر (2026-08-11، ادامه)**: به یک پایگاه داده
> جغرافیایی/گردشگری جدید متصل شد (تمام ۴۷ استان ژاپن، ۵۰ ایالت آمریکا،
> پایتخت‌های اصلی جهان با جاذبه‌ها/غذا/سوغاتی) تا آموزش خودمعرفی را
> پویا کند. هنگامی که کوه فوجی مطرح می‌شود، برنامه اکنون یک هشدار
> ایمنی دوزبانه نشان می‌دهد (پوشیدن لباس اسکی + کلاه ایمنی، رزرو
> زودهنگام کلبه کوهستانی) به‌همراه اطلاعات واقعی درباره کلبه‌ها/
> اتوبوس‌ها/فروشگاه‌های تجهیزات و جست‌وجوی رزرو تور. رابط انتخاب
> گروه سنی/سطح/انگلیسی تجاری اضافه شد. به‌صورت زنده در برابر
> `aruaru-llm` و سرور استاتیک واقعاً در حال اجرا تأیید شد (۳ باگ
> واقعی پیدا و رفع شد).

> 📌 **به‌روزرسانی قدیمی‌تر (2026-08-11)**: پنل تنظیماتی برای ذخیره
> کلید API/cx جست‌وجوی گوگل خودتان مستقیماً از مرورگر اضافه شد
> (`POST /v1/settings/google-search`، فقط در حافظه، هرگز روی دیسک
> نوشته نمی‌شود). نصب‌کننده ویندوز (`installer/windows/`، Inno Setup)
> واقعاً روی سخت‌افزار واقعی ساخته، نصب، اجرا و حذف شده است (بدون نیاز
> به دسترسی مدیر).

> 📌 **به‌روزرسانی قدیمی‌تر (2026-08-10، ادامه)**: (۱) مدل پیش‌فرض از
> `gpt2` (124M) به `distilgpt2` (82M) تغییر یافت، حدود ۴۲٪ سریع‌تر.
> (۲) تصمیم گرفته شد که کد JS بخش رابط کاربری به Rust/WASM منتقل
> **نشود** (بدون مزیت عملکردی، و `SpeechRecognition` هیچ باندینگ
> استاندارد web-sys ندارد) — در عوض **سرور فایل محلی به Rust منتقل
> شد** (crate جدید `server/`، مبتنی بر `open-runo-poem-compat` از
> RPoem، که وابستگی به `python3 -m http.server` را حذف می‌کند).
> (۳) پردازش ورودی ژاپنی بهبود یافت تا پاسخ‌های ترکیبی (انگلیسی+
> ژاپنی) همیشه تضمین شوند. (۴) مدیریت نسخه اضافه شد (`version.json`
> با فیلد معنایی `version`، نمایش‌داده‌شده در پاورقی) و پاک‌سازی
> خودکار ردپاهای سمت مرورگر از نسخه‌های قدیمی.

> 📌 **به‌روزرسانی اخیر (2026-08-10)**: پشتیبانی CORS اضافه شد، علت
> ریشه‌ای حلقه تکرار معیوب رمزگشایی حریصانه GPT-2 برطرف شد
> (`generate_with_repetition_penalty` از `open-cuda`، جریمه پیش‌فرض
> ۱.۳)، ظاهر شخصیت تورا-سان تنظیم شد + یک جینگل تغییر شخصیت اضافه شد
> + خودمعرفی او اصلاح شد، یک گام آموزشی مبتنی بر فن خدمات مشتری
> واقعی یک مِیدکافه واقعی در آکیهابارا (@ほぉ～むカフェ) اضافه شد، یک
> گام درباره رونق فعلی فرهنگ ژاپنی در خارج از کشور تحقیق و اضافه شد
> (انیمه/مانگا، آهنگ‌های انیمه، بازی‌ها، فراگیران زبان ژاپنی، جمع‌آوری
> گوشوئین، گردشگری آنسن/ریوکان، غذای ژاپنی)، آیکون‌های راه‌انداز برای
> ویندوز/مک/لینوکس/اندروید/آیفون/آیپد اضافه شد و یک مکانیزم به‌روزرسانی
> خودکار پیاده‌سازی شد.

یک برنامه وب (فاز ۰) برای یادگیری مکالمه انگلیسی برای رایانه شخصی/
تبلت/گوشی هوشمند. با سبک «کلاس انگلیسی در میدکافه»، شخصیتی از یک
میدِ جادویی (طراحی اصلی، متحرک) دانش‌آموزان را از سطح کاملاً مبتدی
تا پیشرفته همراهی می‌کند.

## معماری (طبق دستور کاربر، 2026-08-10)

- **سمت لینوکس (VPS)**: فقط یک سرور توزیع برای دانلود (جایی نیست که
  برنامه واقعاً در آن اجرا شود). مدیریت برنامه توسط
  [`open-easy-web`](https://github.com/aon-co-jp/open-easy-web) انجام
  می‌شود.
- **سمت دستگاه کاربر (رایانه شخصی/تبلت/تلفن)**: بخش استاتیک این
  مخزن (HTML/CSS/JS، در مرورگر اجرا می‌شود) + یک سرور بومی که به‌طور
  محلی از [`aruaru-llm`](https://github.com/aon-co-jp/aruaru-llm)
  اجرا می‌شود (که داخلاً از بک‌اندهای استنتاج `open-directx`/
  `open-cuda` استفاده می‌کند)، که کاربر خودش دانلود و اجرا می‌کند.
  مرورگر به‌صورت محلی (آنلاین یا آفلاین) به `http://localhost:4600`
  (پورت پیش‌فرض aruaru-llm) متصل می‌شود — طراحی «ترکیبی».

## دامنه فعلی (فاز ۰) — افشای صادقانه

- **کیفیت پاسخ‌های هوش مصنوعی**: `/v1/generate` از `aruaru-llm`
  تولید متن خودبازگشتی با GPT-2 (124M تا 1.5B، عمدتاً انگلیسی، بدون
  fine-tuning برای مکالمه) انجام می‌دهد. روانی یا تناسب با سطح
  تضمین‌شده نیست — این موضوع روی صفحه اعلام می‌شود، بدون اغراق. از
  2026-08-10، یک جریمه تکرار (پیش‌فرض ۱.۳) باگ حلقه تکرار بی‌نهایتی
  که قبلاً گزارش شده بود را برطرف می‌کند.
- **CORS**: از 2026-08-10 برطرف شده است — سرور HTTP از `aruaru-llm`
  اکنون هدرهای `Access-Control-*` ارسال می‌کند، بنابراین این بخش
  رابط کاربری می‌تواند از یک منشأ دیگر (یا از طریق `file://`) باز شود
  و همچنان به `http://localhost:4600` دسترسی داشته باشد.
- **انتخاب سطح**: انتخاب‌گر سطح از مبتدی تا پیشرفته در رابط کاربری
  وجود دارد، اما اجرای واقعی سطح فقط به یک دستور کوتاه در prompt
  محدود است — GPT-2 تضمینی برای رعایت آن ندارد.
- **صدا/TTS**: Web Speech API واقعی (SpeechSynthesis برای خروجی،
  SpeechRecognition برای ورودی میکروفون) متصل شده است، با تنظیم زیر و
  بمی/سرعت برای هر شخصیت.
- **حالت آموزش**: یک اسکریپت خودمعرفی قطعی (تولیدشده توسط هوش
  مصنوعی نیست) که اکنون همچنین شامل فن مکالمه مبتنی بر کلیدواژه یک
  میدکافه واقعی در آکیهابارا و یک گام که رونق فعلی فرهنگ ژاپنی در
  خارج از کشور را خلاصه می‌کند.
- **آیکون‌های راه‌انداز**: `icons/` + `manifest.json` (PWA) +
  `launchers/` امکان راه‌اندازی برنامه از یک آیکون روی دسکتاپ
  (ویندوز/مک/لینوکس) یا آیکون صفحه اصلی (اندروید/آیفون/آیپد) را
  فراهم می‌کنند.
- **به‌روزرسانی خودکار**: `auto-update.js` هر ۵ ثانیه `version.json`
  را بررسی می‌کند و هنگام تغییر شناسه build، صفحه را دوباره بارگذاری
  می‌کند. **محدودیت شناخته‌شده**: برخی مرورگرها `fetch()` فایل‌های
  محلی را تحت طرح `file://` مسدود می‌کنند — تضمین‌شده است هنگام
  سرویس‌دهی از طریق یک سرور HTTP محلی کار کند، در غیر این صورت
  به‌آرامی غیرفعال می‌شود.

## نصب‌کننده‌های مورد نیاز (اضافه‌شده در 2026-08-17)

برای اجرای open-english، باید دو نرم‌افزار زیر را دانلود و نصب کنید
(نیازی به ساخت از کد منبع نیست، تقریباً یک نصب تک‌ضربه‌ای).

| # | چه چیزی | Windows | Linux | Android/تبلت |
|---|---|---|---|---|
| 1 | **خود open-english** (این مخزن — رابط کاربری استاتیک + سرور توزیع) | [open-english-install.exe](https://github.com/aon-co-jp/open-english/releases/latest/download/open-english-install.exe) | [tar.gz](https://github.com/aon-co-jp/open-english/releases/latest) | [APK](https://github.com/aon-co-jp/open-english/releases/latest) (فایل `.apk` را از فهرست دارایی‌ها انتخاب کنید) |
| 2 | **aruaru-llm** (موتور پاسخ‌دهی هوش مصنوعی — الزامی، بدون آن گفتگو کار نمی‌کند) | [aruaru-llm-windows-x86_64.zip](https://github.com/aon-co-jp/aruaru-llm/releases/latest/download/aruaru-llm-windows-x86_64.zip) | [tar.gz](https://github.com/aon-co-jp/aruaru-llm/releases/latest) | از قبل گنجانده شده (درون APK مربوط به open-english جاسازی شده، نیازی به نصب جداگانه نیست) |

**افشای صادقانه**: پیوندهای "latest" در بالا همیشه به تازه‌ترین
GitHub Release اشاره می‌کنند (اگر نسخهٔ مشخص و ثابتی می‌خواهید، مستقیماً
از [صفحهٔ Releases](https://github.com/aon-co-jp/open-english/releases)
استفاده کنید). هنوز باینری از پیش ساخته‌شده برای macOS برای `aruaru-llm`
وجود ندارد (خود open-english یک tar.gz برای macOS ارائه می‌دهد، اما
`aruaru-llm` فقط برای Linux/Windows ارائه می‌شود) — روی macOS باید
`aruaru-llm` را از کد منبع بسازید.

در Windows/Linux/macOS، پس از نصب، ویژگی به‌روزرسانی خودکار داخلی
(`server/src/self_update.rs`، که در تاریخ 2026-08-19 به Linux و سپس
macOS گسترش یافت) هنگام اجرا، GitHub Releases را بررسی می‌کند و در
صورت وجود نسخهٔ جدیدتر، به‌طور خودکار به‌روزرسانی می‌کند (Windows:
حذف→نصب؛ Linux/macOS: باینری در حال اجرا خودش را جایگزین می‌کند) —
بدون نیاز به هیچ اقدامی از سوی کاربر. پیش از اعمال به‌روزرسانی، از
باینری فعلی نسخهٔ پشتیبان گرفته می‌شود؛ اگر نسخهٔ جدید در بازهٔ زمانی
کوتاهی به نقطهٔ پایانی جدید `/healthz` پاسخ ندهد، به‌طور خودکار به نسخهٔ
پشتیبان بازگردانده می‌شود (rollback). **افشای صادقانه**: Android/
iPhone/iPad از این مکانیزم به‌روزرسانی/بازگشت خودکار مستثنی هستند
(سیستم‌عامل اجازهٔ نصب بی‌صدای APK را نمی‌دهد) — در آنجا همچنان نصب
دستی توسط کاربر لازم است و مسیر بازگشتی وجود ندارد.

همچنین صفحهٔ ورودی جدیدی به نام `facebook.html` برای کاربرانی که طرح
موبایل آن‌ها فقط اجازهٔ دسترسی به فیسبوک را می‌دهد اضافه شده است —
افشای صادقانه: این یک همکاری رسمی از نوع "Free Basics" با Meta نیست،
بلکه صرفاً یک صفحهٔ عادی است که از مرورگر داخلی فیسبوک قابل دسترسی است
و به نصب‌کننده‌های موجود اشاره می‌کند.

*(یادداشت ترجمهٔ ماشینی: این پاراگراف توسط خود عامل هوش مصنوعی ترجمه
شده و توسط یک گویشور بومی بازبینی نشده است.)*

## نحوه اجرا

۱. [`aruaru-llm`](https://github.com/aon-co-jp/aruaru-llm) را با
   `cargo run --release` اجرا کنید (پیش‌فرض `http://localhost:4600`،
   مدل پیش‌فرض اکنون `distilgpt2`).
۲. در پوشه `server/`، `cargo run --release` را اجرا کنید تا بخش
   استاتیک این مخزن در آدرس `http://127.0.0.1:4601/` سرویس‌دهی شود
   (مبتنی بر RPoem — دیگر نیازی به `python3 -m http.server` نیست؛
   پورت با متغیر محیطی `OPEN_ENGLISH_SERVER_BIND` قابل تغییر است).
۳. `http://127.0.0.1:4601/` را در مرورگر باز کنید. باز کردن مستقیم
   `index.html` از طریق `file://` هنوز کار می‌کند، اما برخی مرورگرها
   در آنجا `fetch()` را مسدود می‌کنند و به‌روزرسانی خودکار را غیرفعال
   می‌کنند — سرور مرحله ۲ توصیه می‌شود.

## گام‌های بعدی

۱. ~~پشتیبانی CORS در سمت `aruaru-llm`~~ **انجام‌شده (2026-08-10)**.
۲. ~~حلقه تکرار رمزگشایی حریصانه GPT-2~~ **علت ریشه‌ای برطرف شد
   (2026-08-10، جریمه تکرار)**.
۳. ~~سرعت‌بخشی به مدل پیش‌فرض~~ **انجام‌شده (2026-08-10، تغییر به
   distilgpt2، حدود ۴۲٪ سریع‌تر)**.
۴. ~~تضمین پاسخ‌های ترکیبی برای ورودی ژاپنی~~ **انجام‌شده
   (2026-08-10)**.
۵. ~~انتقال سرور فایل محلی به Rust~~ **انجام‌شده (2026-08-10، crate
   `server/`)**. انتقال خود کد JS رابط کاربری به Rust/WASM ارزیابی و
   کنار گذاشته شد (بدون مزیت عملکردی — به `CLAUDE.md` مراجعه کنید).
۶. افزودن پرداخت نهایی TTS/هماهنگی لب.
۷. پیاده‌سازی برنامه درسی بر اساس سطح (گرامر، فهرست‌های واژگان و
   غیره).
۸. **(طبق دستور کاربر، 2026-08-10)** یک ایده آینده برای اجرای
   `open-directx`/`open-cuda`/`aruaru-llm` مستقیماً در مرورگر (WASM/
   WebGPU) و یکپارچه‌سازی با `RPoem` (یک پلتفرم GraphQL Federation).
   این یک جهت معماری بزرگ و جداگانه نسبت به طراحی فعلی فاز ۰ است که
   تا پس از تکمیل MVP به تعویق افتاده است.
۹. بررسی اینکه آیا فناوری‌های Toshiba SBM یا خانواده DeepSeek کاربرد
   واقعی اینجا دارند (هنوز شروع نشده است).

---

زبان‌های دیگر: [日本語](README.md) · [English](README-English.md) ·
[Deutsch](README-German.md) · [Italiano](README-Italian.md) ·
[Français](README-French.md) · [Русский](README-Russian.md) ·
[Українська](README-Ukrainian.md) · [עברית](README-Hebrew.md)

---

## 🎓 دورهٔ معلم خصوصی: ذخیرهٔ تاریخچهٔ یادگیری (پیش از شروع توصیه می‌شود)

**پیش از استفاده از این دوره، راه‌اندازی یک پایگاه‌داده برای تاریخچهٔ یادگیری را
توصیه می‌کنیم** — **aruaru-db** یا یک **سرور معمولی PostgreSQL**. در این صورت
ثبت می‌شود که چه پایه‌ها و درس‌هایی را خوانده‌اید، کدام تمرین‌ها را درست پاسخ
داده‌اید و نتیجهٔ آزمون‌ها چه بوده است، و بعدها می‌توانید مسیر پیشرفت خود را
مرور کنید. بدون پایگاه‌داده، نتیجه‌ها فقط در فایل محلی SQLite داخلی می‌مانند
(با نصب دوباره یا تغییر دستگاه از دست می‌روند) و انتخاب پایه و درس فقط در
localStorage مرورگر ذخیره می‌شود.

هر دو با یک تنظیم انجام می‌شوند: `OPEN_ENGLISH_DATABASE_URL` با رشتهٔ اتصال
PostgreSQL (aruaru-db همان پروتکل را می‌فهمد). **تذکرهای صادقانه**: اتصال بدون
TLS انجام می‌شود، بنابراین سرویس‌های مدیریت‌شدهٔ PostgreSQL که SSL لازم دارند
هنوز کار نمی‌کنند، و PostgreSQL معمولی را ما به‌صورت سرتاسری آزمایش نکرده‌ایم.

- **پیکربندی دوگانه (DUAL) — aruaru-db + PostgreSQL**: دو پایگاه‌داده که از هم
  نسخهٔ پشتیبان دارند، در برابر خرابی یک طرف محافظت می‌کنند. **وضعیت واقعی**:
  open-english در SQLite و *یک* پایگاه‌داده می‌نویسد؛ **نوشتن هم‌زمان در دو
  پایگاه‌داده هنوز پیاده‌سازی نشده است**. امروز DUAL از طریق
  `DUAL_DATABASE_URL` خودِ aruaru-db به دست می‌آید.
- **پشتیبان‌گیری با rsync**: می‌توان پایگاه‌داده را از پنل
  «💾 Data & Model Storage» به دیسک خارجی، رایانهٔ دیگر یا سرور *افزود*
  (خودکار نیست). **یافتهٔ صادقانه**: در `open-easy-web` هیچ سازوکار rsync
  نیافتیم؛ آنچه واقعاً در دسترس است، قابلیت داخلی open-english است.
- **گوگل درایو**: با [rclone](https://rclone.org/drive/) می‌توانید پوشهٔ پشتیبان
  را همگام کنید: `rclone sync /path/to/backup gdrive:open-english-backup`.
  **هیچ‌چیز خودکار همگام نمی‌شود**؛ راه‌اندازی با خود شماست.
- **میزبانی اشتراکی / VPS**: هر میزبانی که با SSH در دسترس باشد (طرح‌های دارای
  SSH مانند Lolipop یا Sakura Internet و VPSهایی مانند ConoHa) با rsync کار
  می‌کند: `rsync -avz /path/to/backup user@your-vps-host:/backup/open-english/`.
