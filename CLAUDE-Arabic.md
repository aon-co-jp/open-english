# فلسفة التطوير وقواعد بيئة العمل (open-english)

> **ملاحظة**: هذه ترجمة مختصرة للحالة الراهنة. سجل HANDOFF التاريخي
> المفصّل (عشرات الإدخالات منذ 2026-08-10) يبقى متاحًا باليابانية فقط
> في [CLAUDE.md](CLAUDE.md) للإيجاز — راجعه هناك لتفاصيل كل جلسة.

قرص العمل: `F:\runo`. يتبع هذا القسم ممارسة نسخ الجزء المقابل من
`CLAUDE.md` الخاص بـ
[`open-raid-z`](https://github.com/aon-co-jp/open-raid-z) إلى كل مشروع
كمصدر موحّد. مستودع GitHub:
[aon-co-jp/open-english](https://github.com/aon-co-jp/open-english).

**بدء التطوير: 2026-08-10.**

## دور هذا المشروع

تطبيق ويب لتعلّم المحادثة الإنجليزية يعمل على أجهزة الكمبيوتر/الأجهزة
اللوحية/الهواتف الذكية. بأسلوب "درس إنجليزي في مقهى خادمات"، تقوم شخصية
خادمة ساحرة (تصميم أصلي، متحركة) بتدريب المتعلمين من المبتدئ تمامًا إلى
المستوى المتقدم. تُدار استجابات الذكاء الاصطناعي بواسطة
[`aruaru-llm`](https://github.com/aon-co-jp/aruaru-llm).

## البنية المعمارية (بناءً على تعليمات المستخدم، 2026-08-10)

- **جانب Linux (VPS)**: خادم توزيع للتنزيل فقط. إدارة التطبيق تتم عبر
  [`open-easy-web`](https://github.com/aon-co-jp/open-easy-web).
- **جانب جهاز المستخدم**: الواجهة الأمامية الثابتة لهذا المستودع + خادم
  أصلي يعمل محليًا من `aruaru-llm` (يستخدم داخليًا محرك الاستدلال
  الخاص بـ `open-directx`/`open-cuda`)، يقوم المستخدم بتنزيله وتشغيله
  بنفسه، ويتصل المتصفح محليًا (متصلاً أو غير متصل بالإنترنت) بـ
  `http://localhost:4600` — تصميم هجين.

## إفصاح صادق / قيود معروفة (اعتبارًا من 2026-08-10)

- `/v1/generate` الخاص بـ `aruaru-llm` مبني على GPT-2 (يركز على
  الإنجليزية، بدون ضبط دقيق للحوار) — جودة الاستجابة والالتزام بالمستوى
  غير مضمونين.
- لم يكن خادم HTTP الخاص بـ `aruaru-llm` يرسل رؤوس CORS في البداية، مما
  يحجب `fetch()` عند تقديم هذه الواجهة من أصل مختلف (تم إصلاح ذلك لاحقًا
  عبر `.with_cors()`؛ راجع التحديثات الأحدث في `CLAUDE.md` الياباني).
- تحويل النص إلى كلام (TTS) ومزامنة الشفاه ما زالا محدودين (حلقة CSS
  بسيطة لفتح/إغلاق الفم في البداية، ثم تم لاحقًا تفعيل Web Speech API
  الحقيقية).

## الرؤية المستقبلية (بناءً على تعليمات المستخدم، 2026-08-10، لم تبدأ بعد)

هناك فكرة لتشغيل `open-directx`/`open-cuda`/`aruaru-llm` أيضًا بشكل
مستقل داخل المتصفح (WASM/WebGPU) والتكامل مع `RPoem` (منصة GraphQL
Federation). بما أن هذا يمثل تغييرًا معماريًا كبيرًا مقارنة بتصميم
المرحلة 0 الحالي (خادم مقيم محلي + اتصال localhost)، سيُعالَج فقط بعد
اكتمال المنتج الأولي (MVP)، بنطاق عمل منفصل.

---

## تحديثات أخرى (2026-08-19 إلى 2026-08-20)

منذ ذلك الحين أُضيف، من بين أمور أخرى: إشعار عند بلوغ حد الاستخدام
اليومي مع توجيه المستخدم إلى الخطط المدفوعة والحصص المجانية لدى مزودين
آخرين؛ لوحة جديدة "Data & Model Storage" بواجهة للنسخ الاحتياطي
والاستعادة؛ لافتة تشجّع على تعبئة الهواتف الذكية غير المستخدمة
(`PhoneAccelWorker` مع اكتشاف NNAPI)؛ نسخ احتياطي مشترك عبر rsync
لـ aruaru-db/PostgreSQL؛ صفحة خارطة طريق أولية لمنافذ الأجهزة
(PlayStation/Switch/Wii/Wii U، بانتظار موافقة الجهات المالكة
للمنصات)؛ توسيع التحديث التلقائي ليشمل جميع المكوّنات المرفقة
(aruaru-llm/aruaru-db) مع فحص دوري كل 6 ساعات وإمكانية الرجوع اليدوي
لإصدار سابق لكل مكوّن على حدة؛ إصلاح يجعل aruaru-llm يبدأ تلقائيًا
على Windows (دون أي خطوة يدوية متبقية)؛ لافتة معلومات عن الحصص
المجانية لمزوّدي الذكاء الاصطناعي/البحث (بما في ذلك
Claude/Anthropic)؛ إصلاح حرج يمنع فقدان قاعدة بيانات المحادثات أثناء
التحديث/الرجوع التلقائي على Windows (تم التحقق منه شاملاً عبر مثبِّت
حقيقي)؛ وميزات جديدة في Android (اكتشاف تلقائي لعنوان URL الخاص
بالكمبيوتر عبر مسح الشبكة الفرعية، تم التحقق منه على جهاز حقيقي مزوّد
بمعالج Snapdragon مع اكتشاف ناجح لـ NNAPI، مع بقاء نقل الحوسبة الفعلي
عبر NNAPI غير منفَّذ بعد). التفاصيل في سجلات HANDOFF بتاريخ
2026-08-19 و2026-08-20 في [CLAUDE.md](CLAUDE.md).

## آخر التحديثات المتعلقة بالتثبيت (2026-08-19)

مثبِّت ويندوز موحّد `open-english-install.exe`؛ تم توسيع التحديث
التلقائي المدمج (`server/src/self_update.rs`) ليشمل Linux ثم macOS،
مع تراجع تلقائي (rollback) يعتمد على فحص سلامة `/healthz` إذا فشلت
النسخة الجديدة في بدء التشغيل بشكل صحيح؛ صفحة دخول جديدة `facebook.html`
للمستخدمين الذين لا يمكنهم الوصول إلا إلى Facebook (إفصاح صادق: هذه
ليست شراكة رسمية مع Meta)؛ ومنذ 2026-08-18، يشجّع التطبيق على تثبيت
`rsync` ويقوم بأتمتة ذلك عند غيابه، ثم يبدأ النسخ الاحتياطي مباشرة بعد
ذلك. التفاصيل الكاملة متوفرة فقط في سجلات HANDOFF اليابانية أدناه.

*(ملاحظة حول الترجمة الآلية: تمت ترجمة هذا الملخص بواسطة وكيل الذكاء
الاصطناعي نفسه، دون مراجعة من متحدث أصلي للغة.)*

---

لغات أخرى: [日本語 (الأصل، مع سجل HANDOFF كامل)](CLAUDE.md) ·
[Deutsch](CLAUDE-German.md) · [Italiano](CLAUDE-Italian.md) ·
[Français](CLAUDE-French.md) · [Русский](CLAUDE-Russian.md) ·
[Українська](CLAUDE-Ukrainian.md) · [עברית](CLAUDE-Hebrew.md) ·
[فارسی](CLAUDE-Persian.md)

## تحديث 2026-08-22: اختبارات لغات العالم، اختيار اللغات، القراءة متعددة اللغات

قبل التنفيذ جرى فحص الشيفرة: **لم تكن هناك أي قائمة بلغات الترجمة ولا
جدول i18n** (لم يعرف `learn-target` سوى الإنجليزية واليابانية). لذلك
عُرِّفت قائمة اللغات من جديد ضمن هذه الميزة — **38 لغة** (17 أوروبية
منها الرومانشية في سويسرا، والروسية، و4 من الشرق الأوسط، و7 من جنوب
آسيا، و8 من شرق وجنوب شرق آسيا، وواحدة أفريقية)، إضافة إلى الإنجليزية
واليابانية المفعّلتين دائمًا.

- بيانات جديدة: `world-language-exams.json` (أسئلة اختيار من متعدد أصلية
  بمستويات على نمط CEFR، 3–6 لكل لغة) و`world-language-phrases.json`
  (5 جمل أساسية × 40 لغة).
- واجهة برمجية جديدة: `GET /v1/world-languages` تعيد الملخّص فقط دون نص
  الأسئلة. ولم يُنشأ أي مسار جديد للحفظ في قاعدة البيانات — يكفي
  `POST /v1/db/history` الموجود مسبقًا.
- الواجهة: لافتة ثنائية اللغة، لوحة "🌐 Languages" بمربعات اختيار، و"تحديد
  الكل" و"إلغاء الكل عدا الإنجليزية واليابانية"، واختيار **من 2 إلى 5
  لغات** (الحد مفروض في الواجهة)، وعرض وقراءة متتابعان قابلان للتكرار
  بلا حد (الكل أو لغة واحدة)، والنسخ، وتنزيل .txt، والحفظ في SQLite.
- بعد التصحيح تقود الأسئلة الخاطئة إلى محادثة مع مدرّس تلك اللغة (بإعادة
  استخدام `examPrepMissedQuestions`).

**إفصاح أمين**: هذه أسئلة أصلية لا صلة لها بأي شهادات رسمية؛ 3–6 أسئلة
فقط لكل لغة؛ لم تُراجَع من متحدثين أصليين؛ ولم تُختبر القراءة الفعلية
بـ38 لغة بأصوات حقيقية (بل عبر بديل للمراقبة فقط). التفاصيل في سجل
HANDOFF بتاريخ 2026-08-22 في [CLAUDE.md](CLAUDE.md).

## تحسين صياغة نصّ ٦٦٦ (2026-08-23، أسلوب فقط)

أُعيدت كتابة ثلاثة مواضع في `MARK_OF_BEAST_TEXTS` بلغة أخفّ: (١) التورية
**«٦٦٦ = WWW»**، (٢) أسطورة الباركود الحضرية، (٣) الملاحظة عن سفر الرؤيا
١٣: ١٦-١٧ إلى جوار التسوّق بالباركود وAmazon.

- **قبل**: «…لا بوصفه تقريرًا بأن نبوءةً تحقّقت».
- **بعد**: «خذوا هذا طُرفةً مسلّية لا دليلًا راسخًا»؛ «من يدري ما المقصود —
  لا أحد يُثبت شيئًا هنا، لكنها من المصادفات التي تجعل نصًّا قديمًا يبدو
  أقرب قليلًا».

**القيود محفوظة كما هي** ويجب الالتزام بها: (١) لا شيء يُقرَّر بوصفه نبوءةً
متحقّقة؛ (٢) ولا يزال ٦٦٦ الباركود يُسمّى صراحةً أسطورةً حضرية بلا أساس
تقنيّ (Snopes: خاطئة؛ أشرطة الحراسة ثلاث وحدات مقابل سبع للرقم الواحد)؛
(٣) ولا يزال تشابه شعار أفعى Python مع «الوحش» **محض مصادفة**؛ (٤)
والتفسيرات تُعرض عرضًا فقط ولا تُلقَّن بوصفها عقيدة. تغيّر **أسلوب النثر
وحده** — من أجل سهولة القراءة، لا لإضعاف الأمانة.

## ردود بنصّ ثابت قائمة على قواعد في الموضوعات الحسّاسة (2026-08-23)

في الموضوعات التي يكون فيها الخطأ الوقائعي أشدّ ضررًا، يردّ التطبيق **دون
استدلال بالذكاء الاصطناعي**، بنصّ مكتوب يدويًا. ثلاث حالات تتبع النمط
نفسه: `isCreatorQuestion()` (التعريف بالمؤلف)،
`isReligionHistoryQuestion()` / `RELIGION_HISTORY_TEXTS` (الإسلام وإيران
والعالم العربي)، و`isMarkOfBeastQuestion()` / `MARK_OF_BEAST_TEXTS` (٦٦٦
وعلامة الوحش). السبب: نموذج GPT-2 المجرَّد يختلق المحتوى، والضرر في تاريخ
الأديان كبير. لا تُستهلك الحصّة اليومية، والنصوص محفوظة في جدول مفتاحه رمز
اللغة (حاليًا `ja` و`en` فقط).

**تاريخ ديني محايد**: التنوّع الديني في جزيرة العرب قبل الإسلام؛ ونشأة
القرآن التي تصفها الدراسات بأنها **تقليد مستقل** (وفي العقيدة الإسلامية
وحيٌ أُنزل على محمد)؛ والفرق بين الحضارتين الإيرانية والعربية؛ والأثر
الزرادشتي بوصفه فرضية بعض الباحثين فقط. أما الادّعاءان المطلوبان في
البداية (القرآن من ترجمة الكتاب المقدس؛ وأخو محمد مترجمًا) فقد
**استُبعدا لانعدام السند** بعد مراجعات متعدّدة، والنصّ يُفصح *لماذا* لا
يقرّرهما. والملاحظة عن حدود الترجمة قبل الحديثة تخصّ **فقط** الترجمات
العربية الموثّقة تاريخيًا للكتاب المقدس، و**لا يجوز** ربطها بنشأة القرآن
البتّة. وفي الختام رسالة الترجمة والحوار متعدد اللغات والسلام.

**٦٦٦ / علامة الوحش**: يُذكر نصّ سفر الرؤيا بحياد دون تقرير تفسير
«صحيح». وتُعرض التورية **«٦٦٦ = WWW»** (الجيماطريا، الواو = ٦) **بوصفها
قراءةً شاعت منذ التسعينيات** لا عقيدة. وحكاية ٦٦٦ المخبّأة في الباركود
**تُوصف بأنها أسطورة حضرية** وتُشرح تقنيًا: **أشرطة الحراسة** تدلّ الماسح
على البداية والنهاية والمنتصف، وتشبه الرقم ٦ في المظهر فقط، وترميزها
مختلف (ثلاث وحدات مقابل سبع)؛ ويصنّفها Snopes بأنها خاطئة — **لا معنى
باطنيًا ولا أساس تقنيًا**. ثم خاتمة إيجابية (الشبكة والماسحات يسّرتا
التسوّق **دون علامة على الجسد**) وملاحظة Python (شعار الأفعى، والاسم من
مونتي بايثون) **المُعلَنة محض مصادفة**.

**إضافة بتاريخ 2026-08-23 (تشابه طريف — عرضٌ لا تقرير)**: يتضمّن سفر
الرؤيا ١٣: ١٦-١٧ فعلًا نصًّا يقول إن من لا يحمل العلامة لا يستطيع أن يشتري
أو يبيع (ووجود هذه الآية نفسه يجوز إيراده بوصفه واقعة). أُضيفت جملة مفادها
أن **بعض الناس يلاحظون تشابهًا طريفًا** بين ذلك وبين اعتماد التسوّق الحديث
المتزايد على الباركود والمدفوعات الإلكترونية مثل Amazon. **قيد إلزامي**:
يُعرض ذلك بوصفه احتمالًا فقط («يقول بعضهم ذلك»)، و**لا يجوز أبدًا** التقرير
بأن نبوءةً قد تحقّقت — والنصّ نفسه يذكر ذلك صراحةً.

**عند التعديل**: لا يجوز إضعاف أو حذف ضمانات الأمانة الأربع — «العرض لا
التقرير»، و«الأسطورة تُسمّى أسطورة»، و«المصادفة تُعلَن مصادفة».

## لغز أصلي من ابتكار المؤلّف (2026-08-23، تكملة ٤)

بناءً على تعليمات المستخدم، حين يطلب المستخدم «اطرح عليّ سؤالًا» /
«أعطني لغزًا» / "give me a quiz"، يطرح التطبيق **لغزًا أصليًا من ابتكار
مؤلّفه، السيد ماساهيرو إيشيزوكا**. وتتبع هذه الميزة **النمط القائم على
القواعد نفسه** الذي تتبعه `isCreatorQuestion()` /
`isReligionHistoryQuestion()` / `isMarkOfBeastQuestion()`: نصّ ثابت لا
يمرّ باستدلال الذكاء الاصطناعي (`aruaru-llm`)، ولا يستهلك الحصّة اليومية.

**نصّ اللغز**: استعمل الرقم ٩ أربع مرّات، واملأ مواضع ◯ في
`9 ◯ 9 ◯ 9 ◯ 9 = 10` بإحدى العلامات `+ - × ÷` (يجوز تكرار العلامة نفسها،
ويجوز تغيير أولوية العمليات بالأقواس ( )) لتكون النتيجة ١٠.
**الجواب**: `(9 × 9 + 9) ÷ 9 = 10` (٩×٩=٨١، ٨١+٩=٩٠، ٩٠÷٩=١٠) —
**تحقّقنا من الحساب**؛ ومن أراد إضافة حلّ آخر فعليه أن يتحقّق بنفسه.

**خاصّيةٌ لا يجوز إسقاطها من النصّ أبدًا**: هذا **ليس لغزًا فكاهيًا ولا
سؤالًا مخادعًا**، بل يُحلّ بالعمليات الحسابية الأربع وحدها (ويمكن التحقّق
بالآلة الحاسبة أو بالمِعداد). فبغير هذه الجملة يضيع المستخدم في اتجاهات
بعيدة كـ«إمالة الرقم ٩ على جنبه». وكذلك حكاية «أصغر من حلّه كان طفلًا في
الصفّ الأول الابتدائي» واقعةٌ حقيقية رواها المؤلّف، **فلا يُضاف إليها
شيء من الخيال**.

**لماذا لا نترك التوليد للذكاء الاصطناعي**: لو تُرك طرح المسائل وتصحيحها
لنموذج GPT-2 المجرَّد لأخرج «أكاذيب مقنعة» بحسابات غير صحيحة. ولهذا
اقتُصر على نصّ ثابت في السؤال والجواب معًا (وهو سبب اعتماده في تاريخ
الأديان و٦٦٦).

**التنفيذ**: `isQuizRequest()` (كشف طلب اللغز)، `isQuizAnswerRequest()`
(كشف «لا أعرف» / «أخبرني بالجواب»)، وجدول الترجمات `QUIZ_TEXTS`، وبناء
النصوص `quizQuestionText()` / `quizAnswerText()`، وتحديد اللغة
`quizPreferredLangCode()` في `app.js`.
**تبادل من مرحلتين**: تُحفظ الحالة برايةٍ واحدة على مستوى الوحدة هي
`quizAwaitingAnswer` (على غرار `examPrepMissedQuestions`، دون بناء آلة
حالات): أولًا **نصّ اللغز وحده**، ثم الجواب عند قول «لا أعرف». وفي معالج
الإرسال يأتي فحص «بانتظار الجواب» **قبل** `isQuizRequest`. وكلمات
`isQuizAnswerRequest()` عامّة («لا أعرف») لكنها لا تُفحص إلا حين
`quizAwaitingAnswer === true`، فلا تخطف تدريب المحادثة المعتاد.

**تعدّد اللغات وحدوده بصدق**: الافتراضي **اليابانية + الإنجليزية**. وإن
وُجدت في `QUIZ_TEXTS` ترجمةُ اللغة التي اختارها المستخدم («اللغة التي
أريد تعلّمها» `world:<code>` لها الأولوية العليا، وإلا فاللغة الأمّ)
أُضيفت في المقدّمة. والمتوفّر حاليًا **سبع لغات فقط: ja / en / es / fr /
de / zh / ko**؛ ولم نملأ اللغات الـ١٣٠ المدعومة بترجمة آلية عمدًا لئلا
يبدو الأمر «دعمًا كاملًا» — فمن كانت لغته غير مُدرجة يُطرح عليه اللغز
بالصيغة الافتراضية. ولإضافة ترجمة يكفي إدخال مفتاح برمز اللغة في
`QUIZ_TEXTS` (والرموز الفرعية مثل `zh-Hant` تستعمل ترجمة الرمز الأساس
`zh`).

**الخطوة التالية**: إذا أفادنا المؤلّف بلغز أصلي آخر، يُوسَّع
`QUIZ_TEXTS` إلى **مصفوفة من المسائل** (الحالي مسألة واحدة، وحتى عند طلب
«لغزًا آخر» يعود اللغز نفسه).
