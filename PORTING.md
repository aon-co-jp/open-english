# PORTING.md — open-english を他プロジェクトへお引越しする際のガイド

他プロジェクトへ`open-english`の実装パターンを移植する際の要点をまとめる。

## 0-y. aruaru-llmのModel Folding機能(2026-09-01追記)——「DeepSeekの
折りたたみ理論」は実在しないと判明

`aruaru-llm`側`POST /v1/models/fold-layers`(層冗長性検出・実際の
折りたたみ)を紹介するUI開示文を`index.html`のセットアップパネルへ
追記した。**実装本体はこのリポジトリではなく`aruaru-llm`/`open-cuda`
側**であり、移植時にこの機能を持ち込みたい場合は、そちらの
`CLAUDE.md`(2026-09-01エントリ)・`README.md`(API節)を参照する
こと。このリポジトリ側で行うのは、その機能の存在・正直な限界
(「DeepSeekの折りたたみ理論」という技術自体は実在しない、という
調査結果を含む)をUI上でユーザーへ伝える開示文の追加のみ。

## 0-x. 検索拡張QAプロンプトには「生の発話」だけを渡す(2026-08-27追記、
実機E2Eテストで発見したバグの教訓)

検索結果を踏まえたQA形式プロンプト(`"Search results:\n{context}\n\n
Question: {question}\nAnswer:"`)を組み立てる際、`{question}`部分に
**アプリ側で既に別のペルソナ・システムプロンプトでラップ済みの
テンプレート文字列**(例: 「あなたは○○のトレーナーです…Student: {発話}
\nTrainer:」)をそのまま渡してはならない。渡すと`Question: {ペルソナ
全文...Trainer:}\nAnswer:`という、質問の中に別の生成キュー
(`Trainer:`)が入れ子になった、モデルが混乱しやすいプロンプトになる
(`open-english/app.js`で実機テストにより発見・修正済み)。**移植時は
必ず、QAプロンプトの「質問」には利用者が実際に入力した生のテキスト
だけを渡すこと**——ペルソナ・システム指示はQAプロンプトの外側(例えば
検索結果コンテキストと一緒に別途)で扱うか、そもそも検索拡張時は
省略する設計にする。**正直な補足**: この教訓はクライアント側で完全に
制御できる経路にのみ適用済みで、サーバー側が生の発話とラップ済み
テンプレートを区別できない設計のAPI(`req.prompt`しか受け取らない等)
には、API自体の変更(生発話を別フィールドで送る等)が必要になる場合が
ある。

## 0-y. バックアップ用の第2メールアドレスによるOTPログイン(2026-08-27追記)

`server/src/auth.rs`の`request_otp(email1, email2: Option<&str>)`
パターン。単一メールアドレスへのワンタイムコード送信に、任意の
バックアップアドレスを追加する設計。移植時のポイント:

1. **同じコードを両方へ送る**(別々のコードにしない)。理由: 別々の
   コードにすると「両方とも正しく入力しないとログインできない」二段階
   認証になってしまい、可用性向上という当初の目的と異なる仕組みに
   なる。今回は「どちらか一方が届けば十分」という**可用性目的**の
   設計を選んだ(セキュリティ強化が目的なら別々のコードにする二段階
   認証パターンの方が適切——移植先プロジェクトの目的に応じて選ぶこと)。
2. **保存は両方のメールアドレスをキーにして同じ`OtpEntry`を複製する**
   (`otps.insert(email1, entry.clone()); otps.insert(email2, entry)`)。
   これにより検証関数(`verify_otp`)側の変更は不要——どちらのメール
   アドレスで呼ばれても既存のロジックのまま通る。
3. **重複排除**: `email2`が空文字列、または`email1`と大文字小文字を
   無視して同一の場合は、実質的に送信をスキップする(同じアドレスへ
   同じメールを2通送らない)。
4. **後方互換**: リクエストボディの`email2`フィールドは
   `#[serde(default)]`で任意にし、`email2`を送らない既存クライアント
   もそのまま動くようにする。
5. **UI側**: 検証(コード入力)画面で「どちらのメールアドレスで
   受け取ったか」を選べる入力欄を用意する(既定値は第1アドレス、
   利用者が第2アドレスで受け取った場合は書き換えられるようにする)。
6. **正直な開示、必ず明記すること**: この設計は「二段階認証」では
   ない。UIにもコードコメントにも、「片方だけ届けば認証を突破できる、
   可用性目的であってセキュリティ強化ではない」旨を明記すること
   (誤解を招く実装をしないための必須事項)。

## 0-z. ブラウザから直接サードパーティAPIを叩く「visitor-supplied
credentials」パターン(2026-08-26追記、フリーランス開発コーナーの
GitHub連携で採用)

サーバー側でAPIトークンを保存・仲介させたくない機能(このリポジトリでは
GitHubへのリポジトリ作成+ファイルpush)は、`fetch()`をブラウザから
直接そのサービスのAPIドメインへ投げ、トークンは`localStorage`に留める
設計にすると、自前サーバーの認証情報保管・漏洩リスクを増やさずに済む。
`aruaru-llm`側の`google_search_api_key`/`google_search_cx`(訪問者
自身のGoogle Custom Search資格情報を、共有インスタンスのGoogle API
クォータを消費させずリクエスト単位で使う設計、2026-08-25導入)と同じ
考え方の応用。**移植時の注意**:
1. 対象APIがブラウザからの直接呼び出し(CORS許可)に対応しているか
   事前確認が必須(GitHub REST APIは`api.github.com`がCORSを許可して
   いるため動作するが、全てのAPIが対応しているわけではない)。
2. トークン入力欄の直前に、日英併記で**具体的なリスク**(fine-grained
   token推奨・最小権限・共有端末で使わない・使用後revoke推奨・
   localStorageが拡張機能/XSSに読まれるリスクはゼロでない)を明記する
   ——警告を出さずに「保存できます」とだけ書くのは誠実さの方針に反する。
3. マルチバイト文字を含むファイル内容をBase64化する際は`btoa`単体
   ではなく`TextEncoder`経由にする(ASCII前提の`btoa`はそのままでは
   日本語等を正しく扱えない、`app.js`の`freelanceUtf8ToBase64`参照)。
4. **この方式が使えないケース**: SSH等、ブラウザが直接開けないプロトコル
   を要する連携(VPSの自動読み書き等)は同じ方式では実現できない
   ——サーバー側で資格情報を扱う設計が別途必要(未実装、要検討)。

## 0-a. 検索コンテキスト活用精度は「速度基盤」では改善しない(2026-08-26追記、未着手タスク)

`/v1/generate-with-search`(Google検索結果をプロンプトへ埋め込む機能)を
移植する際、**「検索結果を埋め込む」ことと「モデルがその文脈を正確に
活用する」ことは別問題**である点を混同しないこと。GPT-2系(指示追従
ファインチューニング無し)は埋め込んだコンテキストを正確に踏まえる
保証が無い——`open-directx`/`open-cuda`はGPU推論の**速度**基盤であり、
この**精度**の問題には直接寄与しない(過去のHANDOFF記録の通り、GPU
配線は速度の話であって精度の話ではない)。この制約を緩和できるか
どうかの調査(プロンプト書式改善・instruction-tunedモデルへの切替・
要約段階の追加)は`aruaru-llm/CLAUDE.md`の2026-08-26(続き3)エントリに
方針を記録済みだが、**まだ未着手**——移植先でも同じ制約を引き継ぐ
ことを前提にすること。

*English summary*: When porting `/v1/generate-with-search`, don't
conflate "search results are embedded in the prompt" with "the model
accurately uses that context" — GPT-2-family models (no instruction
tuning) provide no such guarantee, and `open-directx`/`open-cuda` are
speed infrastructure for GPU inference, not accuracy infrastructure.
Investigation of possible mitigations is recorded in `aruaru-llm/
CLAUDE.md`'s 2026-08-26 (cont. 3) entry but not yet started.

## 0-b. マルチLLMプロバイダ優先順位パネルの移植ポイント(2026-08-26追記)

「🔀 AI Provider Priority」パネル(ChatGPT/DeepSeek/Gemini/Claudeの単体・
同時実行呼び出し+無料枠優先順次使用)を他プロジェクトへ移植する場合:
- **サーバー側実装は`aruaru-llm`にある**(`src/chat_providers.rs`・
  `src/provider_priority.rs`)。open-english側はUIのみで、APIキー自体は
  持たない——上記0番のGoogle検索キーと同じく、各社APIキーはブラウザの
  localStorageにのみ保存し、保存操作のたびに`POST /v1/settings/
  chat-providers`(1プロバイダずつ)・`POST /v1/settings/
  provider-priority`(5サービス共通の順序+有効/無効)へ実行時設定として
  送る設計を踏襲すること。
- 優先順位の並べ替えは「番号入力欄」「番号のラジオボタン」のどちらから
  でも同じ`setPosition()`関数を通す設計(重複は入れ替えで解決)——
  片方だけ実装すると要件を満たさない。
- パネルを開くたびに`GET /v1/settings/provider-priority`・
  `GET /v1/settings/chat-providers`を呼び、サーバー側の実際の現状
  (環境変数等で既に設定済みの場合を含む)を反映すること(2026-08-26の
  実機TESTで発見した使いやすさの粗——保存するまで画面に何も表示され
  ないUIは分かりにくい)。

## 0. Google検索APIキーは訪問者ごとにブラウザローカル保存(2026-08-25追記)

複数の訪問者が同一の`aruaru-llm`インスタンスを共有するデプロイ
(VPS等)へ「🔎 Setup Google Search」パネルを移植する場合、
`POST /v1/settings/google-search`(プロセス全体で共有されるグローバル
設定)へ書き込む旧方式は使わないこと——ある訪問者のキー設定が他の
全訪問者の検索へ影響してしまう。代わりに、`app.js`の
`loadOwnGoogleSearchCredentials()`パターン(localStorageにのみ保存し、
`POST /v1/generate-with-search`のリクエストボディへ`google_search_
api_key`/`google_search_cx`として同梱する)を踏襲すること
(`aruaru-llm`側`main.rs`の対応する実装は`aruaru-llm/PORTING.md`参照)。

## 0b. ブラウザ内AI実行(WASM+WebGPU)構想(2026-08-25追記、計画段階)

`RPoem/CLAUDE.md`・`aruaru-llm/CLAUDE.md`の2026-08-25エントリに、
「利用者の端末上でブラウザ内AI実行(GPU/CPU問わず)を行う」構想の
技術検証結果・段階的導入計画を記載済み。現時点(2026-08-25)では
未実装——open-englishの既存アーキテクチャ(利用者が`aruaru-llm`
ネイティブ版を自分の端末で起動し`localhost:4600`へローカル接続)が
唯一の実用的な手段のまま。移植先でこの機能を先行実装する場合は
RPoem側のCLAUDE.mdを必ず先に確認すること。

## 1. aruaru-llm連携パターン(CORS+繰り返しペナルティ前提)

`app.js`の`askTrainer`関数は、`aruaru-llm`の`POST /v1/generate`を
直接fetchする最小構成。移植先で同じパターンを使う場合、以下2点が
`aruaru-llm`側に実装済みであることを確認すること(2026-08-10以降):

1. **CORS対応**: `aruaru-llm/src/main.rs`の`Route`チェーン末尾に
   `.with_cors()`(`open-runo-poem-compat`提供、`RPoem`側実装)が
   呼ばれていること。無い場合、別オリジン(別ポート/`file://`)からの
   `fetch()`はブラウザにブロックされる。
2. **繰り返しペナルティ**: `open-cuda`側`GptModel::generate_with_
   repetition_penalty`が`aruaru-llm`の`generate()`から呼ばれている
   こと(既定`penalty=1.3`)。無い場合、対話ファインチューニング無しの
   素のGPT-2貪欲デコードが同一文字列の無限ループに陥る既知の劣化モードに
   遭遇しうる。

## 2. 発話テキストの言語抽出パターン(`extractSpeechText`)

`app.js`のメッセージは全て`"English sentence / 日本語訳"`という
バイリンガル1行形式の規約で書かれている。この文字列をそのまま
`SpeechSynthesisUtterance`へ渡すと、単一言語の`utter.lang`設定と
実際の混在テキストが噛み合わず、TTSエンジンが不自然に途切れる
(ユーザー報告「喋りが途切れ途切れ」の実際の原因)。

移植手順: `extractSpeechText(text, lang)`関数(各行を`" / "`で分割し、
`lang`が`ja`始まりなら日本語側、それ以外なら英語側だけを抽出して
句点/ピリオドで連結する)をそのままコピーし、`speak()`関数内で
`new SpeechSynthesisUtterance(text)`を`new SpeechSynthesisUtterance(
extractSpeechText(text, lang))`に置き換えること。

## 3. ランチャーアイコン一式(`icons/` + `manifest.json` + `launchers/`)

外部画像ツール(ImageMagick/PIL/Inkscape等)非依存で、Node.js標準の
`zlib`のみを使った手書きPNGエンコーダ(`tools/gen-icons.js`)・Windows
`.ico`エンコーダ(`tools/gen-ico.js`、PNGバイト列をそのまま埋め込む
モダンICO形式)でアイコンを生成している。移植手順:

1. `tools/gen-icons.js`・`tools/gen-ico.js`をコピーし、
   `drawIcon`関数内の配色・図形(角丸背景・顔・髪・襟等)を対象アプリの
   ブランドに合わせて書き換える。
2. `node tools/gen-icons.js && node tools/gen-ico.js`を実行し
   `icons/`配下にPNG(32/180/192/512px)+`.ico`を生成。
3. `manifest.json`(PWA)・`index.html`の`<link rel="manifest">`/
   `<link rel="icon">`/`<link rel="apple-touch-icon">`をコピーし
   パスを調整。
4. `launchers/windows/create-shortcut.ps1`(Windows `.lnk`作成)・
   `launchers/linux/install-launcher.sh`(`.desktop`)・
   `launchers/mac/build-app.sh`(macOS上で実行する`.app`ビルド
   スクリプト、`sips`/`iconutil`必須)・`launchers/mobile/README.md`
   (PWA「ホーム画面に追加」手順)をコピーし、パス・アプリ名を書き換える。
   **注意**: `create-shortcut.ps1`は`-RepoRoot`引数を明示的に渡す運用
   にすること(`$PSScriptRoot`/`$MyInvocation.MyCommand.Path`が
   一部の実行経路〈ネストしたpowershell呼び出し等〉でnullになる実例が
   あったため、自動検出に頼らずフォールバック引数を用意している)。

## 4. 自動更新機能(`auto-update.js` + `version.json`)

`version.json`の`buildId`を5秒間隔でポーリングし、初回読み込み時の値と
異なれば`location.reload()`する単純な方式。移植手順:

1. `auto-update.js`・`version.json`をコピーし、`index.html`に
   `<script src="auto-update.js"></script>`を追加(他のscriptタグより
   後に置くこと)。
2. コード変更を行うたびに`version.json`の`buildId`を手動で更新する
   運用ルールを、移植先のCLAUDE.md等に明記すること(自動ビルド
   パイプラインが無い前提の設計、CI/CDがある場合はビルド時刻を
   自動注入する形に発展させてもよい)。
3. **正直な開示**: `file://`直開き環境では、ブラウザによってはローカル
   ファイルへの`fetch()`がブロックされ機能が無効化される(エラーを
   握りつぶして静かに何もしないだけで、他機能への影響は無い)。確実に
   動かすには簡易HTTPサーバー経由での配信が必要。

## 4a. RPoemベースの静的配信サーバー(`server/`)——`python3 -m http.server`代替

`python3 -m http.server`はPythonの有無・バージョンに依存し、
`auto-update.js`の`fetch()`が`file://`ではブロックされうるという既存の
制約への恒久対応として、`server/`(独立Rustクレート`open-english-server`)を
新設した。移植手順:

1. `server/Cargo.toml`を作成し、`open-runo-poem-compat = { path =
   "<RPoemリポジトリへの相対パス>/crates/open-runo-poem-compat" }`を
   path依存として追加(このエコシステムのsibling-repo path依存パターン、
   `aruaru-llm`の`opencuda-llm`依存等と同じ)。
2. `server/src/main.rs`で`open_runo_poem_compat::hyper_compat::
   static_file_handler(path, content_type)`(**既存関数の再利用、
   新規実装は不要**)を配信したい静的ファイルの数だけ`Route::new()
   .at(url_path, get(static_file_handler(...)))`へ登録し、
   `Server::new(TcpListener::bind(addr)).run(app)`で起動する。
3. **正直な開示**: このハンドラは`tokio::fs::read`でリクエストごとに
   ディスクから読み直す設計(埋め込み配信ではない)——配布時はこの
   バイナリと静的ファイル群を同じ相対位置(`server/`の親ディレクトリ)に
   置く必要がある。HEADメソッドは未登録(GETのみ)。

## 4b. ハイブリッド(英日併記)応答の構造的保証(`ensureHybridReply`)

英語中心のBPE語彙で事前学習された小型LLM(GPT-2系)は、プロンプトで
「英語と日本語を混ぜて返答して」と指示しても、日本語を一切含まない
応答を返すことが多い(モデルの本質的な限界であり、プロンプト調整では
解決しない)。移植手順:

1. `containsJapanese(text)`(ひらがな・カタカナ・漢字のUnicodeプロパティ
   エスケープ`\p{Script=Hiragana}`等での判定)と`ensureHybridReply
   (completion, userText)`をコピーする。
2. モデルの応答に日本語が含まれない場合、機械翻訳の質を偽って主張せず、
   定型の短い日本語の一言(「このAIはまだ日本語が苦手です」等)を
   フロントエンド側で追記するに留める——これにより「英日併記」という
   *構造*だけは常に保証できる(意味内容の翻訳精度は保証しない)。
3. ユーザー発話が日本語の場合はその事実をプロンプトへ明示すると
   (`containsJapanese(userText)`で判定)、モデルが日本語混じりの応答を
   返す確率がやや上がる(保証はされない、実測ベースで確認すること)。

## 4c. バージョン管理+旧バージョンのブラウザ側クリーンアップ

`version.json`に`buildId`(自動更新のトリガー用)に加え`version`
(セマンティックバージョン、画面への表示用)を追加。移植手順:

1. `version.json`に`{"version": "x.y.z", "buildId": "..."}`の形式で
   両方保持する。
2. アプリのフッター等に`fetch("version.json")`で取得した`version`を
   表示する(`app.js`の`showAppVersion`参照)。
3. **正直な開示・スコープ**: このアプリはネイティブインストーラーを
   持たない静的Webアプリのため、「旧バージョンの自動アンインストール」は
   ディスク上のファイル削除としては実装していない(他のファイルを
   誤って削除するリスクを避けるため)。代わりに、新バージョン検出時に
   このアプリ専用の名前空間(`openEnglish.`接頭辞)を持つ
   `localStorage`キーを削除し、キャッシュ破棄付き(`?v=<buildId>`
   クエリ付き)で再読み込みする、という「ブラウザ側の痕跡クリーンアップ」
   に限定している(`auto-update.js`の`clearOwnLocalStorage`/
   `reloadBustingCache`参照)。ネイティブアプリへ移植する場合は、
   この節の設計をそのまま流用せず、OS標準のアンインストーラー機構
   (Windowsのレジストリアンインストールエントリ等、`RPoem/apps/
   desktop-tray`のInno Setup採用例を参照)を使うこと。

## 4d. Google Custom Search JSON APIブリッジ + ブラウザ設定パネル(2026-08-11)

`aruaru-llm`側`POST /v1/generate-with-search`(検索結果でプロンプトを
補強するブリッジ式)と、ブラウザから直接APIキー/cxを保存できる設定
パネル(`POST /v1/settings/google-search`、`aruaru-llm/src/web_search.rs`の
`RUNTIME_CREDENTIALS`——プロセスメモリ上のみ、ディスク非保存)を追加した。
移植手順:

1. `web_search.rs`をコピーし、`RUNTIME_CREDENTIALS`(実行時設定)→
   環境変数の順にフォールバックする`read_credentials()`パターンを
   踏襲する。
2. **正直な開示**: ユーザー自身のGoogle Cloud Console契約(無料枠あり、
   超過分は課金)が前提——このエコシステム共通の「契約不要」方針への
   意図的な例外であることを利用規約・UI双方に明記すること。
3. **セキュリティ配慮(最重要)**: APIキーはログ・ディスクに一切残さない。
   `GET /v1/settings/google-search`はキー値自体を返さず設定済みかどうかの
   真偽値のみ返す。Google側のエラー応答本文はキー値をエコーバックしない
   仕様を確認済みのため、そのままユーザーへ表示しても安全。

## 4e. Windowsインストーラー(Inno Setup)の実機検証パターン(2026-08-11)

`installer/windows/open-english.iss`で得られた、他リポジトリでも
再利用できる落とし穴と対処:

1. **UAC昇格ハング対策**: `PrivilegesRequired=lowest`を`[Setup]`へ追加
   しないと、管理者権限を要求しないアプリでも既定でUAC昇格を要求し、
   非対話的な`/VERYSILENT`インストール検証がGUIプロンプト待ちで無限に
   ハングする。
2. **Git Bashでのスイッチ誤変換対策**: Git Bash/MSYSは`/VERYSILENT`
   `/SUPPRESSMSGBOXES`のような`/`始まりの引数を偽のWindowsパスへ自動
   変換してしまう。`MSYS_NO_PATHCONV=1`を先頭に付けて回避する。
3. **検証手順**: `ISCC.exe`でビルド→`MSYS_NO_PATHCONV=1 ./setup.exe
   /VERYSILENT /SUPPRESSMSGBOXES /DIR=... /LOG=...`でサイレント
   インストール→インストール済みバイナリを実際に起動しHTTP応答を確認→
   `unins000.exe /VERYSILENT /SUPPRESSMSGBOXES`でアンインストール→
   インストールディレクトリが実際に消えたことを確認、まで一気通貫で
   行うこと(ビルド成功だけで「完成」と報告しない既存方針の徹底)。

## 4f. 地理・観光DB連携+話題駆動の安全案内パターン(2026-08-11)

`aruaru-llm`側`geo_content.rs`(`GET /v1/geo/random`・`POST /v1/geo/
lookup`・`GET /v1/geo/fuji`・`POST /v1/geo/tours`)と連携し、会話の
話題(国名・山名)に応じて動的にコンテンツを差し込むパターン。移植手順:

1. **部分一致検索を使うこと(最重要、実際に踏んだ落とし穴)**: ユーザーの
   発話文全体("I from Japan."等)を検索クエリとして渡す設計の場合、
   完全一致(`==`)ではなく部分一致(`contains`/SQLの`LIKE '%...%'`)で
   検索すること。完全一致のみだと実際の発話文では一致しない(実機
   テストで発見した実バグ、`aruaru-llm/CLAUDE.md`同日HANDOFF参照)。
2. **危険な話題(登山等)には安全案内を必ず添える**: 富士山のように
   実際に死亡事故が起きうる話題を扱う場合、教材として面白い情報
   (ランドマーク・名物)だけでなく、安全上の注意(装備・事前予約の
   必要性)を必ず併記すること。出典元(公式サイトのURL)と収集時点を
   明記し、「営業期間・電話番号は毎年変わるため利用前に直接確認する
   こと」を正直に開示する(`fuji_source_ja`/`fuji_source_en`パターン)。
3. **観光ツアー検索は既存のGoogle Custom Search連携を再利用**:
   新規の検索APIクライアントを増やさず、`web_search.rs`
   (`/v1/generate-with-search`と同じAPIキー設定)をそのまま呼ぶ。
   YouTube検索は専用API連携までは実装せず、URLエンコード済みの検索
   結果ページへの直リンクで代替してよい(過剰実装を避ける)。

## 4g. 起動時の自動アップデート(GitHub Releases検出→自動アンインストール
→自動インストール)パターン(2026-08-11)

`server/src/self_update.rs`——ネイティブ配信サーバー(Rust)が起動時に
GitHub Releasesの最新版を確認し、新しければWindowsアプリを自動更新する
パターン。移植手順・落とし穴:

1. **ブラウザ側JSでは絶対に実装できない**: アンインストール・
   インストールという特権操作はネイティブコード側でしか行えない
   ——`auto-update.js`(ページ再読み込みのみ)とは別レイヤーとして
   位置づけること。
2. **実行中の自分自身のexeは削除できない(Windows特有の制約)**:
   アンインストーラー/新インストーラーを起動する前に、必ず**別プロセス
   (一時バッチスクリプト)をデタッチ起動してから、このプロセス自身を
   `std::process::exit`で終了する**こと。同一プロセス内で
   `Command::new(uninstaller).status()`のように同期的に呼んでしまうと、
   実行中のexe自身をアンインストーラーが削除しようとして失敗する。
3. **バージョン比較は安全側に倒す**: パース不能なバージョン文字列は
   `(0,0,0)`扱いにし、「不明時は最新と誤判定しない」設計にすること
   (`parse_version`参照)。
4. **GitHub Releaseが存在しない場合(初回リリース前)を必ず考慮**:
   `GET .../releases/latest`は`404`を返す——これを異常終了させず、
   ログに記録して通常起動を継続すること(このリポジトリ自身、
   2026-08-11時点でまだリリースが1件も無い状態でこの分岐を実機確認
   済み)。
5. **CI(`.github/workflows/release.yml`)でインストーラーアセットを
   自動生成**: `choco install innosetup`でInno Setupを導入し
   `ISCC.exe`でビルド、ファイル名に"setup"を含む`.exe`としてGitHub
   Releaseへ添付する(`self_update.rs`側のアセット検出条件
   `name.contains("setup")`と対応させること)。

## 4h. 同梱コンポーネント(aruaru-llm/aruaru-db)の自動アップデート
パターン(2026-08-19)

`server/src/component_update.rs`——本体自身の自己更新(4g)とは別に、
インストーラーが同梱する**別リポジトリ由来のコンポーネント**(実行
ファイルのみを`{exe_dir}/{name}/`配下へ配置する形で同梱するもの)も
同じタイミングで自動更新するパターン。移植手順・落とし穴:

1. **本体の自己更新ロジックを`pub(crate)`化して再利用**する
   (`self_update::fetch_latest_release_for`/`is_newer`/`download_to`/
   `copy_dir_recursive`/`health_check`等)。GitHub Releases問い合わせ・
   バージョン比較・ダウンロード・ロールバック用バックアップは、対象
   リポジトリが変わるだけで本質的に同じロジックのため、複製しないこと。
2. **コンポーネントには独自のバージョンマーカーファイル
   (`.component-version`)を持たせる**: 本体と違い`version.json`のような
   既存の仕組みが無いため、初回は「マーカーが無い=ローカル`0.0.0`扱い」
   として最新版を取得し、成功したらマーカーを書き込む——という
   「初回だけ強制的に最新へ揃える」移行動作を許容する設計にする。
3. **zip解凍はOS標準機能を子プロセスとして呼ぶ**(Windows:
   `powershell -Command Expand-Archive`、Linux: `tar xzf`)。zip解凍用の
   crate依存を追加する必要は無い——既存の`fetch-*.ps1`と同じ発想。
4. **コンポーネントごとに「本物のヘルスチェックが可能か」を正直に
   評価する**: HTTPの`/healthz`があるコンポーネント(今回のaruaru-llm)
   なら本体と同じロールバック機構(新版起動→ヘルスチェック→失敗なら
   復元)がそのまま使える。無いコンポーネント(今回のaruaru-db、pgwireの
   みでHTTPヘルスエンドポイント無し)は、代替の簡易確認(TCP接続可否)に
   格下げし、その限界をコード上のコメント・ドキュメント双方に明記する
   こと——「ヘルスチェック済み」と過大に主張しないよう注意。
5. **元々そのコンポーネントを自動起動していたかどうかで挙動を分ける**:
   本体が自動起動しているコンポーネント(aruaru-llm)は新版でも起動して
   確認する。ユーザーが手動起動する運用のコンポーネント(aruaru-db)は、
   起動していなかった場合はファイル差し替えのみに留め、勝手に起動
   しない(既存の起動ポリシーを更新処理側でも一貫させる)。
6. **同梱されていないコンポーネントは存在チェックだけで即スキップ**:
   `{exe_dir}/{name}/`が存在しなければGitHub APIへの問い合わせすら
   行わない(任意コンポーネントの利用者に不要なネットワークアクセスを
   させない)。

## 5. 実在の接客技法・文化コンテンツを教材へ翻案する際の著作権配慮

参考記事(ブログ等)の技法・実例を教材化する際は、記事本文を丸ごと
転載せず、技法自体を短い引用・要約(15語未満の直接引用+出典明記)に
留めて独自の練習問題へ翻案すること(`trainingSteps`内の秋葉原メイド
カフェ技法の実装を参考にすること)。出典クレジットはページ内の
disclosure領域に明記する。

## 6. モーダル用CSSクラスに`max-height`/`overflow-y`を必ず定義する

複数のモーダル(`setup-modal`)が異なる内側コンテナクラス
(`setup-card`/`setup-panel`)を使い回す設計の場合、**全てのコンテナ
クラスに`max-height: 85vh; overflow-y: auto;`相当を定義すること**。
2026-08-11、`setup-panel`にこの定義が漏れていたため、内容が長い
モーダル(資格試験対策の5問表示)で実機スマホの画面からはみ出し
下へスクロールできず後半の問題に回答できないという実バグが発生した
(`google-search-modal`にも同じ穴があったが問題が短かったため気づか
れていなかった)。新しいモーダルを追加する際は、既存の`setup-card`と
同じスクロール可能設計を必ず踏襲すること。

## 7. AndroidでRust製サーバーを単体同梱する際の注意点

- 静的アセットのパスをコンパイル時定数(`env!("CARGO_MANIFEST_DIR")`
  等)にハードコードせず、環境変数で上書きできるようにしておくこと
  (Android側はアプリの内部ストレージへ展開したパスを実行時に渡す
  必要がある)。
- Windows専用の自己更新・インストーラー差し替えロジックがある場合、
  **必ず`cfg!(target_os = "windows")`等でプラットフォーム判定を
  入れること**。判定漏れがあると、Android上でも「新バージョン検出→
  Windows用インストーラー起動を試みる→失敗→(Windows向けに書かれた)
  `std::process::exit`でサーバー自身を毎回強制終了する」という実バグに
  つながる(2026-08-11に実機で発見・修正)。
- 依存クレートの中にプラットフォーム専用API(Windows専用の`windows`
  クレート等)が既定feature有効のまま隠れていないか、実際に
  `cargo ndk build`でcrクロスコンパイルして確認すること
  (`rs-smarttcp`が既定で`zfs_accel_hlsl`のWindows専用`gpu`feature を
  引き込んでいたため、間接的にAndroidビルドが225件のエラーで失敗した
  実例あり)。

## 8. `localStorage`ベースの日次利用回数制限+上限到達メッセージ

`app.js`の`DAILY_USAGE_LIMIT_KEY`/`isDailyLimitExceeded()`/
`recordDailyUsage()`/`dailyLimitExceededMessage()`(2026-08-19(続き8)
追加)は、サーバー側にレート制限基盤が無いプロジェクトへそのまま移植
できる最小構成:

- 日付文字列(`YYYY-MM-DD`)+カウントを`localStorage`へ`JSON`保存し、
  読み出し時に日付が変われば`0`へリセットする(タイムゾーンは
  ブラウザのローカル時刻依存、サーバー側の日付とはズレうる点に注意)。
- 上限到達メッセージは、他プロバイダの無料枠情報を**ハードコードせず**
  既存の設定ファイル(本プロジェクトでは`provider-free-tiers.json`)を
  `fetch`して動的に組み立てること——複数の案内(バナー、上限到達
  メッセージ等)が同じデータソースを参照することで、情報の乖離を防げる。
- テスト用に`window.<APP>_DAILY_LIMIT_OVERRIDE`のようなグローバル
  フックを用意しておくと、実機ブラウザで上限を一時的に下げて実際の
  表示を確認しやすい(本番運用ではこの変数は未設定のまま)。
- **正直な開示として移植先でも明記すべき点**: これはクライアント側
  のみの簡易カウンタであり、`localStorage`消去や別ブラウザ/別端末の
  利用で回避できてしまう——サーバー側での強制ではなく、あくまで
  利用者への通知目的である旨。

## 9. 自己更新の複数世代履歴+手動ダウングレードAPI

`server/src/self_update.rs`の`save_history_dir`/`save_history_file`/
`list_history_versions`/`history_root`/`MAX_HISTORY_GENERATIONS`
(2026-08-20追加)は、自動アップデート機構を持つ他プロジェクトへ
そのまま移植できるパターン:

- 更新前に既に取っている「1世代だけの一時バックアップ」(自動
  ロールバック用)を、`{exe_dir}/.update-history/{component}/
  {version}/`という世代ディレクトリへ**コピー**保存するよう1行
  追加するだけで、複数世代保持へ拡張できる(既存の一時バックアップ
  ロジックを壊さない、重複実装を避ける設計)。
- ディスク容量への配慮から、既定`3`世代のみを残し、`parse_version`
  比較で古い順に自動削除する(`prune_history`)。バージョン不明
  (`"0.0.0"`等の移行前マーカー)は履歴として無意味なため保存しない
  ガードを忘れないこと。
- 手動ダウングレードAPI(`GET .../history`・`POST .../downgrade`)は
  既存の「新版検出→ダウンロード→ヘルスチェック→失敗時ロールバック」
  ロジックの**逆方向**として実装できる(kill→履歴ディレクトリで
  上書き→バージョンマーカー更新→必要なら再起動)。本体
  (自分自身の実行ファイル)のダウングレードはWindows/Unixで対応が
  異なる点(Windowsは実行中の自分自身を上書きできないためデタッチ
  したスクリプトへ委ねてプロセス終了、Unix系は直接上書き可能)も
  既存の自動更新ロジックと同じ非対称性を踏襲すること。
- **正直な開示として移植先でも明記すべき点**: 保持世代を超えて古い
  バージョン、またはそのマシンで一度も自動更新が発生していない
  バージョンへは戻せない(保持していないものは戻しようがない)。

## 10. 多言語オリジナル模擬試験+言語選択+多言語連続読み上げ(2026-08-22)

「日本語における英検・TOEICのような運用能力テストを、世界中の言語で
提供する」という要件を、翻訳エンジンもLLMの多言語生成能力も当てにせず、
静的データ+既存UIの拡張だけで実現するパターン。

1. **データ層を2ファイルへ分離する**: `world-language-exams.json`
   (言語メタデータ+CEFR風レベル付きの4択問題)と
   `world-language-phrases.json`(フレーズIDごとの各言語訳文)。
   前者は「試験」、後者は「連続読み上げの題材」という別用途のため
   1ファイルにまとめない——UI側もそれぞれ独立に読み込める。
2. **一覧APIと本体データを分ける**: サーバーは
   `GET /v1/world-languages`で**問題本文を含まない**サマリ(コード・
   現地語表記・英名・和名・RTLか・問題数・レベル)だけを返し、実際の
   出題時のみ静的JSON本体を取得する。言語選択UIの描画に数十KBの
   問題文を毎回転送しないための二段構え。APIが無い配信形態
   (`file://`直開き等)では静的JSONから同じ形のサマリを組み立てる
   フォールバックをフロントエンド側に置く。
3. **既存の試験UIを作り変えない**: 試験セレクトへ`world:<code>`という
   値の`<optgroup>`を動的に足すだけにし、採点・シャッフル・
   「間違えた問題を持ってトレーナーへ」の既存関数はそのまま流用する
   (既存のJLPT→日本語教室への遷移と同型の分岐を1つ足すだけで、
   多言語版の学習継続導線が完成する)。
4. **選択言語数の上限・下限はチェックボックス側で強制する**:
   下限2(英語・日本語は常時有効・disabledのcheckedとして表示)、
   上限5。上限に達したら**未チェックのものだけ**をdisabledにする
   (チェック済みを無効化すると解除できなくなる)。「全部を選択」は
   上限を超えられないため、先頭N件のみ選択した上でその旨を明示する
   ——ボタンの見た目上の約束と実際の挙動を食い違わせない。
5. **読み上げは既存TTSを流用する**: 新しい音声合成基盤は足さず、
   既存の`pickVoice(lang, preferMale)`と`SpeechSynthesisUtterance`を
   使い、言語コード→BCP-47タグの対応表(`SPEECH_LANG_TAGS`)だけを
   足す。連続再生は`cancel()`を挟まずキューへ積むだけでよく、
   `onstart`/`onend`で「今読んでいる行」をハイライトできる。
   **正直な開示を必ず添えること**: その言語の音声がOSに無ければ
   表示のみになる(別言語の声で誤読される場合もある)。
6. **表示文はコピー可能なDOMで出す**: `readonly`の`<textarea>`+
   `user-select: text`。保存手段は「コピー」「Blob+`<a download>`で
   .txt保存」「既存の永続化API(`POST /v1/db/history`)へ保存」の3系統を
   用意し、**DB保存のために新しいテーブル・新しいエンドポイントを
   作らない**(既存の会話履歴テーブルへ専用の`role`で入れるだけで足りる)。

**正直な開示(移植先でも必ず維持すること)**: 収録問題は自作の
オリジナル問題であり、実在の語学資格試験(DELE・DELF・
Goethe-Zertifikat・HSK・TOPIK等)の過去問ではなく、それらの試験名を
騙ってもいけない。UIの試験名は「XX語 運用能力チェック(オリジナル問題)」
のように、実在の試験と紛れない表記にすること。

## 11. 利用者設定をアップデートを跨いで保持する二重保存パターン(2026-08-22)

「メンテナンスや自動アップデートを挟んでも利用者の設定が消えないように」という
要件を満たすためのパターン。ブラウザだけ・サーバーだけでは不十分な理由がある。

1. **書き込みは常に両方へ**: `localStorage`(一次)と、サーバー側の既存の設定
   永続化API(このリポジトリでは`POST /v1/db/settings`、SQLite)の両方へ同じ
   キーで書く。サーバーが落ちていても`fetch`の失敗を握りつぶすだけで、
   ブラウザ側の保存は成立する。
2. **読み込みは「localStorageに無ければDBから復元」**: 起動時に一度だけ
   `GET /v1/db/settings`を読み、`localStorage`に無いキーだけ埋め戻す。
   **この復元は、設定を読む他の初期化処理より必ず前に実行すること**——
   順序を誤ると「DBには残っているのに画面には反映されない」という、
   実機でしか気づけないバグになる(このリポジトリで実際に発生させ、
   ブラウザ検証で発見・修正した)。
3. **自動更新のデータ破棄処理から明示的に除外する**: 「旧バージョンの痕跡を
   消す」類の処理(このリポジトリでは`auto-update.js`が特定接頭辞の
   `localStorage`キーを一括削除する)は、**利用者が手で選んだ設定まで消して
   しまう**。接頭辞が違うから今は helpfully 巻き込まれない、という偶然に
   頼らず、保持すべきキーの明示的な許可リストを置くこと。
4. **サーバー側のデータディレクトリが更新で消えないことを確認する**: この
   リポジトリでは`self_update.rs`が`data\`を退避・復元する設計になっている
   ことを実際にコードで確認した上で、DBを二次保存先として採用した——
   確認せずに「DBなら残るはず」と仮定しないこと。

## 12. 順番指定UIを複数の入力方法で連動させるパターン(2026-08-22)

数値入力欄・ラジオボタン・▲▼ボタンという3系統から同じ「並び順」を編集できる
UIを、状態がずれずに実装するための要点。

- **入口を1つの関数に集約する**: どの入力方法も最終的に
  `setPosition(item, position)`のような単一の関数だけを呼び、その関数が
  配列を並べ替えて保存し、**UI全体を描画し直す**。各入力の値を個別に同期
  させようとすると、片方だけ更新される組み合わせが必ず残る。
- **重複は「入れ替え」で解決する**: 既に使われている番号を指定されたとき、
  エラーで拒否すると利用者の意図(その位置にしたい)が通らず行き止まりになる。
  その位置にいる要素と席を交換すれば、番号は常に1〜Nが1回ずつになる。
- **範囲外の入力は黙って捨てて再描画する**: 入力欄の値が現在の並びへ戻る
  だけで、UIが壊れた状態にならない。

## 13. 「情報収集」機能を、嘘をつかずに実装する(2026-08-22)

「その言語圏の話題についていけるように情報を集める」という要件に対し、
**何が本当にリアルタイムで、何が静的な作り置きなのかを混ぜない**ための設計。

- **リアルタイムが可能な部分だけを実際に取りに行く**: ニュース見出しは
  認証不要の公開RSSをサーバー側からその都度取得する(APIキー必須の商用
  ニュースAPIへ依存させない)。取得するのは**見出し・日時・リンクのみ**とし、
  記事本文は取得も再配布もしない(著作権への配慮——本文は元記事で読む)。
  取得失敗時はエラーを握りつぶさず`ok:false`と理由を返し、UIにも正直に出す。
- **静的な部分は静的だと明示する**: 首都・都市・名所・名物・著名人・企業などは
  静的JSONに置き、UIに「リアルタイムではなく古くなり得る」と明記する。
- **LLMに事実を生成させない**: 手元のモデル(GPT-2系)に「その国の有名企業は?」と
  尋ねて答えを表示する実装は、もっともらしい嘘を「収集した情報」として提示する
  ことになるため採らなかった。
- **プレースホルダーは空欄にしない**: データ未作成の言語は空の項目を並べるのでは
  なく「まだ作成されていない」と表示する。空欄は不具合に見え、未対応であることも
  伝わらない。

## 14. 対応言語数を大きく増やすときの正直な段階的拡大(2026-08-22)

対応言語を一気に130件へ増やす際、**レジストリ(名前・国旗・国名)と実データ
(問題・地域情報)を別の軸として扱う**。

- レジストリ項目に`authored`フラグ・`detail_status`のような「実データがあるか」を
  示すフィールドを持たせ、UI(選択肢の説明文・ブリーフィング)がそれを読んで
  「問題未収録」「詳細データ未作成」と表示する。
- **一覧に載っている=対応済み、と読めてしまう表記を避ける**。データファイルの
  `_readme`にも「一覧は130件だが実データがあるのは40件」と数字で明記する。
- 一覧が100件を超えたら、**言語名だけでなく国名でも絞り込める検索**を必ず用意する
  (利用者は「フランス語」ではなく「France」で探すことがある)。
- 段階的に増やしながら**その都度サーバーを起動して実機確認する**——今回は
  20言語ずつ5回に分けて追加し、各回でAPIの件数・UIの描画・既存機能の非回帰を
  確認した。

## 15. 機微な話題へのルールベース固定文応答パターン(2026-08-23)

宗教・歴史・都市伝説のように**事実誤りの害が大きい話題**について、
LLMに生成させず人手で書いた固定文を返すパターン。`app.js`の
`isCreatorQuestion()`(作者紹介)・`isReligionHistoryQuestion()` /
`RELIGION_HISTORY_TEXTS`(イスラム・イラン・アラブの歴史)・
`isMarkOfBeastQuestion()` / `MARK_OF_BEAST_TEXTS`(666と獣の数字)の3例が
同じ形をしている。

- **構成は3点セット**: 「キーワード判定関数」+「言語コードをキーとする
  テキスト表」+「表を英日併記で組み立てる関数」。送信ハンドラの冒頭
  (日次上限チェックと同じ位置)に置き、AI推論経路へ入る前に`return`する。
  利用回数は消費させない。
- **話題語と意図語のAND条件で誤検出を防ぐ**: 「Iran」のような話題語が
  一言出ただけの通常会話を乗っ取らないよう、歴史・ルーツ・違いといった
  意図語との組み合わせで判定する(666のように話題語自体が十分具体的な
  場合はOR条件でよい)。英語の語尾派生形(Zoroastrian→Zoroastrianism)を
  取りこぼさないよう、**単語境界`\b`は語頭のみに付ける**(実機テストで
  実際に踏んだバグ)。
- **判定の順序は「具体的な話題を先に」**: 666判定を宗教史判定より前に置く。
- **誠実さの担保を本文へハードコードする**: (a) 学説・現代的な解釈は
  「そう指摘する人がいる」という**紹介**にとどめ断定しない、(b) 都市伝説は
  **都市伝説と明記**した上で、可能なら**技術的な種明かし**まで添える
  (バーコードの「隠れた666」は、両端と中央のガードバーが読み取りの開始・
  終了・区切りを示す目印であり、数字6と見た目が似ているだけでエンコードが
  別物〈3対7モジュール〉である、と説明する)、(c) 偶然の一致は**偶然の
  一致と明記**する(Pythonのロゴのヘビと名前の由来は無関係)、(d) 古い
  記述と現代の状況の「面白い符合」は、**「そう語られることがある」という
  可能性の紹介**にとどめ、「預言が成就した」という断定的な表現は使わない(黙示録13章16-17節の「刻印なしでは売り買い
  できない」という記述と、バーコード・オンライン決済〈Amazon等〉なしでは
  買い物しづらい現代との重ね合わせが実例)。これらの
  留保は他の文と混ざって省略されないよう、テキスト表の中に直接書き込む。
- **含めなかった内容とその理由をコメントとドキュメントの両方に残す**:
  裏付けが取れず除外した説がある場合、「なぜ述べないか」を応答本文でも
  開示し、`app.js`の該当箇所冒頭コメントに「改変時は必ず読むこと」として
  制約を明記する。
- **他言語展開を見越して最初からテキストを表にする**(現状は`ja`・`en`のみ)。
- **【2026-08-23 追記】留保は「硬い否定文」でなくてもよい——機能と文体を
  切り離す**: 上記(a)〜(d)の留保は*必ず書く*が、その書き方は
  「〜と断定するものではありません」「not as a claim that any prophecy has
  been fulfilled」のような硬い定型文である必要はない。「話のタネとして
  どうぞ」「真偽のほどは分かりませんが、こう見ると聖書の世界も少し身近に
  感じられるかもしれません」/ "take this as a fun bit of trivia rather
  than solid proof" のような軽妙な言い回しでも、**読者が「これは証明では
  ない」と確実に受け取れる限り**、留保としての役割は同等に果たせる
  (open-english本体で実際にこの方向へ書き換えた)。移植時の判断基準は
  文体ではなく**「事実として断定していないか」**の一点に置くこと。
  逆に、柔らかくした結果として断定と読める文(「実際にそうなっている」
  「預言は成就した」)へ滑り込ませてはならない。

## 16. 固定文応答を「2段階の出題→解答」へ拡張するパターン(2026-08-23)

上記15の固定文応答を、**一往復では終わらない**やり取り(クイズの出題→
解答)へ広げたパターン。`app.js`の`isQuizRequest()` / `isQuizAnswerRequest()`
/ `QUIZ_TEXTS` / `quizQuestionText()` / `quizAnswerText()` が実例
(作者オリジナルの「9を4つ使って10を作る」問題)。

- **会話状態は単一のbooleanで足りる**: 状態機械やセッションオブジェクトを
  組まず、モジュールスコープの`let quizAwaitingAnswer = false;`だけで
  「出題済みか」を持つ。既存の`examPrepMissedQuestions`(模擬試験の
  間違えた問題を保持する配列)と同じ素朴さで十分——**2段階に必要な状態は
  1ビットしかない**。
- **判定の順序が肝**: 送信ハンドラでは「解答待ちの返事」の判定を
  「出題依頼」の判定より**先**に置く。ただし解答判定側に
  `!isQuizRequest(text)`の除外を入れておくと、「もう一問出して」で
  誤って解答を出さずに済む。
- **一般的すぎるキーワードは状態でガードする**: 「わからない」"I don't
  know" "tell me"のような語は通常会話に頻出するため単独では使えないが、
  `quizAwaitingAnswer === true`のときしか参照しないなら安全に使える。
  **キーワードの安全性は語そのものではなく「いつ評価されるか」で決まる**。
- **依頼判定は話題語×依頼語のAND+単独成立表現のOR**: 15と同じ考え方。
  「問題」「クイズ」だけでは通常会話(「この問題は難しいですね」)を
  拾ってしまうため、「出して」「ください」等の依頼語とのANDを基本とし、
  「クイズを出して」"quiz me"のような単独で明白な表現だけORで拾う。
- **多言語は「主要数言語だけ訳し、残りは既定へ正直にフォールバック」**:
  `QUIZ_TEXTS`は言語コードをキーにした表で、現状は ja/en/es/fr/de/zh/ko の
  7言語のみ。対応言語(130)すべてを機械翻訳で埋めて「全言語対応」に
  見せることはしない——未収録言語の利用者には既定の日英併記を返す
  (14の「正直な段階的拡大」と同じ方針)。`zh-Hant`のような派生コードは
  `-`の前で切って基底コードの訳文を流用する。
- **出題言語の決め方**: 「学びたい言語」(`world:<code>`)を最優先し、
  未設定なら母国語設定へフォールバックする。訳文が無ければ`null`を返して
  既定へ落とす、という一本道にしておくと分岐が増えない。
- **算数・事実の類は絶対にLLMに生成させない**: 素のGPT-2は計算の合わない
  答えを自信満々に出す。**解答は必ず人手で検算し**(`(9×9+9)÷9`は
  81→90→10と確認済み)、`app.js`の冒頭コメントに「別解を足すなら自分で
  検算すること」と明記しておく。
- **問題の性質そのものを問題文に書く**: 「トンチ・ひっかけではなく純粋な
  四則演算」という一文が無いと、利用者が的外れな方向(記号を回転させる等)
  へ迷い込む。**出題文はルールだけでなく「解き筋の土俵」も伝える**。

## 注意事項

本プロジェクトはPhase 0の試作品であり、AI応答品質(GPT-2ベース)・
音声合成の自然さ・レベル別対応の確実性は保証されていない旨を、
移植先でも必ず明記すること(誇大表示の回避、このエコシステム共通の
「正直な開示」規約)。

## 学年別・家庭教師コースの移植ポイント(2026-08-23)

他プロジェクトへ「学年別の練習問題+落ちこぼれ防止」を移植する場合の要点。

- **データ構造**: `TUTOR_GRADES`(学年の並び。**配列の順序が「学年の上下」
  そのもの**で、先頭が最も低い`p0`=保育園児・幼稚園児)、
  `TUTOR_SUBJECTS_BY_STAGE`(学年段階ごとの教科)、
  `TUTOR_QUESTIONS`(キーは`<学年ID>:<教科ID>`)。この3つだけで、UIは
  自動的に追従する(問題を足したいときは`TUTOR_QUESTIONS`へキーを足すだけ)。
- **落ちこぼれ防止の仕組み(2段構え・回数上限なし)**: 誤答時、まず
  同じ学年の`ladder`(易しい順の類題配列)を1つ進める。使い切ったら
  `tutorLowerGradeCandidates()`で**同じ教科の問題がある1つ下の学年**を探して
  切り替える(問題が無い学年は飛ばす)。下限に到達したら下げずに解答提示。
  **「最大N回」のような固定上限をコードに置かないこと**——「用意されている
  データがある限り下がる」という条件だけで動かすのが設計意図。
- **未収録学年のフォールバック**: `tutorResolveSourceGrade()`が「実際に
  出題できる学年」を返す。選んだ学年に問題が無くても下の学年で出題でき、
  UIには**どの学年の問題を出しているかを正直に表示**する(未収録を
  「対応済み」に見せない)。
- **年齢による選択制限を作らないこと**: 学年ボタンは全学年を常に選択可能に
  する(高校生・社会人が保育園児レベルからやり直せる、というユーザー要件)。
- **学習履歴の保存**: 既存の`POST /v1/db/history`(ローカルSQLite+
  `OPEN_ENGLISH_DATABASE_URL`があればPostgreSQL互換DBへミラー)へ
  `role`を分けて投げるだけ。新しいテーブル・新しいAPIを作らない。

## バーチャルスクール(高等教育)/バーチャルオンライン職業訓練校の移植ポイント(2026-08-24)

「学年別・家庭教師コース」(前節)と**同一の設計パターンを、対象年齢を上げて
再利用した**例。移植時は前節とあわせて読むこと。

- **データ構造は3枚**: `VSCHOOL_TRACKS`(区分。`mode`が`"school"`=高等教育、
  `"voc"`=職業訓練校)、`VSCHOOL_FIELDS`(区分ごとの分野)、
  `VSCHOOL_QUESTIONS`(キーは`<区分ID>:<分野ID>`)。家庭教師コースの
  「学年→教科→問題」を「区分→分野→問題」へ読み替えただけで、
  新しい概念を増やしていない。
- **1つのモーダルを2つのボタンから開き分ける**: `openVschoolModal(mode)`が
  タイトルと見出しを差し替え、`VSCHOOL_TRACKS`を`mode`で絞り込むだけ。
  モーダルHTMLを二重に持たない(重複を避ける)。モードが変わったら、
  前のモードで選んでいた区分・インストール済み分野は破棄して選び直させる。
- **未収録は必ず「準備中」**: `vschoolHasQuestions()`が偽の分野は
  チェックボックスを`disabled`にし、「準備中 / not ready yet」と表示する。
  「収録済み N/M 分野」を区分ボタン自体にも出して、**開く前に**どれだけ
  用意できているかが分かるようにしている。区分内に1分野も無い場合は、
  その旨の注記を分野一覧に出す(何も起きない画面で迷わせない)。
- **YouTubeは「検索結果ページ」へのリンクのみ**: `vschoolYoutubeUrl()`は
  一般的な検索キーワードを`encodeURIComponent`して
  `https://www.youtube.com/results?search_query=`に付けるだけ。
  **特定の動画を「これが正しい」と紹介しない**(内容を検証できないため)。
  リンクにはその旨の注記を必ず添える。`rel="noopener noreferrer"`を忘れずに。
- **自動採点になじまない出題形式は、その旨を明記して扱わない**:
  小論文・面接・実技は4択の知識問題として近似しているだけであり、
  「小論文添削・面接練習ができる」とは書かない(正直な開示に明記する)。
- **採点結果の保存**: 家庭教師コースと同じく`POST /v1/db/history`へ
  `role`だけ変えて(`"virtual-school-result"`)投げる。新しいテーブル・
  新しいAPIは作らない。
- **著作権**: 実際の入試問題・市販問題集・教科書は転載しない。
  出題形式の一般的傾向(小論文の三部構成、大学院入試での研究計画など)を
  調べた上で、問題文はすべて書き下ろす。

## 17. 「キャリアガイダンス」機能の移植ポイント(2026-08-24)

学年別・家庭教師コース(`TUTOR_*`)の出題画面に、教科ごとの業界・職種の
参考情報を補足表示する機能。移植時のポイント:

- **粒度は教科単位**: `TUTOR_CAREER_GUIDANCE`は問題1問ごとではなく
  教科ID単位(`japanese`/`math`/`life`/`science`/`social`/`english`/
  `programming`)で持つ。数百問すべてに手作業で個別の内容を付けるのは
  現実的でないため、既存データ構造(`TUTOR_QUESTIONS`)を変更せず、
  別の小さいルックアップテーブルとして追加した——既存データへの影響
  ゼロで追加できる設計。
- **断定表現は使わない**: 「〜かもしれません」「〜に役立つ可能性が
  あります」で統一し、「必ず就職できる」「これで資格が取れる」という
  趣旨の文言は一切使わない。末尾に「一般的な参考情報であり保証しない」
  旨の注記(`.setup-honest`相当)を必ず添える。
- **調査に基づく設計判断**: 実装前にドイツの職業教育制度(デュアル
  システム)を日英で調査し、「学習内容が具体的な職業・上級資格と
  結びついている」という考え方を参考にした
  (出典: [IHK Darmstadt](https://www.ihk.de/darmstadt/en/productlabels/training/voctrain-2533080)、
  [deutschland.de](https://www.deutschland.de/en/topic/business/how-germanys-dual-vocational-training-system-works)、
  [Wikipedia](https://en.wikipedia.org/wiki/Dual_education_system))。
  他プロジェクトへ移植する際も、対象国・対象読者に合わせて出典を
  調べ直し、推測で埋めないこと。
- **描画のタイミング**: `renderTutorSingleQuestion()`(1問描画するたび)
  から呼ぶことで、教科を切り替えても・採点後も表示が追従する。専用の
  `<div id="tutor-career-guidance">`をあらかじめ`index.html`に置き、
  データが無い教科(将来追加分等)では静かに`hidden`へ戻す——無理に
  空欄を埋めない。

## 追記(2026-08-24続き): VSCHOOL側のキャリアガイダンス移植ポイント

- `VSCHOOL_FIELDS`の各分野オブジェクトに`career: {ja, en}`を追加し、
  `vschoolCareerHtml(field)`で分野選択画面に描画する構造。移植先でも
  「〜かもしれません」調の非断定表現を崩さないこと。
- 家庭教師コース側の`TUTOR_CAREER_GUIDANCE`(教科ID単位)と役割が
  重複しないよう、**出題画面(`renderTutorSingleQuestion`)側ではなく
  分野/教科選択画面側にのみ**表示する設計にした——似た機能を複数箇所に
  追加する場合は、既存の表示箇所と重複しないか必ず確認すること
  (本セッションで実際に二重表示バグを起こした反省点)。
- CSSで明るい背景色を新設する際は、**必ず同じルールに明示的な文字色も
  併記する**こと(ダークテーマの`body`が明るい文字色を継承するため)。
  今回、`background`だけ指定して`color`を省略した箇所が複数見つかり
  白文字/白背景バグの原因になった。

## 追記(2026-08-24続き8): world-lab(遊休デバイス共有)の移植ポイント

`server/src/world_lab.rs`(デバイスペアリング+WASMサンドボックスでの
計算タスク実行)を他プロジェクトへ移植する場合の要点。

- **踏み台化防止の設計は「機能を削ること」で実現している**: 通信を
  他者へ転送するコード自体を書かない、という消極的な設計が核心。
  「安全に中継する」機構を作ろうとせず、そもそも中継機能を持たせない
  という選択が最も確実(コードが存在しなければ悪用しようがない)。
  移植先で似た機能を作る場合も、まず「中継しない」を要件として先に
  固定し、後から機能追加する形で緩めないこと。
- **二段階のオプトイン**: ペアリング(`OPEN_ENGLISH_WORLD_LAB_ENABLED`)
  とタスク実行(`OPEN_ENGLISH_WORLD_LAB_COMPUTE_ENABLED`)を別の
  環境変数で分離し、片方だけ有効化できるようにした。よりリスクの高い
  機能ほど、既定無効+別フラグで一段階多く保護する、という積み重ね方。
- **【最重要・実機で発見した教訓】fuel上限だけでは足りない**:
  `wasmtime`のfuel(命令数)上限は「guestコードを止める」機構としては
  機能するが、この開発環境(Windows)では**その停止処理自体がホスト
  プロセスをクラッシュさせる**という実バグに遭遇した(`/GS`スタック
  保護チェックとSEHベースのトラップ配送の相性問題、詳細は
  `world_lab.rs`のdoc・`CLAUDE.md`2026-08-24〈続き8〉参照)。
  **移植先でも「WASM実行はサンドボックスの外側(別プロセス)へ隔離する」
  設計を必ず踏襲すること**——fuel/メモリ上限だけでサンドボックスの
  実行系自体(wasmtime/wasmer等)の未知のバグから安全とは限らない。
  プロセス境界という、実行系のバグに依存しない独立した防御層を必ず
  重ねること。
- **サブプロセス通信は単純な長さ接頭辞つきバイナリフレーミング**で
  実装した(`run_in_worker_process`/`run_worker_main`)。既存の
  `--world-lab-worker`引数分岐は`fn main()`の最初(DBオープン等の
  通常起動処理より前)に置くこと——通常起動処理を先に実行してしまうと
  ワーカープロセスの起動が遅くなり、タイムアウトの見積もりが狂う。
- **単体テストでfuel枯渇を検証する場合、テストプロセス自身の中で
  直接実行系を呼ばないこと**——上記の理由で`cargo test`自体が
  クラッシュしうる。実HTTP経由(サブプロセス隔離を通した状態)での
  検証に限定するか、テスト専用の別プロセスを明示的に起動すること。

## 追記(2026-08-24続き9): world-labの残課題対応で得た追加の移植ポイント

- **「任意の入力を受け付けるエンドポイント」では、共有のJSONボディ
  読み取りヘルパーをそのまま使わないこと**: 既存の`read_rs_json_body`
  (他の`/v1/db/*`等と共有)はボディを無制限に読み切ってからパースする
  設計で、サイズ上限チェックは(Base64を使う場合)デコード**後**にしか
  行われない。「任意計算」「任意ファイル」等を受け付ける新しい
  エンドポイントを追加するたびに、専用の`http_body_util::Limited`で
  包んだ読み取り関数(`read_capped_rs_json_body`)を使うかどうか、
  都度立ち止まって考えること。既存の小さな設定値用エンドポイントには
  過剰な対策だが、「任意の大きなデータを受け付ける」性質の
  エンドポイントには必須。
- **同時実行数の上限は`tokio::sync::Semaphore`+キュー長を数える
  `AtomicUsize`の組み合わせが簡潔**: 上限に達している間は
  `acquire_owned().await`で自然に待たせられるが、**無制限に待たせる
  こと自体もメモリ枯渇の一種**なので、待機者数を数えて上限を超えたら
  即座に拒否する層を必ず加えること(`Semaphore`単体では「何人待って
  いるか」を数えて拒否する機能は持たない)。
- **サンドボックス実行系(wasmtime等)のバージョンを上げても、発見した
  クラッシュ系バグが直る保証はない**: 実際にバージョンを引き上げて
  同一手順で再現するかを確かめるまでは「新しいバージョンなら直っている
  はず」と楽観視しないこと。バージョン起因でないと分かった場合は、
  プロセス分離のような実行系のバグに依存しない防御を維持する判断材料
  になる。
- **複数デバイス間のE2E検証を謳う場合、実際に検証した範囲を正直に
  書くこと**: 開発環境に2台目の実機が無い状態での検証は、あくまで
  「同一マシン上での複数プロセス・複数接続方式のシミュレーション」
  であり、「別々の物理デバイス間」と書いてはならない。プロトコルが
  ループバックか実LANかを区別しない設計であることと、実際に別々の
  筐体で検証したかどうかは別の話。

## 追記(2026-08-25): デバイス種別・能力申告+テストフレーク修正で得た
## 移植ポイント

- **自己申告フィールド(デバイス種別・ハードウェア能力等)は、必ず
  サーバー側でホワイトリスト検証すること**: `PairedDevice.kind`/
  `capabilities`はいずれも固定の許可リスト(`ALLOWED_DEVICE_KINDS`/
  `ALLOWED_CAPABILITIES`)と照合してから受理する。文字列をそのまま
  記録すると、UI側の想定外の値やインジェクション的な文字列が一覧に
  混入し得る(今回は`textContent`で描画しているためXSSには繋がらない
  設計だが、値の妥当性自体は別問題)。
- **「ユーザーが強く望む自動化」でも、その自動化自体が新たな攻撃面に
  なる場合は実装を断ること**: 「手動設定時に自動でポート開放
  (UPnP)」という要望があったが、UPnPは長年の既知のルーター攻撃経路
  であり、踏み台化防止を掲げる機能へ組み込むのは本末転倒と判断し
  見送った。既存の設計(既定`127.0.0.1`限定バインド、`OPEN_ENGLISH_
  SERVER_BIND`変更が唯一の公開経路)が実は「手動設定のみで到達可能」
  という要件を既に満たしていることを説明し、新規のリスクを追加せずに
  要件を満たす方法を探すこと。
- **UI側の「自動入力」と「自動接続」を明確に区別すること**: 画面を
  開いた時点で既にある実測情報(このアプリでは`lastRuntimeInfo.
  gpu_in_use`)からフォームの初期値を推測して埋める(`autoDetect
  WorldLabPairFields()`)のは利便性向上として安全だが、**トークン
  入力・ボタン押下という明示的な操作を経ないペアリングの自動化は
  行わないこと**。この境界線を対話の中でユーザーへ明示的に説明した
  ことが重要(黙って要望の一部だけを実装するのではなく、なぜ実装
  しない部分があるのかを都度説明する)。
- **`std::env::set_var`/`remove_var`を使うRustの単体テストは、複数
  ファイルに同種のパターンが分散しやすい**: `local_agent.rs`で
  テストフレークを1件見つけて`Mutex`で修正した後、`grep -rl
  "set_var\|remove_var" src/*.rs`でリポジトリ全体を横断検索したところ、
  `vps_agent.rs`にも同じ問題が見つかった。1箇所直したら終わりにせず、
  同種のコードパターンが他に無いか横断検索する習慣を付けること。

## 追記(2026-08-25続き2): 一括操作APIの移植ポイント

- **一括APIを追加しても、1件ごとの検証をバイパスしないこと**:
  `bulk_pair()`は内部で単発の`pair()`をそのまま呼ぶ設計にした
  (専用の検証ロジックを別に書かない)。「一括だから速い簡易チェックで
  済ませる」という誘惑を避け、1件ごとの安全性は単発APIと完全に同一に
  保つこと。
- **部分成功を許容し、全滅にしないこと**: 100件中1件が不正でも、
  残り99件は登録される設計にした(`Vec<Result>`的な構造を返す)。
  「1件でもエラーなら全体を中断」という設計は、大量登録の実運用では
  かえって使いにくい。
- **件数上限を設けること**: 検証コスト自体は軽くても、無制限の配列を
  受理するとリクエストサイズ・処理時間が青天井になる。固定の上限
  (今回は100件)を設け、超過時は明確なエラーメッセージで理由を返す。

## 追記(2026-08-25続き): 開閉可能パネル+VPSデプロイの移植ポイント

- **「開閉可能パネル」は1つの汎用ヘルパーへ集約すること**:
  正直な開示ボックスの開閉(1箇所)から始まり、後から3箇所
  (スマホ活用バナー・多言語案内バナー・設定パネル全体)へ横展開する
  ことになった。最初から`makeCollapsiblePanel(boxId, btnId,
  storageKeySuffix, closedLabel, openLabel)`という汎用関数として
  実装しておいたため、横展開は各3行の呼び出し追加だけで済んだ——
  最初の1箇所を個別実装のまま放置していたら、横展開のたびに
  重複コードが増えていたはず。
- **VPS(easy-web.tokyo)への実デプロイ手順**(open-cg-cad等、他
  リポジトリへ同型のサーバーを追加する際に再利用できる手順、詳細は
  `CLAUDE.md`のHANDOFF〈2026-08-25(続き6)〉参照):
  1. VPS上に`git clone`(sibling依存があれば`ln -s`で共有クローンを
     再利用、無ければ新規clone)。
  2. `cargo build --release`(依存のバージョン不一致で実行時に
     初めて発覚するエラー——今回は`RPoem`の古いコミットに`.head()`
     メソッドが無く発覚——に注意、`git pull`で最新化してから再ビルド)。
  3. systemdサービス化(`127.0.0.1:<空きポート>`へバインド、
     `ss -tlnp`で事前に空きポートを確認)。
  4. **重要**: このVPSの実際の本番リバースプロキシがnginxか
     `open-web-server`か、`ss -tlnp`でポート80/443を実際に掴んでいる
     プロセスを確認してから設定を追加すること——nginxの設定ファイルを
     編集して`nginx -t`が通っても、別のプロセスが実際にトラフィックを
     処理していれば無意味な変更になる(今回、この事故を1度実際に
     起こしてから気づいた)。
  5. フロントエンドがAPI呼び出しに絶対パス(`/v1/...`)を使っている
     場合、サブパス配信(`/open-english/`等)だけでなく**ドメイン
     直下の`/v1/`もアプリへ転送する**設定が必要(`strip_prefix =
     false`)。他アプリが同じパス名前空間を使っていないか事前に
     ソース横断検索で確認すること。

## 追記(2026-08-29): AI音声認識(ASR)精度改善の移植ポイント / ASR accuracy overhaul porting notes

正本は [`docs/SPEECH_RECOGNITION_REDESIGN.md`](docs/SPEECH_RECOGNITION_REDESIGN.md)
(調査結果・3フェーズ設計・試作駆動の進め方・受け入れ基準)。ここには
「他リポジトリへ同型の改善を持ち込むときに再利用できる要点」だけを記す。
The canonical record is `docs/SPEECH_RECOGNITION_REDESIGN.md`; this section
only lists the reusable takeaways.

- **音声入力は必ず「言語タグ → BCP-47」で始める / Always start from a
  correct BCP-47 language tag**: `recognition.lang` を ja-JP/en-US 固定に
  しない。`app.js` の `speechLangTag()` + `LEARN_TARGET_TO_LANG_CODE` +
  `SPEECH_LANG_TAGS_EXTRA` が「学びたい言語 → BCP-47」を解決する。
  これだけで多言語での認識精度が大きく変わる(P1-α)。
- **n-best → 1回の LLM 訂正へ束ねる / Fuse all n-best into one LLM pass**:
  `refineTranscript(alts, langTag)` が `maxAlternatives=5` の候補を
  `window.tryPriorityProviderReply`(LLM 訂正)へ渡す。空/過長
  (最長候補の 2.5 倍超)は却下して素の 1-best へフォールバック。
  複数エンジン(Web Speech + ブラウザ Whisper + サーバー)を足す場合も、
  候補を1リストに `concat` してこの関数へ渡すだけ(§4.4 融合)。
  これは MPA GER(多パス増強生成訂正)の縮小版。
- **contextual biasing は「直前トレーナー発話」を訂正プロンプトへ /
  Feed the last trainer utterance as bias**: `lastTrainerUtterance()`
  (DOM `.msg.trainer` 末尾 200 字)。Whisper 呼び出し自体の `prompt`
  にも渡せると更に効く(whisper.cpp CLI は `--prompt` で確実)。
- **transformers.js の dtype 落とし穴 / transformers.js dtype pitfalls**:
  WebGPU + q8 デコーダは**出力が壊れる**。**fp32 エンコーダ + q4
  デコーダのハイブリッド**を使う(`dtype: { encoder_model: "fp32",
  decoder_model_merged: "q4" }`、失敗時 q8 単一へリトライ)。
  transformers.js は **3.8.x に固定**(v4.0.0-next はタイムスタンプ回帰)。
  幻覚対策に `condition_on_previous_text:false` /
  `no_speech_threshold` / `compression_ratio_threshold` / 温度
  フォールバックを付ける。VAD(Silero、ブラウザで ORT-web 実行可)での
  無音除去が幻覚対策として最も効く(P2-γ 必須)。
- **ハードウェアアクセラレータは実行段カスケードで / Cascade the
  execution tier**: `getWhisperPipeline()` が WebGPU → WebNN(npu/gpu/cpu)
  → WASM(スレッド数は `/v1/cpu-runtime` = open-cpu の検出ヒント)を
  順に試す。WebNN は 2026 時点でまだフラグ裏なので「効くのは限定的」と
  正直に注記すること。
- **モデル/ランタイムのホスト / Hosting the model + runtime**: リポジトリ
  に入れず、`fetch-whisper-model.ps1`(HF + jsdelivr から一度だけ取得)+
  `whisper-model-installer.exe`(ISCC)+ `server` の
  `maybe_fetch_whisper_model()`(起動時 + 6h ごと)で `/models/…` ・
  `/vendor/…` を**同一オリジン配信**。未配置なら静かに無効化 →
  Web Speech API 単独(回帰ゼロ)。
- **サーバー側 ASR は feature 隔離 + 外部バイナリ子プロセス / Server-side
  ASR = feature-gated + subprocess an external binary**: `aruaru-llm` の
  `POST /v1/transcribe` は `whisper-transcribe` Cargo feature(既定オフ)。
  `whisper-rs` を直接リンクすると **Windows/MSVC で bindgen が破綻**する
  既知ブロッカーがあるため、`pg_dump` / `Expand-Archive` / `adb` と同じ
  「プレビルド CLI を子プロセス起動」パターンで実装すること。
  `GET /v1/runtime` に `whisper` 段(`compiled_in`/`backend`/`model_present`)
  を出して可否を正直に見せる。
