# פילוסופיית פיתוח וכללי סביבה (open-english)

> **הערה**: זהו תרגום מתומצת של המצב הנוכחי. יומן ה-HANDOFF ההיסטורי
> המפורט (עשרות רשומות מאז 2026-08-10) נשאר זמין רק ביפנית בקובץ
> [CLAUDE.md](CLAUDE.md) לשם תמציתיות — עיינו שם לפרטי כל סשן.

כונן עבודה: `F:\runo`. חלק זה עוקב אחר הנוהג של העתקת החלק המקביל
מתוך `CLAUDE.md` של
[`open-raid-z`](https://github.com/aon-co-jp/open-raid-z) לכל פרויקט
כמקור אחיד. מאגר GitHub:
[aon-co-jp/open-english](https://github.com/aon-co-jp/open-english).

**תחילת פיתוח: 2026-08-10.**

## תפקיד הפרויקט הזה

אפליקציית אינטרנט ללימוד אנגלית עבור מחשב/טאבלט/סמארטפון. בסגנון
"שיעור אנגלית בקפה מייד", דמות מייד קסומה (עיצוב מקורי, מונפשת)
מלווה לומדים ממתחילים ועד רמה מתקדמת. תגובות הבינה המלאכותית
מטופלות על ידי
[`aruaru-llm`](https://github.com/aon-co-jp/aruaru-llm).

## ארכיטקטורה (לפי הנחיית המשתמש, 2026-08-10)

- **צד Linux (VPS)**: רק שרת הפצה להורדה. ניהול האפליקציה מתבצע על
  ידי [`open-easy-web`](https://github.com/aon-co-jp/open-easy-web).
- **צד מכשיר המשתמש**: הצד הקדמי הסטטי של המאגר הזה + שרת native
  שפועל מקומית מתוך `aruaru-llm` (משתמש פנימית ב-backend ההסקה של
  `open-directx`/`open-cuda`), שהמשתמש מוריד ומפעיל בעצמו. הדפדפן
  מתחבר מקומית (מקוון/לא מקוון) אל `http://localhost:4600`.

## גילוי כן / מגבלות ידועות (נכון ל-2026-08-10)

- `/v1/generate` של `aruaru-llm` מבוסס על GPT-2 (ממוקד אנגלית, ללא
  כוונון עדין לדיאלוג) — איכות התגובות והתאמה לרמה אינן מובטחות.
- ה-CORS תוקן ב-2026-08-10 (`.with_cors()` בצד `aruaru-llm`).
- סינתזת דיבור (TTS)/סנכרון שפתיים עבור דמויות המאמן מחוברת דרך
  Web Speech API; האנימציה של דמות המייד עצמה נותרת לולאת CSS פשוטה
  של פתיחה/סגירה של הפה.

## חזון עתידי (לפי הנחיית המשתמש, 2026-08-10, טרם החל)

קיים רעיון להריץ את `open-directx`/`open-cuda`/`aruaru-llm` גם באופן
עצמאי בדפדפן (WASM/WebGPU) ולשלב אותם עם `RPoem` (פלטפורמת GraphQL
Federation). מכיוון שמדובר בשינוי ארכיטקטוני גדול בהשוואה לעיצוב
שלב 0 הנוכחי (שרת שוכן מקומי + חיבור localhost), הדבר יטופל רק לאחר
השלמת ה-MVP, עם היקף עבודה נפרד.

## עדכונים אחרונים הקשורים להתקנה (2026-08-19)

מתקין Windows מאוחד `open-english-install.exe`; העדכון האוטומטי המובנה
(`server/src/self_update.rs`) הורחב ל-Linux ולאחר מכן ל-macOS, עם חזרה
אחורה (rollback) אוטומטית המבוססת על בדיקת תקינות `/healthz` אם הגרסה
החדשה אינה עולה כראוי; עמוד כניסה חדש `facebook.html` עבור משתמשים עם
גישה ל-Facebook בלבד (גילוי כן: אין שותפות רשמית עם Meta); ומאז
2026-08-18 האפליקציה מעודדת ומאוטמת את התקנת `rsync` כשהוא חסר, ומיד
לאחר מכן מפעילה גיבוי. פרטים מלאים רק ברשומות ה-HANDOFF ביפנית למטה.

*(הערת תרגום מכונה: סיכום זה תורגם על ידי סוכן הבינה המלאכותית עצמו,
ללא בדיקה של דובר שפת אם.)*

---

שפות נוספות: [日本語 (מקור, עם היסטוריית HANDOFF מלאה)](CLAUDE.md) ·
[Deutsch](CLAUDE-German.md) · [Italiano](CLAUDE-Italian.md) ·
[Français](CLAUDE-French.md) · [Русский](CLAUDE-Russian.md) ·
[Українська](CLAUDE-Ukrainian.md) · [فارسی](CLAUDE-Persian.md)
