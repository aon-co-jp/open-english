# فلسفه توسعه و قوانین محیط (open-english)

> **یادداشت**: این یک ترجمه فشرده از وضعیت فعلی است. تاریخچه دقیق
> HANDOFF (ده‌ها ورودی از 2026-08-10) به دلیل رعایت اختصار فقط به
> ژاپنی در [CLAUDE.md](CLAUDE.md) در دسترس باقی می‌ماند — برای جزئیات
> هر جلسه به آنجا مراجعه کنید.

درایو کاری: `F:\runo`. این بخش از رویه کپی‌کردن بخش مربوطه از
`CLAUDE.md` پروژه [`open-raid-z`](https://github.com/aon-co-jp/open-raid-z)
در هر پروژه به‌عنوان مرجع پیروی می‌کند. مخزن گیت‌هاب:
[aon-co-jp/open-english](https://github.com/aon-co-jp/open-english).

**تاریخ شروع توسعه: 2026-08-10.**

## نقش این پروژه

یک برنامه وب برای یادگیری انگلیسی برای رایانه شخصی/تبلت/گوشی هوشمند.
با سبک «کلاس انگلیسی در میدکافه»، شخصیتی از یک میدِ جادویی (طراحی
اصلی، متحرک) دانش‌آموزان را از مبتدی تا پیشرفته همراهی می‌کند.
پاسخ‌های هوش مصنوعی توسط
[`aruaru-llm`](https://github.com/aon-co-jp/aruaru-llm) مدیریت
می‌شوند.

## معماری (طبق دستور کاربر، 2026-08-10)

- **سمت لینوکس (VPS)**: فقط یک سرور توزیع برای دانلود. مدیریت برنامه
  توسط [`open-easy-web`](https://github.com/aon-co-jp/open-easy-web)
  انجام می‌شود.
- **سمت دستگاه کاربر**: بخش استاتیک این مخزن + یک سرور بومی که
  به‌طور محلی از `aruaru-llm` اجرا می‌شود (داخلاً از بک‌اندهای
  استنتاج `open-directx`/`open-cuda` استفاده می‌کند)، که کاربر خودش
  دانلود و اجرا می‌کند. مرورگر به‌صورت محلی (آنلاین/آفلاین) به
  `http://localhost:4600` متصل می‌شود.

## افشای صادقانه / محدودیت‌های شناخته‌شده (تا تاریخ 2026-08-10)

- `/v1/generate` از `aruaru-llm` بر پایه GPT-2 است (عمدتاً انگلیسی،
  بدون fine-tuning برای مکالمه) — کیفیت پاسخ‌ها و رعایت سطح تضمین‌شده
  نیست.
- CORS در تاریخ 2026-08-10 برطرف شد (`.with_cors()` در سمت
  `aruaru-llm`).
- سنتز گفتار (TTS)/هماهنگی لب برای شخصیت‌های مربی از طریق Web Speech
  API متصل شده است؛ انیمیشن خود شخصیت مید همچنان یک حلقه ساده CSS
  برای باز و بسته شدن دهان است.

## چشم‌انداز آینده (طبق دستور کاربر، 2026-08-10، هنوز شروع نشده)

ایده‌ای وجود دارد برای اجرای `open-directx`/`open-cuda`/`aruaru-llm`
به‌طور مستقل نیز در مرورگر (WASM/WebGPU) و یکپارچه‌سازی آن‌ها با
`RPoem` (یک پلتفرم GraphQL Federation). از آنجا که این یک تغییر
معماری بزرگ نسبت به طراحی فعلی فاز ۰ (سرور مقیم محلی + اتصال
localhost) است، این کار تنها پس از تکمیل MVP و با محدوده کاری جداگانه
انجام خواهد شد.

## سایر به‌روزرسانی‌ها (۲۰۲۶-۰۸-۱۹ تا ۲۰۲۶-۰۸-۲۰)

از آن زمان، از جمله موارد زیر اضافه شده‌اند: پیامی هنگام رسیدن به سقف
استفاده روزانه همراه با راهنمایی درباره طرح‌های پولی و سهمیه رایگان
سایر ارائه‌دهندگان؛ پنل جدید «Data & Model Storage» با رابط
پشتیبان‌گیری/بازیابی؛ بنری برای بسیج گوشی‌های هوشمند بلااستفاده
(`PhoneAccelWorker` همراه با تشخیص NNAPI)؛ پشتیبان‌گیری ترکیبی rsync
برای aruaru-db/PostgreSQL؛ صفحه نقشه راه موقت برای نسخه‌های کنسول
(PlayStation/Switch/Wii/Wii U، در انتظار تأیید صاحبان پلتفرم)؛
به‌روزرسانی خودکار که به همه اجزای همراه (aruaru-llm/aruaru-db) با
بررسی دوره‌ای هر ۶ ساعت و امکان کاهش نسخه دستی برای هر جزء گسترش
یافته؛ رفع اشکالی که باعث می‌شود aruaru-llm در ویندوز به‌طور خودکار
اجرا شود (بدون نیاز به مرحله دستی)؛ بنر اطلاعاتی درباره سهمیه رایگان
ارائه‌دهندگان هوش مصنوعی/جستجو (از جمله Claude/Anthropic)؛ رفع یک
اشکال حیاتی که از از دست رفتن پایگاه داده گفتگو هنگام
به‌روزرسانی/کاهش نسخه خودکار در ویندوز جلوگیری می‌کند (به‌صورت
سرتاسری از طریق نصب‌کننده واقعی تأیید شده)؛ و همچنین قابلیت‌های جدید
اندروید (تشخیص خودکار نشانی PC از طریق اسکن زیرشبکه، که روی یک دستگاه
واقعی مجهز به Snapdragon با تشخیص موفق NNAPI تأیید شده، هرچند
واگذاری واقعی محاسبات از طریق NNAPI هنوز پیاده‌سازی نشده است). برای
جزئیات به بخش‌های HANDOFF مورخ ۲۰۲۶-۰۸-۱۹ و ۲۰۲۶-۰۸-۲۰ در
[CLAUDE.md](CLAUDE.md) مراجعه کنید.

## به‌روزرسانی‌های اخیر مرتبط با نصب (2026-08-19)

نصب‌کننده یکپارچه ویندوز `open-english-install.exe`؛ به‌روزرسانی
خودکار داخلی (`server/src/self_update.rs`) به Linux و سپس macOS
گسترش یافته، همراه با بازگشت خودکار (rollback) مبتنی بر بررسی سلامت
`/healthz` در صورتی که نسخه جدید به‌درستی راه‌اندازی نشود؛ صفحه ورودی
جدید `facebook.html` برای کاربرانی که فقط به Facebook دسترسی دارند
(افشای صادقانه: این یک مشارکت رسمی با Meta نیست)؛ و از ۲۰۲۶-۰۸-۱۸،
برنامه نصب `rsync` را در صورت نبود آن ترویج و خودکار می‌کند و بلافاصله
پس از آن پشتیبان‌گیری را آغاز می‌کند. جزئیات کامل فقط در بخش‌های
HANDOFF ژاپنی در زیر موجود است.

*(یادداشت ترجمه ماشینی: این خلاصه توسط خود عامل هوش مصنوعی ترجمه شده
است، بدون بازبینی توسط یک گویشور بومی.)*

---

زبان‌های دیگر: [日本語 (نسخه اصلی، با تاریخچه کامل HANDOFF)](CLAUDE.md) ·
[Deutsch](CLAUDE-German.md) · [Italiano](CLAUDE-Italian.md) ·
[Français](CLAUDE-French.md) · [Русский](CLAUDE-Russian.md) ·
[Українська](CLAUDE-Ukrainian.md) · [עברית](CLAUDE-Hebrew.md)

## به‌روزرسانی 2026-08-22: آزمون‌های زبان‌های جهان، انتخاب زبان، خواندن چندزبانه

پیش از پیاده‌سازی کد بررسی شد: **هیچ فهرستی از زبان‌های ترجمه و هیچ
جدول i18n وجود نداشت** (`learn-target` تنها انگلیسی و ژاپنی را
می‌شناخت). بنابراین فهرست زبان‌ها در همین قابلیت از نو تعریف شد —
**۳۸ زبان** (۱۷ اروپایی شامل رومانش سوئیس، روسی، ۴ خاورمیانه‌ای،
۷ جنوب آسیا، ۸ شرق و جنوب‌شرق آسیا و یک آفریقایی)، به‌علاوهٔ انگلیسی و
ژاپنی که همیشه فعال‌اند.

- داده‌های تازه: `world-language-exams.json` (پرسش‌های چندگزینه‌ای اصلی
  با سطوح سبک CEFR، ۳ تا ۶ برای هر زبان) و `world-language-phrases.json`
  (۵ جملهٔ پایه × ۴۰ زبان).
- API تازه: `GET /v1/world-languages` تنها خلاصه را بازمی‌گرداند، بدون
  متن پرسش‌ها. برای ذخیره در پایگاه داده هیچ نقطهٔ پایانی تازه‌ای ساخته
  **نشد** — همان `POST /v1/db/history` کافی بود.
- رابط: بنر دوزبانه، پنل «🌐 Languages» با جعبه‌های انتخاب، «انتخاب همه»
  و «برداشتن همه جز EN و JA»، انتخاب **۲ تا ۵ زبان** (محدودیت در رابط
  اعمال می‌شود)، نمایش و خواندن پیاپی با تکرار نامحدود (کل یا یک زبان)،
  کپی، دانلود .txt و ذخیره در SQLite.
- پس از نمره‌دهی، پرسش‌های نادرست به گفت‌وگو با مربی همان زبان می‌رسند
  (استفادهٔ دوباره از `examPrepMissedQuestions`).

**افشای صادقانه**: این‌ها پرسش‌های اصلی‌اند و با گواهی‌نامه‌های رسمی
ارتباطی ندارند؛ تنها ۳ تا ۶ پرسش برای هر زبان؛ بازبینی توسط گویشوران
بومی انجام نشده؛ خواندن واقعی در ۳۸ زبان با صداهای واقعی بررسی نشده است
(تنها با یک جایگزین مشاهده‌ای). جزئیات در مدخل HANDOFF مورخ 2026-08-22
در [CLAUDE.md](CLAUDE.md).

## بازنویسی واژگانی متن ۶۶۶ (2026-08-23، فقط سبک)

سه بخش از `MARK_OF_BEAST_TEXTS` با زبانی روان‌تر بازنویسی شد: (۱) بازی
واژگانی **«۶۶۶ = WWW»**، (۲) افسانه‌ی شهری بارکد، (۳) نکته‌ی مکاشفه
۱۳:۱۶-۱۷ در کنار خرید با بارکد و Amazon.

- **پیش از تغییر**: «…نه به‌عنوان ادعای تحقق پیشگویی».
- **پس از تغییر**: «این را یک نکته‌ی سرگرم‌کننده بدانید، نه دلیلی محکم»؛
  «چه می‌دانم چه باید از آن برداشت کرد — اینجا کسی چیزی را ثابت نمی‌کند،
  اما از آن هم‌زمانی‌هایی است که متنی کهن را کمی نزدیک‌تر حس می‌کنی».

**محدودیت‌ها دست‌نخورده مانده‌اند** و همچنان باید رعایت شوند: (۱) هیچ‌چیز
به‌عنوان پیشگویی تحقق‌یافته ادعا نمی‌شود؛ (۲) ۶۶۶ بارکد همچنان صراحتاً
افسانه‌ای شهری و بی‌پایه از نظر فنی خوانده می‌شود (Snopes: نادرست؛
میله‌های محافظ ۳ ماژول در برابر ۷ ماژولِ یک رقم)؛ (۳) شباهت نشانِ مارِ
Python به «جانور» همچنان **تصادف محض** است؛ (۴) تفسیرها تنها معرفی
می‌شوند و هرگز به‌عنوان آموزه‌ی دینی تعلیم داده نمی‌شوند. تنها **سبک نثر**
تغییر کرده — برای خوانایی، نه تضعیف صداقت.

## پاسخ‌های متن‌ثابت مبتنی بر قاعده در موضوعات حساس (2026-08-23)

در موضوعاتی که خطای واقعیت‌محور بیشترین زیان را دارد، برنامه **بدون
استنتاج هوش مصنوعی** و با متنی دست‌نوشته پاسخ می‌دهد. سه مورد الگوی
یکسانی دارند: `isCreatorQuestion()` (معرفی سازنده)،
`isReligionHistoryQuestion()` / `RELIGION_HISTORY_TEXTS` (اسلام، ایران،
جهان عرب) و `isMarkOfBeastQuestion()` / `MARK_OF_BEAST_TEXTS` (۶۶۶ و نشان
جانور). دلیل: GPT-2 خام مطالب را از خود می‌سازد و در تاریخ ادیان زیان آن
بزرگ است. سهمیه‌ی روزانه مصرف نمی‌شود و متن‌ها در جدولی با کلید کد زبان
نگهداری می‌شوند (فعلاً تنها `ja` و `en`).

**تاریخ دینی بی‌طرفانه**: گوناگونی دینی عربستانِ پیش از اسلام؛ شکل‌گیری
قرآن که پژوهش آن را **سنتی مستقل** توصیف می‌کند (در آموزه‌ی اسلامی، وحی
به محمد)؛ تفاوت تمدن ایرانی و عربی؛ تأثیر زردشتی تنها به عنوان فرضیه‌ی
برخی پژوهشگران. دو ادعای نخستین (قرآن برآمده از ترجمه‌ی کتاب مقدس؛ برادر
محمد به عنوان مترجم) **به سبب نبود پشتوانه کنار گذاشته شد** و متن آشکارا
می‌گوید *چرا* آن‌ها را بیان نمی‌کند. یادداشت درباره‌ی محدودیت‌های ترجمه‌ی
پیشامدرن **تنها** به ترجمه‌های عربیِ مستند تاریخیِ کتاب مقدس مربوط است و
**هرگز** نباید به شکل‌گیری قرآن پیوند بخورد. در پایان، پیام ترجمه،
گفت‌وگوی چندزبانه و صلح.

**۶۶۶ / نشان جانور**: بند مکاشفه بی‌طرفانه نقل می‌شود، بدون ادعای تفسیر
«درست». بازی واژگانی **«۶۶۶ = WWW»** (جماتریا، واو = ۶) **تنها به عنوان
خوانشی رایج از دهه‌ی ۱۹۹۰** معرفی می‌شود، نه آموزه. داستان ۶۶۶ پنهان در
بارکد **افسانه‌ی شهری خوانده می‌شود** و فنی توضیح داده می‌شود: **میله‌های
محافظ** آغاز، پایان و میانه را برای اسکنر نشان می‌دهند، تنها ظاهرشان به
رقم ۶ می‌ماند و رمزگذاری‌شان متفاوت است (سه ماژول در برابر هفت)؛ Snopes
این ادعا را نادرست ارزیابی می‌کند — **نه معنای رمزی، نه پایه‌ی فنی**.
سپس جمع‌بندی مثبت (وب و اسکنرها خرید را آسان کرده‌اند **بی‌نیاز از نشانی
بر بدن**) و نکته‌ی Python (نشانِ مار، نام از مونتی پایتون) که **صراحتاً
هم‌زمانی تصادفی اعلام شده است**.

**افزوده‌ی 2026-08-23 (هم‌سانی جالب — معرفی، نه ادعا)**: در مکاشفه
۱۳:۱۶-۱۷ به‌راستی بندی هست که می‌گوید هیچ‌کس بدون آن نشان نمی‌تواند بخرد یا
بفروشد (وجود خودِ این آیه را می‌توان به‌عنوان واقعیت نقل کرد). جمله‌ای
افزوده شد مبنی بر اینکه **برخی افراد میان این و وابستگی روزافزون خرید امروزی
به بارکد و پرداخت‌های آنلاین مانند Amazon هم‌سانی جالبی می‌بینند**.
**محدودیت الزامی**: تنها به‌عنوان یک احتمال مطرح شود («برخی چنین می‌گویند»)،
و **هرگز** ادعا نشود که پیشگویی‌ای تحقق یافته است — خودِ متن این را
صراحتاً می‌گوید.

**هنگام تغییر**: چهار تضمین صداقت — «معرفی به‌جای ادعا»، «افسانه صراحتاً
افسانه»، «تصادف صراحتاً تصادف» — نباید تضعیف یا حذف شود.

## معمای اصیل سازنده (2026-08-23، ادامه ۴)

به درخواست کاربر، هنگامی که کاربر بگوید «یک معما بپرس» / «کوییز بده» /
"give me a quiz"، برنامه **معمای اصیل سازنده، آقای ماساهیرو ایشیزوکا**
را مطرح می‌کند. این ویژگی **دقیقاً همان شاخه‌ی مبتنی بر قاعده** را دنبال
می‌کند که `isCreatorQuestion()` / `isReligionHistoryQuestion()` /
`isMarkOfBeastQuestion()` دارند: متن ثابت، بدون عبور از استنتاج هوش
مصنوعی (`aruaru-llm`)، و بدون مصرف سهمیه‌ی روزانه.

**صورت مسئله**: با چهار بار نوشتن رقم ۹، جای ◯ در
`9 ◯ 9 ◯ 9 ◯ 9 = 10` را با `+ - × ÷` پر کنید (تکرار نشانه آزاد، پرانتز
( ) برای تغییر ترتیب مجاز) تا حاصل ۱۰ شود.
**پاسخ**: `(9 × 9 + 9) ÷ 9 = 10` (۹×۹=۸۱، ۸۱+۹=۹۰، ۹۰÷۹=۱۰) — **محاسبه
بازبینی شده است**؛ اگر راه‌حل دیگری افزودید، حتماً خودتان حساب را وارسی
کنید.

**ویژگی‌ای که هرگز نباید از متن حذف شود**: این معما **چیستانِ بامزه یا
پرسشی حقه‌بازانه نیست** و تنها با چهار عمل اصلی حل می‌شود (با ماشین‌حساب
یا چرتکه قابل آزمودن). بدون این جمله، کاربر بیهوده به راه‌هایی مانند
«رقم ۹ را بخوابانید» می‌افتد. همچنین نکته‌ی «کم‌سن‌ترین حل‌کننده کودکی در
کلاس اول دبستان بود» ماجرایی واقعی است که سازنده نقل کرده — **چیزی از خود
به آن نیفزایید**.

**چرا تولید آن را به هوش مصنوعی نمی‌سپاریم**: اگر طرح مسئله و تصحیح را به
GPT-2 خام بسپاریم، «دروغ‌های باورپذیر» با حساب نادرست می‌سازد. به همین
سبب (همان دلیل تاریخ ادیان و ۶۶۶) هم پرسش و هم پاسخ به متن ثابت محدود
شده‌اند.

**پیاده‌سازی**: `isQuizRequest()` (تشخیص درخواست معما)،
`isQuizAnswerRequest()` (تشخیص «نمی‌دانم» / «جواب را بگو»)، جدول ترجمه‌ها
`QUIZ_TEXTS`، سازنده‌های متن `quizQuestionText()` / `quizAnswerText()` و
تعیین زبان `quizPreferredLangCode()` در `app.js`.
**گفت‌وگوی دومرحله‌ای**: وضعیت تنها با یک پرچمِ سطحِ ماژول
`quizAwaitingAnswer` نگهداری می‌شود (مانند `examPrepMissedQuestions`؛
ماشین حالت ساخته نمی‌شود): نخست **تنها صورت مسئله**، و پس از «نمی‌دانم»
پاسخ. در کنترل‌کننده‌ی ارسال، بررسی «در انتظار پاسخ» **پیش از**
`isQuizRequest` قرار می‌گیرد. کلیدواژه‌های `isQuizAnswerRequest()` عمومی‌اند
(«نمی‌دانم») اما تنها هنگام `quizAwaitingAnswer === true` سنجیده می‌شوند و
بنابراین تمرین عادی مکالمه را نمی‌ربایند.

**چندزبانگی و محدودیت صادقانه‌ی آن**: پیش‌فرض **ژاپنی + انگلیسی**. اگر
ترجمه‌ی زبان انتخابی کاربر («زبانی که می‌خواهم بیاموزم» `world:<code>` با
بالاترین اولویت، وگرنه زبان مادری) در `QUIZ_TEXTS` باشد، در آغاز افزوده
می‌شود. اکنون تنها **۷ زبان ja / en / es / fr / de / zh / ko** ترجمه
دارند؛ ۱۳۰ زبان پشتیبانی‌شده را عمداً با ترجمه‌ی ماشینی پر نکرده‌ایم تا
«پشتیبانی کامل» به نظر برسد — برای زبان‌های نیامده همان پیش‌فرض ژاپنی+
انگلیسی مطرح می‌شود. برای افزودن ترجمه کافی است کلیدی با کد زبان به
`QUIZ_TEXTS` بیفزایید (کدهای فرعی مانند `zh-Hant` از ترجمه‌ی کد پایه `zh`
استفاده می‌کنند).

**کار بعدی**: اگر سازنده معمای اصیل دیگری در اختیار گذاشت، `QUIZ_TEXTS`
را به **آرایه‌ای از مسئله‌ها** گسترش دهید (اکنون تنها یک مسئله هست و با
«یکی دیگر» همان مسئله بازمی‌گردد).

## راهنمای شغلی در دوره خصوصی بر اساس پایه تحصیلی (2026-08-24)

صفحه تمرین اکنون برای هر درس (زبان ژاپنی، حساب، علوم، مطالعات اجتماعی،
انگلیسی، برنامه‌نویسی، زندگی روزمره) کادر «راهنمای شغلی» را نشان
می‌دهد که صنعت‌ها/مشاغلی که تسلط بر آن ممکن است کمک کند و مشاغل
پیشرفته‌ای که با تعمیق بیشتر ممکن است در دسترس باشد را فهرست می‌کند.
طراحی بر اساس پژوهش واقعی درباره نظام دوگانه آموزش حرفه‌ای آلمان
(Berufsschule، مدارک IHK، Ausbildung — منابع: IHK Darmstadt،
deutschland.de، ویکی‌پدیا) انجام شده است. لحن همیشه محتاطانه («ممکن
است کمک کند»)، بدون وعده استخدام تضمینی. دامنه: سطح درس، نه هر سؤال
به‌طور جداگانه؛ به‌صورت واقعی آزمایش شده (سرور اجرا شد، ریاضی پایه سوم
نصب شد، نمایش صحیح تأیید شد). جزئیات کامل فقط در نسخه ژاپنی CLAUDE.md.
