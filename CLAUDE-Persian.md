# فلسفه توسعه و قوانین محیط (open-english)

> **یادداشت**: این یک ترجمه فشرده از وضعیت فعلی است. تاریخچه دقیق
> HANDOFF (ده‌ها ورودی از 2026-08-10) به دلیل رعایت اختصار فقط به
> ژاپنی در [CLAUDE.md](CLAUDE.md) در دسترس باقی می‌ماند — برای جزئیات
> هر جلسه به آنجا مراجعه کنید.

درایو کاری: `F:\runo`. این بخش از رویه کپی‌کردن بخش مربوطه از
`CLAUDE.md` پروژه [`open-raid-z`](https://github.com/aon-co-jp/open-raid-z)
در هر پروژه به‌عنوان مرجع پیروی می‌کند. مخزن گیت‌هاب:
[aon-co-jp/open-english](https://github.com/aon-co-jp/open-english).

**تاریخ شروع توسعه: 2026-08-10.**

## نقش این پروژه

یک برنامه وب برای یادگیری انگلیسی برای رایانه شخصی/تبلت/گوشی هوشمند.
با سبک «کلاس انگلیسی در میدکافه»، شخصیتی از یک میدِ جادویی (طراحی
اصلی، متحرک) دانش‌آموزان را از مبتدی تا پیشرفته همراهی می‌کند.
پاسخ‌های هوش مصنوعی توسط
[`aruaru-llm`](https://github.com/aon-co-jp/aruaru-llm) مدیریت
می‌شوند.

## معماری (طبق دستور کاربر، 2026-08-10)

- **سمت لینوکس (VPS)**: فقط یک سرور توزیع برای دانلود. مدیریت برنامه
  توسط [`open-easy-web`](https://github.com/aon-co-jp/open-easy-web)
  انجام می‌شود.
- **سمت دستگاه کاربر**: بخش استاتیک این مخزن + یک سرور بومی که
  به‌طور محلی از `aruaru-llm` اجرا می‌شود (داخلاً از بک‌اندهای
  استنتاج `open-directx`/`open-cuda` استفاده می‌کند)، که کاربر خودش
  دانلود و اجرا می‌کند. مرورگر به‌صورت محلی (آنلاین/آفلاین) به
  `http://localhost:4600` متصل می‌شود.

## افشای صادقانه / محدودیت‌های شناخته‌شده (تا تاریخ 2026-08-10)

- `/v1/generate` از `aruaru-llm` بر پایه GPT-2 است (عمدتاً انگلیسی،
  بدون fine-tuning برای مکالمه) — کیفیت پاسخ‌ها و رعایت سطح تضمین‌شده
  نیست.
- CORS در تاریخ 2026-08-10 برطرف شد (`.with_cors()` در سمت
  `aruaru-llm`).
- سنتز گفتار (TTS)/هماهنگی لب برای شخصیت‌های مربی از طریق Web Speech
  API متصل شده است؛ انیمیشن خود شخصیت مید همچنان یک حلقه ساده CSS
  برای باز و بسته شدن دهان است.

## چشم‌انداز آینده (طبق دستور کاربر، 2026-08-10، هنوز شروع نشده)

ایده‌ای وجود دارد برای اجرای `open-directx`/`open-cuda`/`aruaru-llm`
به‌طور مستقل نیز در مرورگر (WASM/WebGPU) و یکپارچه‌سازی آن‌ها با
`RPoem` (یک پلتفرم GraphQL Federation). از آنجا که این یک تغییر
معماری بزرگ نسبت به طراحی فعلی فاز ۰ (سرور مقیم محلی + اتصال
localhost) است، این کار تنها پس از تکمیل MVP و با محدوده کاری جداگانه
انجام خواهد شد.

## سایر به‌روزرسانی‌ها (۲۰۲۶-۰۸-۱۹ تا ۲۰۲۶-۰۸-۲۰)

از آن زمان، از جمله موارد زیر اضافه شده‌اند: پیامی هنگام رسیدن به سقف
استفاده روزانه همراه با راهنمایی درباره طرح‌های پولی و سهمیه رایگان
سایر ارائه‌دهندگان؛ پنل جدید «Data & Model Storage» با رابط
پشتیبان‌گیری/بازیابی؛ بنری برای بسیج گوشی‌های هوشمند بلااستفاده
(`PhoneAccelWorker` همراه با تشخیص NNAPI)؛ پشتیبان‌گیری ترکیبی rsync
برای aruaru-db/PostgreSQL؛ صفحه نقشه راه موقت برای نسخه‌های کنسول
(PlayStation/Switch/Wii/Wii U، در انتظار تأیید صاحبان پلتفرم)؛
به‌روزرسانی خودکار که به همه اجزای همراه (aruaru-llm/aruaru-db) با
بررسی دوره‌ای هر ۶ ساعت و امکان کاهش نسخه دستی برای هر جزء گسترش
یافته؛ رفع اشکالی که باعث می‌شود aruaru-llm در ویندوز به‌طور خودکار
اجرا شود (بدون نیاز به مرحله دستی)؛ بنر اطلاعاتی درباره سهمیه رایگان
ارائه‌دهندگان هوش مصنوعی/جستجو (از جمله Claude/Anthropic)؛ رفع یک
اشکال حیاتی که از از دست رفتن پایگاه داده گفتگو هنگام
به‌روزرسانی/کاهش نسخه خودکار در ویندوز جلوگیری می‌کند (به‌صورت
سرتاسری از طریق نصب‌کننده واقعی تأیید شده)؛ و همچنین قابلیت‌های جدید
اندروید (تشخیص خودکار نشانی PC از طریق اسکن زیرشبکه، که روی یک دستگاه
واقعی مجهز به Snapdragon با تشخیص موفق NNAPI تأیید شده، هرچند
واگذاری واقعی محاسبات از طریق NNAPI هنوز پیاده‌سازی نشده است). برای
جزئیات به بخش‌های HANDOFF مورخ ۲۰۲۶-۰۸-۱۹ و ۲۰۲۶-۰۸-۲۰ در
[CLAUDE.md](CLAUDE.md) مراجعه کنید.

## به‌روزرسانی‌های اخیر مرتبط با نصب (2026-08-19)

نصب‌کننده یکپارچه ویندوز `open-english-install.exe`؛ به‌روزرسانی
خودکار داخلی (`server/src/self_update.rs`) به Linux و سپس macOS
گسترش یافته، همراه با بازگشت خودکار (rollback) مبتنی بر بررسی سلامت
`/healthz` در صورتی که نسخه جدید به‌درستی راه‌اندازی نشود؛ صفحه ورودی
جدید `facebook.html` برای کاربرانی که فقط به Facebook دسترسی دارند
(افشای صادقانه: این یک مشارکت رسمی با Meta نیست)؛ و از ۲۰۲۶-۰۸-۱۸،
برنامه نصب `rsync` را در صورت نبود آن ترویج و خودکار می‌کند و بلافاصله
پس از آن پشتیبان‌گیری را آغاز می‌کند. جزئیات کامل فقط در بخش‌های
HANDOFF ژاپنی در زیر موجود است.

*(یادداشت ترجمه ماشینی: این خلاصه توسط خود عامل هوش مصنوعی ترجمه شده
است، بدون بازبینی توسط یک گویشور بومی.)*

---

زبان‌های دیگر: [日本語 (نسخه اصلی، با تاریخچه کامل HANDOFF)](CLAUDE.md) ·
[Deutsch](CLAUDE-German.md) · [Italiano](CLAUDE-Italian.md) ·
[Français](CLAUDE-French.md) · [Русский](CLAUDE-Russian.md) ·
[Українська](CLAUDE-Ukrainian.md) · [עברית](CLAUDE-Hebrew.md)

## به‌روزرسانی 2026-08-22: آزمون‌های زبان‌های جهان، انتخاب زبان، خواندن چندزبانه

پیش از پیاده‌سازی کد بررسی شد: **هیچ فهرستی از زبان‌های ترجمه و هیچ
جدول i18n وجود نداشت** (`learn-target` تنها انگلیسی و ژاپنی را
می‌شناخت). بنابراین فهرست زبان‌ها در همین قابلیت از نو تعریف شد —
**۳۸ زبان** (۱۷ اروپایی شامل رومانش سوئیس، روسی، ۴ خاورمیانه‌ای،
۷ جنوب آسیا، ۸ شرق و جنوب‌شرق آسیا و یک آفریقایی)، به‌علاوهٔ انگلیسی و
ژاپنی که همیشه فعال‌اند.

- داده‌های تازه: `world-language-exams.json` (پرسش‌های چندگزینه‌ای اصلی
  با سطوح سبک CEFR، ۳ تا ۶ برای هر زبان) و `world-language-phrases.json`
  (۵ جملهٔ پایه × ۴۰ زبان).
- API تازه: `GET /v1/world-languages` تنها خلاصه را بازمی‌گرداند، بدون
  متن پرسش‌ها. برای ذخیره در پایگاه داده هیچ نقطهٔ پایانی تازه‌ای ساخته
  **نشد** — همان `POST /v1/db/history` کافی بود.
- رابط: بنر دوزبانه، پنل «🌐 Languages» با جعبه‌های انتخاب، «انتخاب همه»
  و «برداشتن همه جز EN و JA»، انتخاب **۲ تا ۵ زبان** (محدودیت در رابط
  اعمال می‌شود)، نمایش و خواندن پیاپی با تکرار نامحدود (کل یا یک زبان)،
  کپی، دانلود .txt و ذخیره در SQLite.
- پس از نمره‌دهی، پرسش‌های نادرست به گفت‌وگو با مربی همان زبان می‌رسند
  (استفادهٔ دوباره از `examPrepMissedQuestions`).

**افشای صادقانه**: این‌ها پرسش‌های اصلی‌اند و با گواهی‌نامه‌های رسمی
ارتباطی ندارند؛ تنها ۳ تا ۶ پرسش برای هر زبان؛ بازبینی توسط گویشوران
بومی انجام نشده؛ خواندن واقعی در ۳۸ زبان با صداهای واقعی بررسی نشده است
(تنها با یک جایگزین مشاهده‌ای). جزئیات در مدخل HANDOFF مورخ 2026-08-22
در [CLAUDE.md](CLAUDE.md).
