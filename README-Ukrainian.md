# open-english

> 📌 **Оновлення (2026-08-27)**: Додано «Куточок фриланс-розробки» (100 мов, посилання для пошуку вакансій, передача ШІ-вчителю), кроссдоменне iframe-сховище (`vault.html`), яке ізолює GitHub-токен і ключ Google Search API від основної сторінки, другу резервну електронну адресу для входу та ярлик запуску Chrome в режимі інкогніто. Виправлено дві реальні помилки, знайдені під час живого тестування в браузері (витік застарілого ключа, оманливе повідомлення про стан у режимі сховища). Деталі в записах від 2026-08-27 у CLAUDE.md (японською).


> 📌 **Оновлення (2026-08-24): до курсу репетиторства за класами додано
> «кар'єрну навігацію».** Екран практики тепер показує для кожного
> предмета, яким галузям/професіям він може стати в пригоді, і які
> просунуті професії можна розглядати, поглиблюючи ці знання —
> формулювання завжди обережні («може допомогти», без обіцянки
> гарантованого працевлаштування). Дизайн спирається на реальне
> дослідження німецької дуальної системи професійної освіти
> (Berufsschule, кваліфікації IHK, Ausbildung). Подробиці — у CLAUDE.md.

> 📌 **Найсвіжіше (2026-08-23, продовження 4)**: **функція вікторини з
> оригінальною задачею автора**. Якщо написати «загадай мені задачу»,
> «дай вікторину» чи "give me a quiz", застосунок видає оригінальну
> задачу **Масахіро Ішідзуки (石塚正浩)**, автора цієї програми: між
> **чотирма дев'ятками** — `9 ◯ 9 ◯ 9 ◯ 9 = 10` — потрібно поставити
> знаки `+`, `-`, `×`, `÷` (той самий знак можна вживати кілька разів);
> за потреби дозволено змінювати порядок дій дужками ( ). Результат має
> дорівнювати **рівно 10**. Відповідь: `(9 × 9 + 9) ÷ 9 = 10` (9×9=81,
> 81+9=90, 90÷9=10). Це **не жарт і не задача з підступом**, а **чиста
> арифметика чотирьох дій**, яку можна перевірити на калькуляторі чи
> рахівниці. До речі, найменшим, хто дав правильну відповідь, була
> дитина з **першого класу початкової школи**. Спілкування **двоетапне**:
> спершу показується лише умова, і тільки коли користувач напише «не
> знаю» або «скажи відповідь», з'являється розв'язок.
> **Багатомовність і чесна межа**: типово текст подається двома мовами —
> японською та англійською; якщо як мову вивчення або рідну обрано
> es/fr/de/zh/ko, переклад додається спереду. Переклади існують **лише
> для цих семи мов** (ja/en/es/fr/de/zh/ko) — усі 130 підтримуваних мов
> **свідомо не** заповнювали машинним перекладом, щоб не вдавати
> «підтримку всіх мов»; користувач іншої мови отримує стандартну
> японсько-англійську пару. Як і відповіді «хто це зробив», про історію
> релігій і про «666», це **правиловий фіксований текст без звернення до
> ШІ** — «гола» GPT-2 в обчисленнях видає переконливі, але неправильні
> результати. **Денний ліміт використань при цьому не витрачається.**

> 📌 **Найсвіжіше (2026-08-23, лише формулювання)**: три місця у відповіді
> про «666 / число звіра» переписано живішою мовою — гру слів **«666 =
> WWW»**, міську легенду про штрихкод і зауваження про Одкровення 13:16-17
> поряд із сучасними покупками. Сухі застереження («не як твердження, ніби
> пророцтво справдилося») замінено теплішим тоном: «сприймайте це радше як
> кумедну дрібничку, ніж як переконливий доказ» і «хтозна, що з цим робити —
> ніхто тут нічого не доводить, але саме такі збіги роблять давній текст
> трохи ближчим». **Зміст обмежень не змінився**: нічого не подано як
> справджене пророцтво, штрихкодовий 666 і далі прямо названо технічно
> безпідставною міською легендою (Snopes — ХИБНЕ; захисні риски мають 3
> модулі проти 7 у цифри), а схожість Python зі «звіром» — чистим збігом.
> Змінився **лише стиль**: заради читабельності й приязності, а не заради
> послаблення чесності.

> 📌 **Останнє оновлення (2026-08-24, продовження 6)**: додано цитати/
> прислів'я + мотиваційне повідомлення + новий предмет «Комунікація та
> вміння ставити запитання». Кожен блок кар'єрної орієнтації тепер
> показує двомовну (українську/англійську) цитату чи прислів'я
> (наприклад, «Strike while the iron is hot») і обережно сформульоване
> мотиваційне повідомлення (не гарантію), що виражає надію, що учень
> зможе знайти роботу, змінити кар'єру та заробляти на життя будь-де.
> Новий предмет (середня/старша школа) навчає реальних англійських фраз
> для конструктивного гіпотетичного обговорення та конструктивного
> відгуку. Деталі в CLAUDE.md.

> 📌 **Останнє оновлення (2026-08-23)**: додано дві **правилові відповіді
> фіксованим текстом**, написані вручну (двомовно JA/EN, без звернення до
> ШІ).
> **(1) Іслам, Іран/Персія та арабський світ**: на запитання про історію та
> коріння видається нейтральна фактологічна довідка — християнські громади
> доісламської Аравії (Наджран, Гассаніди), складання Корану, яке наука
> описує як **окрему самостійну традицію**, відмінність іранської та
> арабської цивілізацій і зороастрійський вплив, причому останній подано
> суто як **«гіпотезу, яку висловлюють деякі дослідники»**, а не як
> усталений факт. Два твердження, які спершу хотіли включити (Коран постав
> із перекладу Біблії; перекладачем був брат Мухаммада), **свідомо
> вилучено через брак підтверджень у джерелах**. Завершує текст думка, що
> мовні бар'єри живлять непорозуміння, а **машинний переклад і багатомовне
> спілкування можуть зміцнювати взаєморозуміння та мир**.
> **(2) «666 — це число звіра?»**: легка відповідь у жанрі цікавинки. Місце
> з Одкровення наведено нейтрально; сучасну гру слів **«666 = WWW»**
> (єврейська гематрія: літера вав дорівнює 6) подано **явно як прочитання
> окремих людей, а не як віровчення**; історію про «прихований 666 у
> штрихкоді» **прямо названо міською легендою** з поясненням справжньої
> техніки: довші риски по краях і посередині — це **захисні (guard) риски**,
> що позначають для сканера початок, кінець і середину коду; вони лише
> *схожі* на цифру 6, але мають інше кодування (3 модулі проти 7) — Snopes
> оцінює твердження як ХИБНЕ: **жодного окультного змісту й жодних
> технічних підстав**. Далі позитивний висновок: веб і сканери зробили
> покупки зручними, **і нікому не потрібна позначка на тілі**; наприкінці
> примітка, що логотип Python — змія, але назва походить від комедійного
> серіалу «Літаючий цирк Монті Пайтона», а схожість зі «звіром» **прямо
> названо чистим збігом і грою слів** без жодного змістового зв'язку.
> **Доповнення від 2026-08-23**: додано ще одне зауваження про те, що в
> Одкровенні 13:16-17 справді є місце, за яким ніхто без начертання не може
> купувати чи продавати, і що **дехто вбачає в цьому цікаву паралель** із
> тим, як сучасні покупки дедалі більше залежать від штрихкодів і онлайн-
> платежів на кшталт Amazon — **виключно як збіг, що комусь видається
> прикметним, і аж ніяк не як твердження, ніби пророцтво справдилося**.
> Подробиці — у записах HANDOFF від 2026-08-23 у [CLAUDE.md](CLAUDE.md).

> 📌 **Останнє оновлення (2026-08-22)**: додано **пробні тести з мов
> світу, інтерфейс вибору мов і послідовний багатомовний показ з
> озвученням**. Англійська та японська залишаються мовами за
> замовчуванням, але двомовний банер і панель «🌐 Languages» дають
> змогу увімкнути оригінальні набори завдань для **38 мов** (Європа,
> Близький Схід, Азія, Індія, Африка). Після перевірки помилкові
> питання переходять у розмовну практику з викладачем відповідної мови
> — так само, як у наявному сценарії Eiken/TOEIC/TOEFL/JLPT. Також
> можна обрати **від 2 до 5 мов** (разом з англійською та японською) і
> по черзі показувати й прослуховувати ту саму фразу, повторюючи
> скільки завгодно разів, з копіюванням, завантаженням .txt і
> збереженням у локальну базу SQLite. Чесне розкриття: це оригінальні
> питання, написані для цього застосунку, а не завдання минулих років;
> вони не пов'язані з реальними мовними іспитами (DELE, DELF,
> Goethe-Zertifikat, HSK, TOPIK тощо). Рівні у стилі CEFR (A1–C2) — це
> лише приблизний орієнтир, кількість завдань неоднакова (3–6 на мову),
> а озвучення використовує Web Speech API браузера: без встановленого
> голосу текст лише відображається. Подробиці — у записі HANDOFF від
> 2026-08-22 у [CLAUDE.md](CLAUDE.md).

> 📌 **Останнє оновлення (2026-08-18)**: Додано повноцінну локальну базу
> даних для історії чату/налаштувань (SQLite + опційне
> самовідновлювальне дзеркало `aruaru-db`/PostgreSQL), а також API
> вибору місця зберігання, резервного копіювання через rsync та імпорту
> старих даних. Якщо `rsync` не встановлено, застосунок показує
> двомовне повідомлення **«Let's install RSync!»** і може встановити
> його автоматично через менеджер пакунків ОС, одразу після цього
> запустивши резервне копіювання. Деталі в [CLAUDE.md](CLAUDE.md)
> (записи HANDOFF від 2026-08-18, японською).

> 📌 **Попереднє оновлення (2026-08-11–12, v0.6.0)**: Android/планшет
> тепер працює повністю автономно — без потреби в ПК чи Linux-сервері.
> Сам рушій ШІ-відповідей (`aruaru-llm`) тепер вбудований у APK;
> перевірка на пристрої підтвердила, що обидва процеси залишаються
> живими та відповідають на `/healthz`/`/v1/chat`. Також додано:
> розділ підготовки до сертифікаційних іспитів (Eiken 1-5, TOEIC,
> TOEFL, JLPT N1-N5, Nihongo Kentei 1-3, по 10 оригінальних питань
> кожен), який після оцінювання передає неправильні відповіді
> ШІ-тренеру (з автоматичним перемиканням у режим «японський клас» для
> JLPT/Nihongo Kentei), вибір «яку мову вивчати» та інсталятори для
> Linux/macOS (`installer/unix/install.sh`). Чесне розкриття: ваги
> моделі (родина GPT-2, модель ембеддингів) не включені до APK —
> ШІ-чат на Android досі вимагає вручну розмістити файли моделі у
> внутрішньому сховищі (автозавантаження ще немає). Див. записи
> HANDOFF від 2026-08-11 (продовження 7-10) у [CLAUDE.md](CLAUDE.md).

> 📌 **Попереднє оновлення (2026-08-11, продовження 3)**: Додано
> автоматичне самооновлення (лише Windows), яке під час запуску
> перевіряє останню версію на GitHub і, якщо вона новіша, автоматично
> видаляє стару та встановлює нову. Чесне розкриття: реліз на GitHub
> ще не створено, тому повний цикл видалення→встановлення ще не
> перевірено наскрізно (логіку порівняння версій та шлях «реліз не
> знайдено, безпечно продовжуємо» перевірено).

> 📌 **Попереднє оновлення (2026-08-11, продовження 2)**: Додано
> розпізнавання тем пошуку роботи/зміни кар'єри/туризму, яке представляє
> aruaru.tokyo, audiocafe.tokyo/aruaru, audiocafe.tokyo/aruaru-lady та
> nasa.tokyo англійською та японською — працює як у звичайному чаті,
> так і в режимі тренування, перевірено наживо.

> 📌 **Попереднє оновлення (2026-08-11, продовження)**: Підключено нову
> гео-/туристичну базу даних (усі 47 японських префектур, 50 штатів
> США, основні світові столиці з визначними місцями/їжею/сувенірами)
> для динамізації тренування самопрезентації. Коли згадується гора
> Фудзі, застосунок тепер показує двомовне попередження про безпеку
> (одягати лижний одяг + шолом, заздалегідь бронювати гірську хатину),
> а також реальну інформацію про хатини/автобуси/магазини спорядження
> та пошук бронювання турів. Додано UI вибору вікової групи/рівня/
> ділової англійської. Перевірено наживо на реально працюючому
> `aruaru-llm` + статичному сервері (знайдено та виправлено 3 реальні
> баги).

> 📌 **Попереднє оновлення (2026-08-11)**: Додано панель налаштувань
> для збереження власного API-ключа/cx Google Search прямо з браузера
> (`POST /v1/settings/google-search`, лише в пам'яті, ніколи не
> записується на диск). Інсталятор для Windows (`installer/windows/`,
> Inno Setup) було реально зібрано, встановлено, запущено та видалено
> на реальному обладнанні (без прав адміністратора).

> 📌 **Попереднє оновлення (2026-08-10, продовження)**: (1) Перемкнено
> модель за замовчуванням з `gpt2` (124M) на `distilgpt2` (82M), ~42%
> швидше. (2) Прийнято рішення **не** портувати фронтенд-JS на
> Rust/WASM (немає виграшу в продуктивності, а `SpeechRecognition` не
> має стандартної прив'язки web-sys) — натомість **локальний файловий
> сервер портовано на Rust** (новий крейт `server/`, побудований на
> `open-runo-poem-compat` з RPoem, що усуває залежність від `python3
> -m http.server`). (3) Покращено обробку японського вводу, щоб
> гібридні (англо-японські) відповіді були гарантовані завжди. (4)
> Додано керування версіями (`version.json` із семантичним полем
> `version`, що відображається у підвалі) та автоматичне очищення
> слідів старих версій на боці браузера.

> 📌 **Недавнє оновлення (2026-08-10)**: Додано підтримку CORS,
> усунуто першопричину виродженого циклу повторень жадібного
> декодування GPT-2 (`generate_with_repetition_penalty` з
> `open-cuda`, штраф за замовчуванням 1,3), скориговано зовнішній
> вигляд персонажа Тора-сан + додано джингл при перемиканні + виправлено
> його самопрезентацію, додано крок тренування на основі реальної
> техніки обслуговування клієнтів справжнього мейд-кафе в Акіхабарі
> (@ほぉ～むカフェ), досліджено та додано крок про поточний бум японської
> культури за кордоном (аніме/манга, аніме-пісні, ігри, ті, хто вивчає
> японську мову, колекціонування госюін, туризм онсен/рьокан,
> японська їжа), додано іконки запуску для Windows/Mac/Linux/Android/
> iPhone/iPad та реалізовано механізм автооновлення.

Веб-застосунок (Фаза 0) для вивчення розмовної англійської для
ПК/планшета/смартфона. У стилі «уроків англійської в мейд-кафе»,
персонаж дівчинки-чарівниці-покоївки (оригінальний дизайн,
анімований) супроводжує учнів від повного новачка до просунутого
рівня.

## Архітектура (за вказівкою користувача, 2026-08-10)

- **Сторона Linux (VPS)**: лише сервер роздачі для завантаження (не
  місце, де реально працює застосунок). Керування застосунком виконує
  [`open-easy-web`](https://github.com/aon-co-jp/open-easy-web).
- **Сторона пристрою користувача (ПК/планшет/телефон)**: статичний
  веб-фронтенд цього репозиторію (HTML/CSS/JS, працює в браузері) +
  локально запущений нативний сервер з
  [`aruaru-llm`](https://github.com/aon-co-jp/aruaru-llm) (який
  всередині використовує бекенди виведення `open-directx`/`open-cuda`),
  який користувач завантажує та запускає сам. Браузер локально
  підключається (онлайн чи офлайн) до `http://localhost:4600` (порт
  aruaru-llm за замовчуванням) — «гібридний» дизайн.

## Поточний обсяг (Фаза 0) — чесне розкриття

- **Якість ШІ-відповідей**: `/v1/generate` `aruaru-llm` виконує
  авторегресивну генерацію тексту за допомогою GPT-2 (124M-1,5B, з
  акцентом на англійську, без донавчання для діалогу). Плавність або
  відповідність рівню не гарантовані — це зазначено на екрані, без
  перебільшень. З 2026-08-10 штраф за повторення (за замовчуванням
  1,3) усуває раніше повідомлений баг з нескінченним циклом повторень.
- **CORS**: усунено з 2026-08-10 — HTTP-сервер `aruaru-llm` тепер
  надсилає заголовки `Access-Control-*`, тож цей фронтенд можна
  відкрити з іншого джерела (або через `file://`) і все одно
  достукатися до `http://localhost:4600`.
- **Вибір рівня**: селектор рівня від новачка до просунутого є в UI,
  але реальне дотримання рівня обмежене короткою інструкцією в
  промпті — GPT-2 не гарантує її дотримання.
- **Голос/TTS**: підключено справжній Web Speech API (SpeechSynthesis
  для виводу, SpeechRecognition для вводу з мікрофона) з
  налаштуванням висоти тону/швидкості для кожного персонажа.
- **Режим тренування**: детермінований сценарій самопрезентації (не
  згенерований ШІ), який тепер також включає засновану на ключових
  словах техніку розмови справжнього мейд-кафе в Акіхабарі та крок,
  що підсумовує поточний бум японської культури за кордоном.
- **Іконки запуску**: `icons/` + `manifest.json` (PWA) + `launchers/`
  дозволяють запускати застосунок з іконки на робочому столі
  (Windows/Mac/Linux) або з іконки на головному екрані
  (Android/iPhone/iPad).
- **Автооновлення**: `auto-update.js` опитує `version.json` кожні 5с
  і перезавантажує сторінку при зміні build ID. **Відоме обмеження**:
  деякі браузери блокують `fetch()` локальних файлів за схемою
  `file://` — гарантовано працює при роздачі через локальний
  HTTP-сервер, інакше тихо вимикається.

## Необхідні інсталятори (додано 2026-08-17)

Щоб запустити open-english, потрібно завантажити та встановити наступні
два компоненти (збірка з вихідного коду не потрібна, майже встановлення
в один дотик).

| # | Що | Windows | Linux | Android/планшет |
|---|---|---|---|---|
| 1 | **сам open-english** (цей репозиторій — статичний фронтенд + сервер роздачі) | [open-english-install.exe](https://github.com/aon-co-jp/open-english/releases/latest/download/open-english-install.exe) | [tar.gz](https://github.com/aon-co-jp/open-english/releases/latest) | [APK](https://github.com/aon-co-jp/open-english/releases/latest) (оберіть ассет `.apk`) |
| 2 | **aruaru-llm** (рушій відповідей ШІ — обов'язковий, без нього чат не працює) | [aruaru-llm-windows-x86_64.zip](https://github.com/aon-co-jp/aruaru-llm/releases/latest/download/aruaru-llm-windows-x86_64.zip) | [tar.gz](https://github.com/aon-co-jp/aruaru-llm/releases/latest) | Вже включено (вбудовано в APK open-english, окреме встановлення не потрібне) |

**Чесне розкриття**: посилання "latest" вище завжди вказують на
найновіший GitHub Release (якщо потрібна конкретна зафіксована версія,
скористайтеся
[сторінкою Releases](https://github.com/aon-co-jp/open-english/releases)
напряму). Готової збірки `aruaru-llm` під macOS поки немає (сам
open-english постачається у вигляді tar.gz для macOS, а `aruaru-llm` —
лише для Linux/Windows) — на macOS `aruaru-llm` доведеться збирати з
вихідного коду.

У Windows/Linux/macOS після встановлення вбудована функція
автооновлення (`server/src/self_update.rs`, розширена на Linux і
macOS 2026-08-19) під час запуску перевіряє GitHub Releases і, якщо є
новіша версія, автоматично оновлюється (Windows: видалення→
встановлення; Linux/macOS: працюючий бінарний файл замінює сам себе)
— без жодних дій користувача. Перед застосуванням оновлення поточний
бінарний файл резервується; після запуску нової версії перевірка
працездатності через новий ендпойнт `/healthz` має пройти успішно за
короткий час, інакше застосунок автоматично відкочується (downgrade)
до збереженої попередньої версії. **Чесне розкриття**: Android/
iPhone/iPad виключені з цього механізму автооновлення/відкату (ОС не
дозволяє повністю тихе встановлення APK) — сповіщення про оновлення
там все ще вимагають ручного встановлення користувачем (і шляху для
відкату там теж немає).

Також додано нову вхідну сторінку `facebook.html` для користувачів,
чий мобільний тариф дозволяє доступ лише до Facebook — чесне
розкриття: це не офіційне партнерство "Free Basics" з Meta, а лише
звичайна сторінка, доступна з вбудованого браузера Facebook, що
вказує на наявні інсталятори.

*(Примітка про машинний переклад: цей абзац перекладено самим
ІІ-агентом, без вичитки носієм мови.)*

## Як запустити

1. Запустити [`aruaru-llm`](https://github.com/aon-co-jp/aruaru-llm)
   командою `cargo run --release` (за замовчуванням
   `http://localhost:4600`, модель за замовчуванням тепер
   `distilgpt2`).
2. У каталозі `server/` виконати `cargo run --release`, щоб роздавати
   статичний фронтенд цього репозиторію за адресою
   `http://127.0.0.1:4601/` (на основі RPoem — `python3 -m
   http.server` більше не потрібен; порт можна змінити змінною
   середовища `OPEN_ENGLISH_SERVER_BIND`).
3. Відкрити `http://127.0.0.1:4601/` у браузері. Відкриття
   `index.html` напряму через `file://` досі працює, але деякі
   браузери блокують там `fetch()` і вимикають автооновлення —
   рекомендовано сервер з кроку 2.

## Подальші кроки

1. ~~Підтримка CORS на боці `aruaru-llm`~~ **Зроблено (2026-08-10)**.
2. ~~Цикл повторень жадібного декодування GPT-2~~ **Першопричину
   усунуто (2026-08-10, штраф за повторення)**.
3. ~~Пришвидшити модель за замовчуванням~~ **Зроблено (2026-08-10,
   перехід на distilgpt2, ~42% швидше)**.
4. ~~Гарантувати гібридні відповіді для японського вводу~~ **Зроблено
   (2026-08-10)**.
5. ~~Портувати локальний файловий сервер на Rust~~ **Зроблено
   (2026-08-10, крейт `server/`)**. Портування самого фронтенд-JS на
   Rust/WASM було оцінено та відхилено (немає виграшу в
   продуктивності — див. `CLAUDE.md`).
6. Додати доопрацювання TTS/липсинку.
7. Реалізувати навчальну програму за рівнями (граматика, списки
   лексики тощо).
8. **(за вказівкою користувача, 2026-08-10)** Майбутня ідея — запускати
   `open-directx`/`open-cuda`/`aruaru-llm` прямо в браузері
   (WASM/WebGPU) та інтегрувати з `RPoem` (платформою GraphQL
   Federation). Це масштабний, окремий архітектурний напрям порівняно
   з поточним дизайном Фази 0, відкладений до завершення MVP.
9. Дослідити, чи мають реальне застосування тут техніки Toshiba SBM
   або родини DeepSeek (ще не розпочато).

---

Інші мови: [日本語](README.md) · [English](README-English.md) ·
[Deutsch](README-German.md) · [Italiano](README-Italian.md) ·
[Français](README-French.md) · [Русский](README-Russian.md) ·
[עברית](README-Hebrew.md) · [فارسی](README-Persian.md)

---

## 🎓 Репетиторський курс: збереження історії навчання (рекомендовано перед початком)

**Перед використанням репетиторського курсу радимо налаштувати базу даних для
історії навчання** — **aruaru-db** або звичайний **сервер PostgreSQL**. Тоді
збережеться, які класи та предмети ви вивчали, які завдання розв'язали та які
були результати, і згодом ви зможете переглянути свій шлях. Без бази даних
результати залишаються лише у вбудованому локальному файлі SQLite (втрачається
під час перевстановлення чи зміни пристрою), а вибір класу й предметів — лише
в localStorage браузера.

Обидва варіанти налаштовуються однаково: `OPEN_ENGLISH_DATABASE_URL` із рядком
підключення PostgreSQL (aruaru-db використовує той самий протокол). **Чесні
застереження**: підключення без TLS, тож керовані сервіси PostgreSQL з
обов'язковим SSL поки не працюють, а чистий PostgreSQL ми наскрізно не
перевіряли.

- **Подвійна (DUAL) конфігурація — aruaru-db + PostgreSQL**: дві бази, що
  копіюють одна одну, захищають від відмови однієї з них. **Як є насправді**:
  open-english пише у SQLite та в *одну* базу; **одночасний запис у дві бази
  ще не реалізовано**. Наразі DUAL досягається через `DUAL_DATABASE_URL`
  самої aruaru-db (open-english → aruaru-db → PostgreSQL).
- **Резервне копіювання rsync**: базу *можна додати* до резервного копіювання
  на зовнішній диск, інший ПК або сервер у панелі «💾 Data & Model Storage»
  (автоматично це не відбувається). **Чесний висновок**: у `open-easy-web` ми
  не знайшли механізму rsync — доступна саме вбудована функція open-english.
- **Google Диск**: за допомогою [rclone](https://rclone.org/drive/) папку з
  копією можна синхронізувати:
  `rclone sync /path/to/backup gdrive:open-english-backup`. **Автоматично
  нічого не синхронізується** — налаштування виконуєте ви самі.
- **Хостинг / VPS**: будь-який вузол, доступний через SSH (тарифи з SSH, як-от
  Lolipop чи Sakura Internet, VPS на кшталт ConoHa), працює з rsync:
  `rsync -avz /path/to/backup user@your-vps-host:/backup/open-english/`.
