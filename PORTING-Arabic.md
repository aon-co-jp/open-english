# PORTING.md — دليل نقل open-english (نسخة مختصرة)

> **ملاحظة**: هذه ترجمة مختصرة. الدليل التقني الكامل بتفاصيل الكود
> والعقبات يبقى متاحًا باليابانية فقط في [PORTING.md](PORTING.md) — يجب
> مراجعته قبل تبنّي أي نمط فعليًا.

ملخص أنماط التنفيذ القابلة لإعادة الاستخدام من هذا المشروع، في حال
نُقلت إلى مشروع آخر:

1. **نمط التكامل مع `aruaru-llm`**: يتطلب دعم CORS (`.with_cors()`)
   وعقوبة تكرار (`generate_with_repetition_penalty`، الافتراضي 1.3) في
   جانب `aruaru-llm`، وإلا يحجب المتصفح `fetch()` أو يقع فك الترميز
   الجشع لـ GPT-2 في حلقة تكرار.
2. **استخراج اللغة لـ TTS** (`extractSpeechText`): يفصل الأسطر ثنائية
   اللغة بصيغة "English / 日本語" قبل تمريرها إلى
   `SpeechSynthesisUtterance`، لمنع النطق المتقطع.
3. **مجموعة أيقونات الإطلاق** (`icons/` + `manifest.json` +
   `launchers/`): ترميز PNG/ICO مكتوب يدويًا بدون أدوات رسومية خارجية،
   بالإضافة إلى سكربتات لملف `.lnk` في ويندوز و`.desktop` في لينكس
   و`.app` في ماك.
4. **التحديث التلقائي** (`auto-update.js` + `version.json`): فحص بسيط
   لمعرّف البناء (`buildId`) كل 5 ثوانٍ مع `location.reload()`.
5. **خادم ثابت مبني على RPoem** (`server/`): يستبدل `python3 -m
   http.server` بحزمة Rust تعيد استخدام `static_file_handler` من
   `open-runo-poem-compat`.
6. **ضمان بنيوي للردود الهجينة** (`ensureHybridReply`): يضيف تلقائيًا
   ملاحظة يابانية قصيرة إذا لم يحتوِ رد النموذج على يابانية — دون
   المبالغة في جودة الترجمة.
7. **إدارة الإصدارات + تنظيف الإصدارات القديمة في جانب المتصفح**:
   `version.json` بحقل `version`، وحذف مفاتيح `localStorage` الخاصة
   بالتطبيق عند صدور إصدار جديد.
8. **جسر Google Custom Search JSON API + لوحة إعدادات في المتصفح**:
   بيانات الاعتماد تُحفظ في ذاكرة العملية فقط، ولا تُكتب أبدًا على
   القرص.
9. **مثبّت ويندوز (Inno Setup) — خطوات تحقق فعلية**:
   `PrivilegesRequired=lowest` لتجنّب توقف UAC، و`MSYS_NO_PATHCONV=1`
   للتثبيتات الصامتة من Git Bash.
10. **قاعدة بيانات جغرافية/سياحية مع تحذيرات أمان مدفوعة بالموضوع**:
    بحث بالسلسلة الجزئية بدلاً من المطابقة الدقيقة؛ للمواضيع الخطرة
    (مثل تسلّق الجبال)، يجب دائمًا إرفاق تحذير أمني.
11. **التحديث التلقائي عبر GitHub Releases (ويندوز/لينكس/ماك، مع تراجع
    تلقائي)**: يجب تنفيذه في الجانب الأصلي (وليس JS المتصفح)؛ لا يمكن
    حذف ملف .exe الجاري تشغيله بنفسه على ويندوز — يجب أولاً تشغيل
    سكربت batch منفصل، ثم إنهاء العملية نفسها. *(تحديث 2026-08-19،
    ترجمة آلية)*: تم التوسّع إلى لينكس/ماك (استبدال الملف الثنائي الجاري
    تشغيله، ممكن لعدم وجود قفل ملفات على أنظمة يونكس)؛ **التراجع
    التلقائي** يستعيد النسخة الاحتياطية إذا لم يستجب الملف الثنائي
    الجديد على `/healthz` خلال 12 ثانية. أندرويد/iOS يبقيان خارج
    النطاق (قيد المنصة، لا يمكن التثبيت الصامت الكامل).
12. **فئات CSS للنوافذ المنبثقة تتطلب `max-height`/`overflow-y`**: يجب
    أن تحدد كل فئة حاوية نافذة منبثقة قابلية للتمرير، وإلا يصبح المحتوى
    الطويل غير قابل للوصول على الهاتف المحمول.
13. **تضمين خادم Rust في أندرويد**: يجب جعل المسارات قابلة للتجاوز في
    وقت التشغيل عبر متغيّر بيئة؛ ويجب دائمًا حماية منطق التحديث التلقائي
    الخاص بويندوز فقط بـ `cfg!(target_os = "windows")`.

**تنويه هام**: هذا المشروع نموذج أولي في المرحلة 0 — جودة استجابات
الذكاء الاصطناعي (المبنية على GPT-2)، وطبيعية تحويل النص إلى كلام،
والالتزام الموثوق بالمستوى، كلها غير مضمونة. يجب الإفصاح عن ذلك بوضوح
في أي عملية نقل أيضًا.

---

لغات أخرى: [日本語 (الأصل، تفاصيل كاملة)](PORTING.md) ·
[Deutsch](PORTING-German.md) · [Italiano](PORTING-Italian.md) ·
[Français](PORTING-French.md) · [Русский](PORTING-Russian.md) ·
[Українська](PORTING-Ukrainian.md) · [עברית](PORTING-Hebrew.md) ·
[فارسی](PORTING-Persian.md)

## نمط: اختبارات أصلية متعددة اللغات + اختيار اللغات + القراءة (2026-08-22)

1. افصل طبقة البيانات في ملفين: أسئلة الاختبار وجمل القراءة لهما غرضان
   مختلفان.
2. وفّر واجهة ملخّص دون نص الأسئلة (`GET /v1/world-languages`)، ولا
   تحمّل ملف JSON الكامل إلا عند بدء الاختبار فعليًا.
3. لا تُعد كتابة واجهة الاختبار القائمة: يكفي إضافة `<optgroup>` بقيم
   `world:<code>` وإعادة استخدام التصحيح والانتقال إلى المدرّس.
4. افرض الحد الأدنى والأقصى لعدد اللغات في مربعات الاختيار؛ وعند بلوغ
   الحد عطّل **غير المحددة فقط**.
5. أعد استخدام محرك القراءة الحالي مع إضافة جدول ربط رمز اللغة بـBCP-47؛
   وصرّح بأمانة أن النص يُعرض فقط إن لم يكن الصوت مثبتًا.
6. اعرض النصوص في `<textarea>` بخاصية `readonly` (قابلة للنسخ)، واحفظ
   عبر مسار الحفظ الموجود دون إنشاء جداول جديدة.

## نمط: ردود بنصّ ثابت قائمة على قواعد في الموضوعات الحسّاسة (2026-08-23)

١. ثلاثة مكوّنات: دالّة كشف بالكلمات المفتاحية + جدول نصوص مفتاحه رمز
   اللغة + دالّة تبني الردّ ثنائي اللغة. تُوضع في معالج الإرسال **قبل**
   مسار الذكاء الاصطناعي.
٢. اجمع بين كلمة الموضوع وكلمة القصد كي لا تخطف كلمة «إيران» العابرة
   المحادثةَ العادية. وفي الكلمات الإنجليزية ضع حدّ الكلمة `\b` **في
   البداية فقط**، وإلا ضاعت «Zoroastrianism» (خطأ وقع فعلًا).
٣. افحص الموضوعات الأكثر تحديدًا أولًا (٦٦٦ قبل تاريخ الأديان).
٤. اكتب ضمانات الأمانة داخل النصّ نفسه: الفرضيات **تُعرض**، والأساطير
   الحضرية **تُسمّى كذلك** (مع الشرح التقني متى أمكن، كأشرطة حراسة
   الباركود)، والمصادفات **تُسمّى مصادفات**؛ و«التشابهات الطريفة» بين
   النصّ القديم وواقع اليوم **تُعرض بوصفها احتمالًا فقط** («يقول بعضهم
   ذلك») ولا تُقرَّر أبدًا بوصفها نبوءةً تحقّقت (مثال: سفر الرؤيا
   ١٣: ١٦-١٧ «من دون العلامة لا شراء ولا بيع» إلى جوار الباركود
   والمدفوعات الإلكترونية مثل Amazon).
٥. التحفّظات المطلوبة **ليست ملزمةً** بأن تكون تنبيهات رسمية جافّة: فصياغة
   خفيفة دافئة مقبولة تمامًا ما دام القارئ يفهم بوضوح أنه لا يُثبَت شيء
   (مثل: «خذوا هذا طُرفةً مسلّية لا دليلًا راسخًا»). والمعيار هو **«هل
   قُرِّر شيء بوصفه واقعة؟»** لا النبرة. لكن لا يجوز أن ينزلق التلطيف إلى
   صياغات تُقرأ تقريرًا («تحقّقت النبوءة»، «هذا يحدث فعلًا»).
٦. وثّق ما استُبعد وسببه — في نصّ الردّ وفي تعليق داخل الشيفرة («يُقرأ
   قبل التعديل»).

## نمط: ردّ بنصّ ثابت + تبادل من مرحلتين برايةٍ واحدة (2026-08-23)

١. استعمل المكوّنات الثلاثة نفسها من النمط السابق (كشف بالكلمات المفتاحية
   + جدول نصوص مفتاحه رمز اللغة + دالّة تبني الردّ متعدّد اللغات)، قبل
   مسار الذكاء الاصطناعي. والمثال هنا لغز المؤلّف الأصلي: أربعة أرقام ٩
   وعلامات `+ - × ÷` للوصول إلى `9 ◯ 9 ◯ 9 ◯ 9 = 10`؛ والجواب
   `(9 × 9 + 9) ÷ 9 = 10`.
٢. **لا تُسلّم الحساب والمنطق إلى النموذج.** فنموذج GPT-2 المجرَّد يولّد
   المسائل الحسابية وتصحيحها خطأً وبثقة تامّة؛ وكلّ ما له جواب **صحيح أو
   خاطئ قطعًا** ينبغي أن يكون نصًّا ثابتًا مكتوبًا يدويًا ومراجَعًا سلفًا،
   دون استهلاك حصّة الاستدلال.
٣. للتبادل ذي المرحلتين (سؤال ← «لا أعرف» ← جواب) **لا تبنِ آلة حالات**:
   تكفي رايةٌ منطقية واحدة على مستوى الوحدة (`quizAwaitingAnswer`).
٤. **ترتيب الفحص مهمّ**: افحص «هل نحن بانتظار الجواب؟» **قبل** كشف طلب
   جديد، ليُقدَّم إعطاء الجواب على إعادة طرح المسألة.
٥. الكلمات المفرطة في العموم (مثل «لا أعرف») **لا تُفحص إلا والراية
   مرفوعة**، وإلا خطفت المحادثة العادية.
٦. كن صادقًا في تغطية اللغات: ترجم فقط ما رُوجع فعلًا (هنا سبع لغات)
   وأرجِع الباقي إلى اللغة الافتراضية؛ ولا تملأ ١٣٠ لغة بترجمة آلية
   ليبدو الأمر «دعمًا كاملًا».
٧. اكتب خصائص المسألة الحاسمة داخل النصّ نفسه (مثل «هذا ليس سؤالًا
   مخادعًا بل حسابٌ محض»)، وإلا ضلّ المستخدم الطريق.

## الإرشاد المهني (2026-08-24)

أُضيفت جدول مطابقة خفيف `TUTOR_CAREER_GUIDANCE` (حسب المادة وليس حسب
السؤال) يُعرض أسفل كل سؤال في دورة التدريب الخصوصي: قطاعات/مهن مفيدة
ووظائف متقدمة محتملة، بصياغة حذرة دائماً. التفاصيل والمصادر حول النظام
الألماني المزدوج في PORTING.md (القسم ١٧، باليابانية).

## لوحة أولوية مزودي نماذج اللغة المتعددة (2026-08-26)

يقع التنفيذ من جانب الخادم في `aruaru-llm` (`chat_providers.rs` /
`provider_priority.rs`). يحتوي جانب open-english على الواجهة فقط —
تُحفظ مفاتيح API، مثل إعداد بحث Google، فقط في التخزين المحلي للمتصفح
وتُرسل عند كل حفظ كإعداد وقت التشغيل إلى `POST /v1/settings/
chat-providers` / `POST /v1/settings/provider-priority`. يمكن تغيير
الترتيب إما عبر إدخال رقم أو النقر على زر راديو مرقّم — يجب أن يمر
كلاهما عبر نفس دالة `setPosition()` (تُحل التكرارات بالتبديل). عند فتح
اللوحة، يجب جلب الحالة الفعلية للخادم (نقاط GET) بحيث تظهر المزودات
المُعدّة مسبقًا عبر متغيرات البيئة. التفاصيل في النسخة اليابانية من
PORTING.md.
