# PORTING.md — راهنمای انتقال open-english (نسخه فشرده)

> **یادداشت**: این یک ترجمه فشرده است. راهنمای فنی کامل با جزئیات کد
> و نکات دام‌گیر فقط به ژاپنی در [PORTING.md](PORTING.md) در دسترس
> باقی می‌ماند — پیش از استفاده واقعی از هر الگو به آنجا مراجعه کنید.

خلاصه‌ای از الگوهای پیاده‌سازی قابل استفاده مجدد این پروژه، در صورت
انتقال به پروژه دیگری:

۱. **الگوی یکپارچه‌سازی با `aruaru-llm`**: نیازمند پشتیبانی CORS
   (`.with_cors()`) و جریمه تکرار (`generate_with_repetition_penalty`،
   پیش‌فرض ۱.۳) در سمت `aruaru-llm` است، در غیر این صورت مرورگر
   `fetch()` را مسدود می‌کند یا رمزگشایی حریصانه GPT-2 در حلقه تکرار
   گیر می‌کند.
۲. **استخراج زبان برای TTS** (`extractSpeechText`): خطوط دوزبانه با
   فرمت "English / 日本語" را پیش از انتقال به
   `SpeechSynthesisUtterance` جدا می‌کند تا از تلفظ ناهمگون جلوگیری
   شود.
۳. **مجموعه آیکون‌های راه‌انداز** (`icons/` + `manifest.json` +
   `launchers/`): رمزگذار PNG/ICO دستی بدون ابزارهای گرافیکی خارجی، به
   همراه اسکریپت‌هایی برای `.lnk` ویندوز، `.desktop` لینوکس و `.app`
   macOS.
۴. **به‌روزرسانی خودکار** (`auto-update.js` + `version.json`): بررسی
   ساده `buildId` هر ۵ ثانیه با `location.reload()`.
۵. **سرور استاتیک مبتنی بر RPoem** (`server/`): جایگزین `python3 -m
   http.server` با یک crate در Rust که از `static_file_handler` در
   `open-runo-poem-compat` مجدداً استفاده می‌کند.
۶. **تضمین ساختاری پاسخ‌های ترکیبی** (`ensureHybridReply`): به‌طور
   خودکار یک یادداشت کوتاه ژاپنی اضافه می‌کند اگر پاسخ مدل شامل ژاپنی
   نباشد — بدون اغراق درباره کیفیت ترجمه.
۷. **مدیریت نسخه + پاک‌سازی نسخه‌های قدیمی در سمت مرورگر**:
   `version.json` با فیلد `version`، حذف کلیدهای `localStorage`
   مختص برنامه هنگام نسخه جدید.
۸. **پل Google Custom Search JSON API + پنل تنظیمات در مرورگر**:
   اطلاعات اعتبار فقط در حافظه فرآیند، هرگز روی دیسک.
۹. **نصب‌کننده ویندوز (Inno Setup) — مراحل تأیید واقعی**:
   `PrivilegesRequired=lowest` در برابر گیر کردن UAC،
   `MSYS_NO_PATHCONV=1` برای نصب بی‌صدا از Git Bash.
۱۰. **پایگاه داده جغرافیایی/گردشگری با هشدارهای ایمنی مبتنی بر
    موضوع**: جست‌وجوی زیررشته به‌جای تطابق دقیق؛ برای موضوعات خطرناک
    (مثل کوهنوردی)، همیشه یک هشدار ایمنی همراه کنید.
۱۱. **به‌روزرسانی خودکار از طریق GitHub Releases (فقط ویندوز)**: باید
    در سمت بومی پیاده‌سازی شود (نه در JS مرورگر)؛ فایل .exe در حال
    اجرای خود قابل حذف نیست در ویندوز — ابتدا یک اسکریپت batch مجزا
    اجرا کنید، سپس فرآیند خود را پایان دهید.
۱۲. **کلاس‌های CSS پنجره‌های مودال به `max-height`/`overflow-y` نیاز
    دارند**: هر کلاس ظرف مودال باید قابلیت اسکرول را تعریف کند،
    در غیر این صورت محتوای طولانی در موبایل قابل دسترسی نیست.
۱۳. **گنجاندن یک سرور Rust در اندروید**: مسیرها باید در زمان اجرا از
    طریق متغیر محیطی قابل بازنویسی باشند؛ همیشه منطق به‌روزرسانی
    خودکار مخصوص ویندوز را با `cfg!(target_os = "windows")` محافظت
    کنید.

۱۴. **به‌روزرسانی خودکار چندسکویی با بازگشت (rollback) مبتنی بر بررسی
    سلامت**: تعمیم `apply_update_linux` به `apply_update_unix`
    (Linux و macOS جایگزینی فایل باینری در حال اجرا را به‌صورت
    مشترک انجام می‌دهند)؛ پشتیبان‌گیری از فایل باینری فعلی پیش از
    اعمال به‌روزرسانی؛ پس از راه‌اندازی نسخه جدید، بررسی نقطه پایانی
    جدید `/healthz` در بازه زمانی کوتاه، و در صورت شکست، بازگردانی
    نسخه پشتیبان. Android/iOS به‌صراحت خارج از محدوده باقی می‌مانند
    (محدودیت سیستم‌عامل، نه یک کوتاهی).
۱۵. **ترویج + نصب خودکار RSync**: اگر `rsync` یافت نشود، به‌جای خطای
    خشک، پیامی دوزبانه و دعوت‌کننده ("Let's install RSync!") نمایش
    داده می‌شود، سپس نصب خودکار از طریق زنجیره‌ای از مدیران بسته
    بسته به سیستم‌عامل امتحان می‌شود (winget→choco در Windows،
    apt-get→dnf→pacman در Linux، brew در macOS، pkg در Termux/Android)،
    و در صورت موفقیت بلافاصله پشتیبان‌گیری آغاز می‌شود.
۱۶. **صفحه ورودی اختصاصی برای یک کانال دسترسی محدود** (مانند
    `facebook.html` برای کاربرانی که فقط به Facebook دسترسی دارند):
    یک صفحه ثابت ساده که از پیوندهای دانلود موجود در README دوباره
    استفاده می‌کند، همراه با افشای صادقانه و صریح که دسترسی رایگان
    رسمی از نوع "zero-rated" به دست نمی‌آید — فقط یک نقطه ورودی
    جایگزین است.

*(یادداشت ترجمه ماشینی: موارد ۱۴ تا ۱۶ توسط خود عامل هوش مصنوعی ترجمه
شده‌اند، بدون بازبینی توسط یک گویشور بومی.)*

**هشدار مهم**: این پروژه یک نمونه اولیه فاز ۰ است — کیفیت پاسخ‌های
هوش مصنوعی (مبتنی بر GPT-2)، طبیعی بودن سنتز گفتار و تطبیق قابل
اطمینان با سطح تضمین‌شده نیستند. این موضوع باید در هر انتقالی نیز
به‌وضوح افشا شود.

---

زبان‌های دیگر: [日本語 (نسخه اصلی، جزئیات کامل)](PORTING.md) ·
[Deutsch](PORTING-German.md) · [Italiano](PORTING-Italian.md) ·
[Français](PORTING-French.md) · [Русский](PORTING-Russian.md) ·
[Українська](PORTING-Ukrainian.md) · [עברית](PORTING-Hebrew.md)

## الگو: آزمون‌های اصلی چندزبانه + انتخاب زبان + خواندن (2026-08-22)

۱. لایهٔ داده را به دو فایل جدا کنید: پرسش‌های آزمون و جمله‌های خواندن
   کاربردهای متفاوتی دارند.
۲. یک API خلاصه بدون متن پرسش‌ها ارائه دهید
   (`GET /v1/world-languages`) و فایل کامل JSON را تنها هنگام برگزاری
   آزمون بارگذاری کنید.
۳. رابط آزمون موجود را بازنویسی نکنید: تنها یک `<optgroup>` با مقادیر
   `world:<code>` بیفزایید و نمره‌دهی و انتقال به مربی را دوباره به کار
   بگیرید.
۴. کف و سقف تعداد زبان‌ها را در جعبه‌های انتخاب اعمال کنید؛ در سقف، تنها
   جعبه‌های **انتخاب‌نشده** را غیرفعال کنید.
۵. از TTS موجود استفاده کنید و تنها نگاشت کد زبان به BCP-47 را بیفزایید؛
   صادقانه بگویید که بدون صدای نصب‌شده متن فقط نمایش داده می‌شود.
۶. متن‌ها را در `<textarea>` با `readonly` نشان دهید (قابل کپی) و برای
   ذخیره از نقطهٔ پایانی موجود استفاده کنید، بدون جدول تازه.
