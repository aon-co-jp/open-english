# PORTING.md — מדריך להעברת open-english (גרסה מתומצתת)

> **הערה**: זהו תרגום מתומצת. המדריך הטכני המלא עם פרטי קוד ומכשולים
> נשאר זמין רק ביפנית בקובץ [PORTING.md](PORTING.md) — יש לעיין בו
> לפני אימוץ בפועל של כל תבנית.

סיכום תבניות מימוש הניתנות לשימוש חוזר מהפרויקט הזה, למקרה שיועברו
לפרויקט אחר:

1. **תבנית אינטגרציה עם `aruaru-llm`**: דורשת תמיכת CORS
   (`.with_cors()`) וקנס חזרה (`generate_with_repetition_penalty`,
   ברירת מחדל 1.3) בצד `aruaru-llm`, אחרת הדפדפן חוסם את `fetch()` או
   שהפענוח החמדני של GPT-2 נופל ללולאת חזרה.
2. **חילוץ שפה עבור TTS** (`extractSpeechText`): מפריד שורות
   דו-לשוניות בפורמט "English / 日本語" לפני העברתן ל-
   `SpeechSynthesisUtterance`, כדי למנוע היגוי מקוטע.
3. **ערכת סמלי הפעלה** (`icons/` + `manifest.json` + `launchers/`):
   קידוד PNG/ICO שנכתב ידנית ללא כלי גרפיקה חיצוניים, בתוספת סקריפטים
   ל-`.lnk` של Windows, `.desktop` של Linux ו-`.app` של macOS.
4. **עדכון אוטומטי** (`auto-update.js` + `version.json`): בדיקת
   `buildId` פשוטה כל 5 שניות עם `location.reload()`.
5. **שרת סטטי מבוסס RPoem** (`server/`): מחליף את `python3 -m
   http.server` ב-crate של Rust שמשתמש מחדש ב-`static_file_handler`
   מ-`open-runo-poem-compat`.
6. **הבטחה מבנית לתגובות היברידיות** (`ensureHybridReply`): מוסיף
   באופן אוטומטי הערה קצרה ביפנית אם תגובת המודל אינה מכילה יפנית —
   מבלי להגזים באיכות התרגום.
7. **ניהול גרסאות + ניקוי גרסאות ישנות בצד הדפדפן**: `version.json`
   עם שדה `version`, מחיקת מפתחות `localStorage` ייחודיים לאפליקציה
   בעת גרסה חדשה.
8. **גשר Google Custom Search JSON API + פאנל הגדרות בדפדפן**: פרטי
   גישה בזיכרון התהליך בלבד, לעולם לא על הדיסק.
9. **מתקין התקנה ל-Windows (Inno Setup) — שלבי אימות אמיתיים**:
   `PrivilegesRequired=lowest` נגד תקיעות UAC, `MSYS_NO_PATHCONV=1`
   להתקנות שקטות מ-Git Bash.
10. **מסד נתונים גאוגרפי/תיירותי עם אזהרות בטיחות המונעות לפי נושא**:
    חיפוש תת-מחרוזת במקום התאמה מדויקת; עבור נושאים מסוכנים (למשל
    טיפוס הרים), יש תמיד לצרף אזהרת בטיחות.
11. **עדכון אוטומטי דרך GitHub Releases (Windows בלבד)**: חייב
    להיות ממומש בצד native (לא ב-JS של הדפדפן); לא ניתן למחוק את קובץ
    ה-.exe הפועל בעצמו ב-Windows — יש להפעיל תחילה סקריפט batch
    מנותק, ואז לסיים את התהליך עצמו.
12. **מחלקות CSS של חלונות מודליים דורשות `max-height`/`overflow-y`**:
    כל מחלקת מכולה של חלון מודלי חייבת להגדיר יכולת גלילה, אחרת תוכן
    ארוך אינו נגיש בנייד.
13. **הכללת שרת Rust באנדרואיד**: יש להפוך נתיבים לניתנים לדריסה
    בזמן ריצה באמצעות משתנה סביבה; יש תמיד להגן על לוגיקת עדכון
    אוטומטי ייחודית ל-Windows עם `cfg!(target_os = "windows")`.

**הסתייגות חשובה**: פרויקט זה הוא אב-טיפוס בשלב 0 — איכות תגובות
הבינה המלאכותית (מבוססת GPT-2), טבעיות סינתזת הדיבור והתאמה אמינה
לרמה אינן מובטחות. יש לגלות זאת בבירור גם בכל העברה.

---

שפות נוספות: [日本語 (מקור, פרטים מלאים)](PORTING.md) ·
[Deutsch](PORTING-German.md) · [Italiano](PORTING-Italian.md) ·
[Français](PORTING-French.md) · [Русский](PORTING-Russian.md) ·
[Українська](PORTING-Ukrainian.md) · [فارسی](PORTING-Persian.md)
