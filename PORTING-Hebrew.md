# PORTING.md — מדריך להעברת open-english (גרסה מתומצתת)

> **מאז (2026-08-27)**: תבניות חדשות — כספת iframe חוצת-מקור לסודות, פונקציות עזר להצפנה גנרית (`owEncryptSecret`/`owDecryptSecret`) — ראו PORTING.md (יפנית) לרשומות מ-2026-08-27.


> **הערה**: זהו תרגום מתומצת. המדריך הטכני המלא עם פרטי קוד ומכשולים
> נשאר זמין רק ביפנית בקובץ [PORTING.md](PORTING.md) — יש לעיין בו
> לפני אימוץ בפועל של כל תבנית.

סיכום תבניות מימוש הניתנות לשימוש חוזר מהפרויקט הזה, למקרה שיועברו
לפרויקט אחר:

1. **תבנית אינטגרציה עם `aruaru-llm`**: דורשת תמיכת CORS
   (`.with_cors()`) וקנס חזרה (`generate_with_repetition_penalty`,
   ברירת מחדל 1.3) בצד `aruaru-llm`, אחרת הדפדפן חוסם את `fetch()` או
   שהפענוח החמדני של GPT-2 נופל ללולאת חזרה.
2. **חילוץ שפה עבור TTS** (`extractSpeechText`): מפריד שורות
   דו-לשוניות בפורמט "English / 日本語" לפני העברתן ל-
   `SpeechSynthesisUtterance`, כדי למנוע היגוי מקוטע.
3. **ערכת סמלי הפעלה** (`icons/` + `manifest.json` + `launchers/`):
   קידוד PNG/ICO שנכתב ידנית ללא כלי גרפיקה חיצוניים, בתוספת סקריפטים
   ל-`.lnk` של Windows, `.desktop` של Linux ו-`.app` של macOS.
4. **עדכון אוטומטי** (`auto-update.js` + `version.json`): בדיקת
   `buildId` פשוטה כל 5 שניות עם `location.reload()`.
5. **שרת סטטי מבוסס RPoem** (`server/`): מחליף את `python3 -m
   http.server` ב-crate של Rust שמשתמש מחדש ב-`static_file_handler`
   מ-`open-runo-poem-compat`.
6. **הבטחה מבנית לתגובות היברידיות** (`ensureHybridReply`): מוסיף
   באופן אוטומטי הערה קצרה ביפנית אם תגובת המודל אינה מכילה יפנית —
   מבלי להגזים באיכות התרגום.
7. **ניהול גרסאות + ניקוי גרסאות ישנות בצד הדפדפן**: `version.json`
   עם שדה `version`, מחיקת מפתחות `localStorage` ייחודיים לאפליקציה
   בעת גרסה חדשה.
8. **גשר Google Custom Search JSON API + פאנל הגדרות בדפדפן**: פרטי
   גישה בזיכרון התהליך בלבד, לעולם לא על הדיסק.
9. **מתקין התקנה ל-Windows (Inno Setup) — שלבי אימות אמיתיים**:
   `PrivilegesRequired=lowest` נגד תקיעות UAC, `MSYS_NO_PATHCONV=1`
   להתקנות שקטות מ-Git Bash.
10. **מסד נתונים גאוגרפי/תיירותי עם אזהרות בטיחות המונעות לפי נושא**:
    חיפוש תת-מחרוזת במקום התאמה מדויקת; עבור נושאים מסוכנים (למשל
    טיפוס הרים), יש תמיד לצרף אזהרת בטיחות.
11. **עדכון אוטומטי דרך GitHub Releases (Windows/Linux/macOS, ראו גם
    סעיף 14 עבור rollback)**: חייב להיות ממומש בצד native (לא ב-JS של
    הדפדפן); לא ניתן למחוק את קובץ ה-.exe הפועל בעצמו ב-Windows — יש
    להפעיל תחילה סקריפט batch מנותק, ואז לסיים את התהליך עצמו.
12. **מחלקות CSS של חלונות מודליים דורשות `max-height`/`overflow-y`**:
    כל מחלקת מכולה של חלון מודלי חייבת להגדיר יכולת גלילה, אחרת תוכן
    ארוך אינו נגיש בנייד.
13. **הכללת שרת Rust באנדרואיד**: יש להפוך נתיבים לניתנים לדריסה
    בזמן ריצה באמצעות משתנה סביבה; יש תמיד להגן על לוגיקת עדכון
    אוטומטי ייחודית ל-Windows עם `cfg!(target_os = "windows")`.
14. **עדכון אוטומטי חוצה-פלטפורמות עם חזרה אחורה (rollback) מבוססת
    בדיקת תקינות**: הכללת `apply_update_linux` לכדי `apply_update_unix`
    (Linux ו-macOS חולקים את אותה שיטת החלפת הקובץ הבינארי הפעיל);
    שמירת גיבוי של הקובץ הבינארי הנוכחי לפני החלת עדכון; לאחר הפעלת
    הגרסה החדשה, בדיקת נקודת קצה חדשה `/healthz` בתוך זמן קצר, ובמקרה
    כשלון שחזור הגיבוי. Android/iOS נותרים באופן מפורש מחוץ לתחום
    (מגבלת פלטפורמה, לא מחדל).
15. **עידוד + התקנה אוטומטית של RSync**: אם `rsync` לא נמצא, הצגת
    הודעה דו-לשונית ידידותית ("Let's install RSync!") במקום שגיאה
    יבשה, ואז ניסיון התקנה אוטומטית דרך שרשרת מנהלי חבילות בהתאם למערכת
    ההפעלה (winget→choco ב-Windows‏, apt-get→dnf→pacman ב-Linux‏, brew
    ב-macOS‏, pkg ב-Termux/Android), והמשך ישיר לגיבוי במקרה הצלחה.
16. **עמוד כניסה ייעודי לערוץ גישה מוגבל** (למשל `facebook.html`
    למשתמשים עם גישה ל-Facebook בלבד): עמוד סטטי פשוט המשתמש מחדש
    בקישורי ההורדה הקיימים כבר ב-README, עם גילוי כן ומפורש שאין
    השגה של גישה חינמית רשמית מסוג "zero-rated" — רק נקודת כניסה
    חלופית.

*(הערת תרגום מכונה: הסעיפים 14-16 תורגמו על ידי סוכן הבינה המלאכותית
עצמו, ללא בדיקה של דובר שפת אם.)*

**הסתייגות חשובה**: פרויקט זה הוא אב-טיפוס בשלב 0 — איכות תגובות
הבינה המלאכותית (מבוססת GPT-2), טבעיות סינתזת הדיבור והתאמה אמינה
לרמה אינן מובטחות. יש לגלות זאת בבירור גם בכל העברה.

---

שפות נוספות: [日本語 (מקור, פרטים מלאים)](PORTING.md) ·
[Deutsch](PORTING-German.md) · [Italiano](PORTING-Italian.md) ·
[Français](PORTING-French.md) · [Русский](PORTING-Russian.md) ·
[Українська](PORTING-Ukrainian.md) · [فارسی](PORTING-Persian.md)

## דפוס: מבחנים מקוריים רב-לשוניים + בחירת שפות + הקראה (2026-08-22)

1. לפצל את שכבת הנתונים לשני קבצים: שאלות המבחן ומשפטי ההקראה משמשים
   מטרות שונות.
2. לחשוף API סיכום ללא נוסח השאלות (`GET /v1/world-languages`), ולטעון
   את קובץ ה-JSON המלא רק בעת הפעלת המבחן בפועל.
3. לא לשכתב את ממשק המבחן הקיים: די בהוספת `<optgroup>` עם ערכים
   `world:<code>` ובשימוש חוזר בבדיקה ובמעבר למורה.
4. לאכוף את המינימום והמקסימום של השפות בתיבות הסימון; בהגעה למגבלה יש
   להשבית **רק את התיבות שאינן מסומנות**.
5. להשתמש במנגנון ההקראה הקיים ולהוסיף רק מיפוי קוד שפה ל-BCP-47; לציין
   בכנות שללא קול מותקן הטקסט רק מוצג.
6. להציג את הטקסטים ב-`<textarea>` עם `readonly` (ניתן להעתקה), ולשמור
   דרך נתיב ההתמדה הקיים בלי ליצור טבלאות חדשות.

## תבנית: תשובות בטקסט קבוע מבוסס כללים בנושאים רגישים (2026-08-23)

1. שלושה רכיבים: פונקציית זיהוי לפי מילות מפתח + טבלת טקסטים שמפתחה קוד
   שפה + פונקציה שמרכיבה את התשובה הדו-לשונית. יש למקם במטפל השליחה
   **לפני** המסלול אל הבינה המלאכותית.
2. לשלב מילת-נושא וגם מילת-כוונה, כדי ש"איראן" שנאמרה דרך אגב לא תחטוף
   את השיחה הרגילה. במילות מפתח באנגלית יש להציב את גבול המילה `\b`
   **רק בתחילה**, אחרת "Zoroastrianism" נשמט (באג שאירע בפועל).
3. לבדוק תחילה נושאים ספציפיים יותר (666 לפני היסטוריה דתית).
4. לכתוב את ערובות היושר בגוף הטקסט: סברות **מציגים**, אגדות אורבניות
   **מסמנים ככאלה** (ובמידת האפשר עם ההסבר הטכני, כמו פסי השמירה של
   הברקוד), וצירופי מקרים **מכנים צירוף מקרים**; "הקבלות מעניינות" בין
   טקסט עתיק למציאות של היום **מציגים רק כאפשרות** ("יש האומרים כך")
   ולעולם לא כנבואה שהתגשמה (דוגמה: ההתגלות יג, טז-יז "בלי האות אין קונים
   ואין מוכרים" מול ברקודים ותשלומים מקוונים כמו Amazon).
5. ההסתייגויות הנדרשות **אינן חייבות** להיות דיסקליימרים פורמליים
   ונוקשים: ניסוח קליל וחם מתאים בהחלט, כל עוד ברור לקורא ששום דבר אינו
   מוכח (למשל "קחו זאת כפיסת טריוויה משעשעת ולא כהוכחה של ממש"). המבחן
   הוא **"האם נטען כאן משהו כעובדה?"** ולא הנימה. אך אין לתת לריכוך
   להידרדר לניסוחים שנקראים כטענה ("הנבואה התגשמה", "זה באמת קורה").
6. לתעד מה הושמט ומדוע — גם בטקסט התשובה וגם בהערה בקוד ("לקרוא לפני
   שינוי").

## תבנית: טקסט קבוע + שיחה דו-שלבית על דגל יחיד (2026-08-23)

החידון עם החידה המקורית של היוצר (`9 ◯ 9 ◯ 9 ◯ 9 = 10`, הפתרון
`(9 × 9 + 9) ÷ 9 = 10`) מועבר כהרחבה של תבנית התשובות הקבועות מבוססות
הכללים שלמעלה.

1. אותה שלישייה: זיהוי לפי מילות מפתח (`isQuizRequest()`), טבלת טקסטים
   שמפתחה קוד שפה (`QUIZ_TEXTS`), ופונקציות הרכבה
   (`quizQuestionText()` / `quizAnswerText()`). למקם במטפל השליחה
   **לפני** המסלול אל הבינה המלאכותית; אין לצרוך את המכסה היומית.
2. **את השיחה הדו-שלבית מחזיקים בדגל בוליאני יחיד** —
   `quizAwaitingAnswer`. השאלה מעלה אותו ל-`true`; מזהה נפרד
   (`isQuizAnswerRequest()` — "אני לא יודע", "תגיד לי את התשובה") פועל
   **רק כל עוד הדגל מורם**, ומאפס אותו לאחר הצגת הפתרון. אין צורך במכונת
   מצבים או בשמירת מצב בשרת.
3. **אין למסור את החישוב למודל השפה.** נכונות החשבון חייבת להיות ניתנת
   לאימות חיצוני (מחשבון, חשבונייה), ולכן התשובה היא טקסט קבוע שנכתב ביד.
4. **בחירת השפה בפונקציה קטנה ונפרדת** (`quizPreferredLangCode()`): לקחת
   את שפת הלימוד או את שפת האם, ואם קיים תרגום — להקדים אותו לצמד ברירת
   המחדל היפני-אנגלי.
5. **לסמן ביושר את גבול התרגומים.** קיימים טקסטים רק ל-
   ja/en/es/fr/de/zh/ko; אין למלא את יתר 130 השפות בתרגום מכונה כדי
   להיראות כ"תמיכה בכל השפות" — עדיף צמד ברירת המחדל והודאה מפורשת
   במגבלה.

## הכוונה תעסוקתית (2026-08-24)

נוספה טבלת מיפוי קלה `TUTOR_CAREER_GUIDANCE` (לפי מקצוע, לא לפי שאלה),
המוצגת מתחת לכל שאלה בקורס ההדרכה האישית: ענפים/מקצועות מועילים
ותפקידים מתקדמים אפשריים, בניסוח זהיר תמיד. פרטים ומקורות על המערכת
הדואלית הגרמנית ב-PORTING.md (§17, ביפנית).

## עדכון (2026-08-27)

פריסה על VPS (יש לבדוק דרך `ss -tlnp` לפני שינוי תצורת nginx האם nginx
או תהליך אחר מטפל בפועל בתעבורה), תג מצב הרשת מזהה רק קישור מקומי, לא
פרוקסי הפוך, פאנלים מתקפלים הוכללו לפונקציית עזר אחת לשימוש חוזר, סוכן
GitHub נבדק בפועל (הטוקן נקרא רק במעטפת ה-VPS, לעולם לא נרשם ביומן).
פרטים מלאים רק ביפנית.
